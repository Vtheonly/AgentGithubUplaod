node_modules
dist
dist-electron
release
coverage
.vite
*.log
.DS_Store

# NOTE: tailwind.config.js and postcss.config.js are INTENTIONALLY tracked.
# They were previously gitignored, which silently broke the CSS pipeline.
# Do NOT add them back to .gitignore.
