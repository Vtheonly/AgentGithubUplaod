# Next Task — Selection Guide

> Single starting point for "what should I work on next?" When you start a task, set it `In Progress` in `task-registry.md` and identify it here. When you finish, move it to Completed, append evidence to `change-log.md`, and update this file's recommendation.

## 88th session (2026-09-22, CLOSED) — T-405 COMPLETE — VERIFIED: the cross-year debt aging & payment-behavior system, end to end (rules-first §15 → engine → 0111 → repo → UI → portal parity)

The mandate is fully delivered (the 7 stage commits, each pushed+merged immediately — pull-before-push every time, five clean interleaved merges with the concurrent T-404 agent, zero conflicts): **T-405** — the canonical rules documented FIRST in financial-rules.md **§15** (INV-14/15/16, thresholds pinned to the existing 60/90/180 aging-bucket edges — zero new arbitrary numbers), the pure TS reference engine (`src/domain/calc/ledger/debt-aging.ts`, 32/32 incl. the task's two archetype parents), **migration 0111** applied live atomically (`compute_debt_aging_rows` + the staff-gated `compute_debt_aging_summary` RPC + `attribute_academic_year` + `mv_debt_aging` EXTENDED with the payment-behavior columns — pre-existing columns byte-identical), the repository layer (`DebtRepository.observeAging` — the Supabase RPC client with a live client↔server parity cross-check + the reactive mock + the T-390 realtime recompute hook), the dedicated **« Suivi des Dettes »** desktop tab (the §15 column set + filters + the drill-down drawer + Fiche famille/Encaisser), and the website portal parity (the sha-pinned canonical port + the parent's own DebtAgingStatusCard, tri-lingual). **Live: verify_t-405.sql 29/29** zero-residue (the two archetypes — the SAME 100 000 DZD debt from 2024-2025 — GREEN/active_payer vs RED/critical_delinquency at the pinned clock; the INV-14 row-beats-convention priority; the Créances parity 0 mismatches; the staff gate; the matview preservation). Gates: desktop tsc 0 / FULL vitest 3681/21/5 (the 21 = the documented pre-existing set, t-390 stash-verified) / +45 T-405 tests; website 637/637 + build GREEN + i18n parity. Chain head after this session: **0112** (the concurrent agent's class-subjects FK) — next free **0113**. New knowledge: **AGENTS.md §15.44** (postgres GREATEST ignores NULLs — the live-caught never-paid inversion), **DATA-020 registered** (the pre-existing Créances-tab mock-vs-live summary-basis divergence). Evidence: `docs/recovery/t-405-live-verification.md`.

**Standing recommendation (updated):**
1. The T-405 residuals: the Android §15 mirror (the engine + the 0111 SQL contract are the porting surface); the dashboards/search/exports integration beyond the delivered surfaces; DATA-020's Créances-basis reconciliation.
2. **OPS-319's restore surface** (the standing recommendation).
3. **BUSINESS-105** (the half-refund defect).
4. The owner's §4 EXE acceptance pass + the Personnel matrix + the 0112 apply (owner-gated) + the SCHED-103 smoke re-run.
5. The standing T-337 (REALTIME-105's PORTAL set) / WORKFORCE-503 / the CALC-001 baseline repair.

## 85th session (2026-09-22, IN FLIGHT) — T-401 COMPLETE — VERIFIED: the filière/spécialité classification end to end (T-402/T-403 in progress in the same session)

**T-401** is fully delivered (commits 6e0ece0 → a1453c5 → the closeout, every one pushed+merged immediately; the website's 82473d5 merged too): migration **0107** — the `filieres` catalog (Algerian secondary streams, seeded per academic tenant), the classification columns on classes/students/histories (NULL = untagged), the canonical `fn_track_compatible` compatibility predicate, the class-formation stamping + rejection rules, the import-preserve semantics, the promotion history stamping — plus the desktop domain/UI integration (student forms, batch registration, class creation, placement studio, badges/filters, exports) and the portal parity display. **Live: verify_t-401 17/17** (production, rolled back). Gates: desktop tsc 6 (the pre-existing concurrent-agent baseline), FULL vitest 3589/21/5 (the failing set byte-identical), website 629/629. Chain head **0107** — the next free migration is **0108**. Evidence: `docs/recovery/t-401-live-verification.md`. ADR-019 records the deliberate omissions (no FK, no class_id trigger — Android partial-sync safety).

**T-402 COMPLETE — VERIFIED** (same session): the promotion-path audit (12 surfaces, ONE canonical RPC path, source-guarded) + the desktop history-read gap CLOSED. Live verify_t-402 **9/9** + verify_t-041 regression **10/10**. **T-403 COMPLETE — VERIFIED** (same session): migration **0108** — the promotion-cycle workflow (one active cycle per source year, the class-by-class review with the [NOTES_INCOMPLETES] two-phase ack, the reopen path, the completion gating; the confirm executes through the SAME canonical RPC); the desktop « Cycles de promotion » tab + the cycle detail + the class review; the one-shot per-class modal RETIRED. Live verify_t-403 **16/16**; desktop **3608/21/5** (byte-identical failing set); website 629/629. Chain head **0108** — the next free migration is **0109**. Evidence: t-401/t-402/t-403-live-verification.md. THE THREE-TASK MANDATE (T-401 → T-402 → T-403) IS FULLY DELIVERED.

**Standing recommendation (updated):**
1. The T-401..T-403 follow-ups: wire `subject_configurations.direction` to `classes.filiere_code` (ADR-018's residual, now unblocked); the skip/exception marking from the cycle UI; the studio deep-link after completion; the Android `p_filiere_code` push; the staff-JWT `filieres` RLS probe.
2. **OPS-319's restore surface** (the standing recommendation).
3. **BUSINESS-105** (the half-refund defect).
4. The owner's §4 EXE acceptance pass + the Personnel matrix.
5. The standing T-337 (REALTIME-105's PORTAL set; next free migration **0109**) / WORKFORCE-503 / the CALC-001 baseline repair / the concurrent agent's 6 tsc + 21 vitest fixture drift (their subtree).

**Standing recommendation (unchanged from the 84th session):**
1. **OPS-319's restore surface** (the standing recommendation): `restore_parent`/`restore_student` SECURITY DEFINER RPCs + a Corbeille section in CRM.
2. **BUSINESS-105** (the half-refund defect).
3. The owner's §4 EXE acceptance pass + the Personnel matrix re-runnable.
4. The standing T-337 (REALTIME-105's PORTAL set; next free migration **0108** — SHARED with T-403, coordinate) / WORKFORCE-503 / the CALC-001 baseline repair / the concurrent agent's 6 tsc + 21 vitest fixture drift (their subtree).
5. NEW from T-401: wire `subject_configurations.direction` to `classes.filiere_code` (ADR-018's residual, now unblocked); probe the `filieres` RLS with a staff JWT.

## 84th session (2026-09-21, CLOSED) — T-400 COMPLETE — VERIFIED: the Personnel workforce live parity (0104 completed + applied, the worker chat FIXED live, the Personnel realtime LIVE, the owner's matrix 63/63 + 20/20)

The owner's mandate is fully delivered (commits a71484d → f4924ec → 201bd8d → 3889540 → 9796921 → the closeout, every one pushed+merged immediately; PR #9's 15 branch commits are all in main): **T-400** — the live §15.11 census falsified the handed-over report's "0104 already applied" claim (chain head was 0103, the RPC nonexistent); migration **0104** completed to conventions and applied live (the financial_officer visibility + the ownership-checked `respond_leave_clarification` RPC — the WORKFORCE-500 residual CLOSED); migration **0105** fixed the live-dead worker chat (0061's staff gate omitted the workforce employee roles — the Personnel dashboard's headline action 403'd live); migration **0106** + `SupabaseLeaveRequestRepository.startRealtime()` made the Personnel realtime actually flow (two-session live proof: the manager receives the worker's chat + leave-request events with zero reloads). The branch's 3 pre-existing Personnel test failures realigned to the new contracts. Gates at the documented baseline (tsc 6 / vitest 3572/21/5, the failing set byte-identical — the concurrent agent's). Evidence: `docs/recovery/t-400-live-verification.md`. Chain head **0106** — the next free migration is **0107** (the 83rd-session note "next free 0104" is superseded).

**Standing recommendation (updated):**
1. **OPS-319's restore surface** (the standing recommendation): `restore_parent`/`restore_student` SECURITY DEFINER RPCs (the 0100 mirror) + a Corbeille section in CRM. The deletion buttons remain a one-way door on real data.
2. **BUSINESS-105** (the half-refund defect): widen `revert_payment_allocation`'s lookup to the `payment_id` FK + the financial equivalence suites + the corpus census.
3. The owner's §4 EXE acceptance pass (the 2026-09-18 report's runbook — now ALSO re-check: Settings → Tests CRUD → 27/27, a registration ~1 s, Ctrl+O on any form → filled → submit, AND the Personnel matrix re-runnable via `scripts/t-400-personnel-e2e.py` + `scripts/t-400-realtime-probe.mjs`).
4. The standing T-337 (REALTIME-105's PORTAL set — notifications/installments/payments/homework/assessments; next free migration **0107**) / WORKFORCE-503 (the attendance-insert hardening) / the CALC-001 baseline repair / the concurrent agent's 6 tsc + 21 vitest fixture drift (their subtree).

## 83rd session (2026-09-21, CLOSED) — T-399 COMPLETE — VERIFIED: the Ctrl+O test-data autofill (any form, realistic + validation-respecting, live-proven insertable)

The owner's mandate is fully delivered (commits 8985457 → e8b0d75 → the closeout, every one pushed+merged immediately): **T-399/OPS-321** — `src/shared/devtools/test-data/` mounted once in `app.tsx`: Ctrl+O fills ANY open form (wizard, modals, page forms — zero per-form registration) with a coherent per-press identity (gender-consistent Algerian names from the REUSED fixture pools, a family last name, a run-unique phone in the placeholder's own format, a derived e-mail, ISO dates, in-range DZD amounts, ADR-003 codes), honoring every declared constraint, with REAL options picked for Radix Selects (never the sentinel, gender-consistent) and full safety rails (never submits, never flips switches, never touches file/disabled/readonly inputs). Unit **23/23** (generators pinned against the REAL form validators — PHONE_RE/EMAIL_RE imported from edit-parent-modal); gates: tsc 6 = the pre-existing baseline, lint 0 errors, i18n parity OK, FULL vitest **3572/21/5** (the failing set identical); **LIVE e2e ALL PASS**: the same generators' data through the REAL one-round-trip `batchRegister` in **447 ms**, every value verified against the persisted rows + the billing content + zero residue. Evidence: `docs/recovery/t-399-live-verification.md`. The two discoveries (the act-deferral trap; the Radix synthetic-event contract) are AGENTS.md §15.40.

**Standing recommendation (updated):**
1. **OPS-319's restore surface** (the standing recommendation): `restore_parent`/`restore_student` SECURITY DEFINER RPCs (the 0100 mirror) + a Corbeille section in CRM. The deletion buttons remain a one-way door on real data.
2. **BUSINESS-105** (the half-refund defect): widen `revert_payment_allocation`'s lookup to the `payment_id` FK + the financial equivalence suites + the corpus census.
3. The owner's §4 EXE acceptance pass (the 2026-09-18 report's runbook — now ALSO re-check: Settings → Tests CRUD → 27/27, a registration ~1 s, and Ctrl+O on any form → filled → submit).
4. The standing T-337 (REALTIME-105, next free migration **0104**) / the CALC-001 baseline repair / the concurrent agent's 6 tsc + 21 vitest fixture drift (their subtree).

## 82nd session (2026-09-21, CLOSED) — T-398 COMPLETE — VERIFIED: the ONE-round-trip registration (368 ms live, was 10–20 s on the owner's route) + the pricing-config passthrough + the atomicity upgrade

The owner's follow-up ("is there no way to make it faster????") is fully delivered (commits 5b33f04 → 4dff031 → 2551afe → de5c9aa → the closeout, all pushed+merged immediately): **T-398/PERF-502** — migrations **0102 + 0103** (`register_family_batch`, ONE SECURITY DEFINER transaction reusing the canonical upserts internally + the source_id identity-token substitution that keeps cross-path re-registrations convergent) + the client rewire (all rows built locally through the canonical TS calc engine, ONE `rpcWithIdempotentRetry` call, the wizard's pricing config passed through the input contract) + the ATOMICITY upgrade (any leg failing rolls back EVERYTHING — the DATA-019 partial-success scope decision superseded). **Live: the REAL `batchRegister` 368 ms** (was 6,984 ms original / 3,189 ms post-T-397; the owner's route projects ≈ 0.6–1.2 s, was 10–20 s); N students stay ONE round-trip. Chain **100/100** both projects (next free migration **0104** — the 81st session's "next free 0102" note is superseded). Gates: verify_t-398 12/12 ×2, the real-path e2e 17/17, the T-396 suite re-run 27/27, t-397 8/8 + t-398 4/4, tsc = the 6-error pre-existing baseline, full vitest 3549/21/5 (the failing set identical). Evidence: `docs/recovery/t-398-live-verification.md`.

**Standing recommendation (updated):**
1. **OPS-319's restore surface** (the standing recommendation): `restore_parent`/`restore_student` SECURITY DEFINER RPCs (the 0100 mirror) + a Corbeille section in CRM. The deletion buttons remain a one-way door on real data.
2. **BUSINESS-105** (the half-refund defect): widen `revert_payment_allocation`'s lookup to the `payment_id` FK + the financial equivalence suites + the corpus census.
3. The owner's §4 EXE acceptance pass (the 2026-09-18 report's runbook — now ALSO re-check: Settings → Tests CRUD → 27/27, and a registration should take ~1 s not tens of seconds).
4. The standing T-337 (REALTIME-105, next free migration **0104**) / the CALC-001 baseline repair / the concurrent agent's 6 tsc + 21 vitest fixture drift (their subtree).
5. OPTIONAL follow-up from T-398's design review: the Excel import adapter's per-family orchestration could ride the same `register_family_batch` RPC for its family legs (the importer already uses the bulk billing paths) — registered as a candidate only; measure first.

## 81st session (2026-09-21, CLOSED) — T-396 + IMPORT-110 + T-397 ALL COMPLETE — VERIFIED: the « Tests CRUD » Settings tab (27/27 live) + the registration write path on the bulk paths (21+ → ~11 round-trips, live 6,984 → 3,189 ms) + the live-broken installments bulk upsert FIXED

The owner's report is fully delivered (commits 1169d37 → 5bcc236 → 8694523 → 72f8cd5, all pushed+merged immediately): **T-396/OPS-320** — the « Tests CRUD » Settings tab (the T-393 family extended): the deterministic 27-check backend CRUD suite, **27/27 ALL GREEN live** with zero probe residue; **IMPORT-110** — DISCOVERED by the suite's first live run (24/27): `bulkImportInstallments` was live-broken since birth (42P10 on the PARTIAL 0032 identity index → silent `Ok([])` — the mocked-client tests can never catch PostgREST ON CONFLICT inference failures) — FIXED (`ignoreDuplicates` + honest errors, wire-form pinned 3/3); **T-397/PERF-501 + DATA-019** — the registration billing leg on the bulk paths (21+ → ~11 round-trips; +2 per additional student, not +12; live 6,984 → 3,189 ms; the owner's route projects 10–20 s → ~5–10 s) + the billingWarning honest surfacing + the idempotent network retry on the identity RPCs. Full suite 3542/21/5 (the failing set identical to the 80th-session baseline — the concurrent agent's fixture drift + i18n fallout + CALC-001 mirrors). No migration (next free **0102**).

**Standing recommendation (updated):**
1. **T-398 candidate — the pricing-read leg** (T-397's registered follow-up): pass the wizard's already-loaded pricing config through the `BatchRegistrationInput` contract — cuts 5 more round-trips (→ ~6 total) AND is more correct (the persisted charges would always match the step-3 preview the parent agreed to). Small, clean, well-scoped.
2. **OPS-319's restore surface** (the standing recommendation): `restore_parent`/`restore_student` SECURITY DEFINER RPCs (the 0100 mirror) + a Corbeille section in CRM. The deletion buttons remain a one-way door on real data.
3. **BUSINESS-105** (the half-refund defect): widen `revert_payment_allocation`'s lookup to the `payment_id` FK + the financial equivalence suites + the corpus census.
4. The owner's §4 EXE acceptance pass (the 2026-09-18 report's runbook — now ALSO re-check: Settings → Tests CRUD → 27/27, and a registration should take seconds not tens of seconds).
5. The standing T-337 (REALTIME-105, next free migration **0102**) / the CALC-001 baseline repair / the concurrent agent's 6 tsc + 21 vitest fixture drift (their subtree).

## 80th session (2026-09-21, CLOSED) — T-395 COMPLETE: the 'Famille SIDI — 0554288142' invisibility root-caused + the family restored live (migration 0101) + OPS-319/BUSINESS-105 registered

The owner's report is fully resolved and live-verified: the row existed (the 0063/DATA-011 reconciliation family) but was soft-deleted 2026-09-16 by a manual walk of the new T-384 Supprimer buttons — the desktop filtered it correctly (NOT the AUTH-302 class, proven by the red-phase control). Migration **0101** restored the family + returned its 3 test-half-refunded payments to canonical `paid`, applied to BOTH projects (chain **98/98**, next free migration **0102** — the 79th session's "next free 0101" note is superseded). Full evidence: `docs/recovery/t-395-live-verification.md`.

**Standing recommendation (updated):**
1. **OPS-319's restore surface** (the recommended next task): `restore_parent`/`restore_student` SECURITY DEFINER RPCs (the 0100 mirror — super_admin gate, tenant scope, the active-guard inverse) + a Corbeille section in CRM reading the audit trail. The deletion buttons remain a one-way door on real data until this exists — the exact gap the owner's report exposed. The `parent.restore`/`student.restore` audit-action codes are already codified by 0101.
2. **BUSINESS-105** (the half-refund defect): widen `revert_payment_allocation`'s original-entry lookup to the `payment_id` FK + add the import-sourced-refund scenario to the financial equivalence suites + census both projects for other half-refunds (`status='refunded'` with no reversal entry) before repairing them. Exposure: the whole 2026-08-11 import corpus.
3. The owner's §4 EXE acceptance pass (the 2026-09-18 report's runbook) — the owner re-checks CRM → Parents: « Famille SIDI — 0554288142 » must now appear.
4. The standing T-337 (REALTIME-105, next free migration **0102**) / the CALC-001 baseline repair.

**Concurrent-agent note:** the layout-editor series (43b3332→127c775) left 6 tsc errors + 12 vitest failures in ITS test fixtures (the `editing`-prop drift) — attributed at the 80th session open per §15.14 and deliberately left to that agent's scope (merge safety). No i18n/dictionary surface was touched by T-395 (zero TS files changed).

## 79th session (2026-09-18, CLOSED) — the handed-over Supabase root-cause mandate COMPLETE: T-390..T-394 all VERIFIED

The owner's handed-over task list ("El-Imtiyaz Desktop — Supabase Root Cause & Remediation", 17 September 2026) is delivered end to end: **T-390** (the access-point audit + the backend health proof — RP3–RP6 disproven), **T-391** (the live auth/tenant/RLS matrix + write round-trip, 29/29), **T-392** (AUTH-302/OPS-317/OPS-318 all fixed — commit 3e2642e, 14/14), **T-393** (the « Diagnostic Supabase » Settings tab — the 18-check PASS/FAIL/NON TESTÉ matrix with the AUTH-302 detector + the OPS-317 explainer card, 12/12 + 20/0/1 live, commit 0286f7a), **T-394** (the final report `docs/audits/supabase-root-cause-report-2026-09-18.md` — 14 PASS / 0 FAIL / 1 NOT PROVEN (the EXE install/launch matrix with the §4 owner runbook) + the delivery zip). Every commit pushed+merged immediately. The mandate's TRUE finding: the backend was healthy; the root cause was the client-side session desynchronization (AUTH-302) — now fixed AND permanently visible via the diagnostics screen.

**Concurrent-agent note (historical):** the T-388 i18n agent was active during this session — no i18n/dictionary surface was touched (all new user-visible strings live in the NEW self-contained diagnostics module); doc edits were append-only in fresh sections.

**Standing recommendation:** the owner's §4 EXE acceptance pass (the report's runbook — clean-state install, sign-in + lists, create-child round-trip, restart persistence, diagnostics-on-failure); then T-388 (I18N-501, the concurrent desktop burn-down); then the standing T-337 (REALTIME-105, next free migration 0101); the CALC-001 baseline repair.

## 78th session (2026-09-17, CLOSED) — the data-lineage inspection repair COMPLETE: T-389 (INSPECT-500, live-verified read-only on BOTH backends)

The owner's mandate — "the inspection feature is supposed to explain exactly what, who, how, and where every dashboard metric comes from… the tracing and calculation logic are incorrect" — is delivered as **T-389**: twelve registered defects fixed in the inspector's derivations, data model, tracing and UI. The resolution engine now lives in a NEW pure module (`data-inspector-lineage.ts`) built ENTIRELY on the canonical derivations (installmentRemaining, daysBetweenFloor, agingBucketFromDays, evaluateStudentRiskProfiles, normalizeTransportTier, isRemiseAdjustment — no more parallel GPA/remaining/aging implementations), every record carries structured lineage (amountKind, category, categoryLabel, sourceTable, sourceId, detail), every trigger's sourceValue shares ONE definition with its resolution (the year/all-years debt scope, the exclusive-bound revenue window, the applied aging-bucket filter, the records/absences attendance modes, the ledger-based remise total, the multi-select method/category filters), the transport domain shows transport INSTALLMENTS with amounts + routes (not student counts), the discount domain traces the 318-entry ledger remise corpus (the `student.remise` read was always undefined — the field exists on CreateStudentInput only), and the top-10 contributors render in TEN distinct colors applied consistently (dot, left border, colored amount + percentage, segmented bar, per-record dots). Gates: t-389 suite 34/34; tsc 0 errors (baseline main carried 5 — all fixed); eslint 0 new warnings; FULL vitest 18 failed / 3495 passed with the failure set IDENTICAL to baseline main (git-stash proof — the 18 are the T-388 i18n text-matcher fallout, pre-existing). LIVE (read-only, `scripts/verify_t-389_live.sh`): production revenue **55 227 100 DZD/891 payments** in the exclusive window; year-scoped debt **58 354 700** vs all-years **58 602 700** — the **248 000 DZD live proof** the old trigger manufactured a permanent false écart; the remise corpus **318 entries / 9 709 700 DZD**; the real top-10 debtor ranking. **NOTE for the concurrent agent (T-388 i18n):** this session's code edits are concentrated in the NEW lineage module + the inspector core (files created for this purpose) with surgical trigger edits in analytics-tab.tsx; the new UI strings are French hardcodes that JOIN your burn-down backlog; every commit was rebased on origin/main before push.

**Standing recommendation:** T-388 (I18N-501, the concurrent agent's desktop burn-down — IN PROGRESS, absorbs this task's new strings); then the standing T-337 (REALTIME-105, next free migration 0101); the CALC-001 baseline repair; the INSPECT-500 registered residuals (the enrollment-vs-KPI population unification is a product decision; the aging 0_30-includes-not-yet-due semantic is mirrored, not changed).

## 76th session (2026-09-17, CLOSED) — the student-import HTTP 400 RESOLVED AS SYNC-300 (T-387) + the OPS-316 discovery (T-386 same session)

The owner's handed-over analysis (missing 0023 seed data → restore it in the SQL Editor) was verified live and **DISPROVEN for the current state**: BOTH Supabase projects carry the full canonical reference census (tenants=1, roles=11, permissions=56, default tenant PRESENT, super_admin=56/56; NEW migration parity 97/97 EXACT) and the RPC succeeds on valid args. The reported 400 reproduced deterministically as **SYNC-300**: the desktop sync push's `??` coalescing passes `""` through to `upsert_student_from_import`'s uuid-typed `p_class_id` → PostgREST gateway cast → `22P02 invalid input syntax for type uuid: ""` (HTTP 400 BEFORE the SECURITY DEFINER body runs; the 22007 date sibling proven on `p_date_of_birth`). **The fix (T-387, desktop client-seam only — no server change, matching the ACAD-501 read-path precedent):** `blankToNull()` on every date/timestamptz-typed argument + `uuidOrNull()` (the exported `isUuid` guard) on every optional uuid-typed argument in `default-push-handler.ts`; `types.ts` re-aligned to the live `pg_proc` signature (the 0028 params + the 0031 `out_*` Returns — the generated types were behind the chain, as suspected). Gates: tsc 6 PRE-EXISTING errors (the concurrent agent's dashboard-analytics files aa19a75/d87c7bf — ZERO new), eslint 0 errors, the t-387 suite 14/14, the infrastructure+integration sweep 743 PASS. LIVE: verify_t-387.sql 7/7 on NEW; t-387-rpc-blank-args-e2e.py **14/14 on NEW + 13/14 on OLD** — the single OLD FAIL (G3 migration parity = 96/97) is the **OPS-316 discovery**: production is missing the `0099` REGISTRATION only (verify_t-376.sql re-run on OLD: 12/12 — the helpers are already in the exact 0099 state; T-376 applied 0099 to the NEW project only). **T-386 (same session, separate commit — DONE, VERIFIED): the atomic registration heal** (apply_0099_live.sh → HTTP 201; production 96→97; verify_t-376 12/12 + verify_t-387 7/7 + the E2E 14/14 on OLD post-heal — both projects now report the identical 97/97 history as the canonical chain). AGENTS.md §15.37 added (the blank-string rule + the 0023-re-run trap: its trailing asserts expect `transport_destinations = 4` and RAISE on the canonical post-0089 28 — never "restore" a healthy DB into an exception). **NOTE for the concurrent agent:** this session touched default-push-handler.ts, types.ts, the t-387 test suite, two scripts, AGENTS.md §15 (rule 37 appended at the END — collision-free), and the recovery docs — files disjoint from the dashboard-analytics area (aa19a75…d87c7bf, which carries the 6 tsc errors + 15 suite failures attributed at session open per §15.14).

**Standing recommendation (unchanged):** T-337 (REALTIME-105) — the publication migration (next free number 0101); or the CALC-001 baseline repair (the 3 cross-platform mirror tests). The concurrent dashboard-analytics work (6 tsc errors + 15 red tests at this session's open) belongs to the other agent's in-flight commits — do not build on that subtree until they land their fix.

## 74th session (2026-09-16, CLOSED) — the parent-removal leg + the RLS-500 discovery COMPLETE: T-384 (PARENT-500 + RLS-500, live-verified on BOTH backends)

The owner's clarification of the removal mandate — "when I mean users I mean the student and parents" — is delivered, tested, AND live-verified on both Supabase projects. **T-384 (PARENT-500)**: the CRM → Parents per-row Supprimer action (`Permission.DeleteParent`-gated — already-registered permission, SuperAdmin via defaults, RBAC-matrix-grantable) + the destructive ConfirmModal → `repos.parents.deleteParent` → toast; the mock aligned (honest notFound + the French operator instruction; the guard + audit + store-removal preserved). **THE MID-TASK DISCOVERY (RLS-500)**: the first live E2E run proved the plain PostgREST soft-delete path RLS-IMPOSSIBLE — the 0019 staff SELECT policies carry `deleted_at IS NULL` and PostgreSQL folds the SELECT-policy predicates into the UPDATE's effective WITH CHECK, so every `PATCH {deleted_at}` returns 403 42501 for every authenticated caller (T-381's shipped student-removal surface was live-broken the whole time; the suites never caught it — the repository tests mock the client). **The fix (migration 0100)**: the canonical `soft_delete_parent`/`soft_delete_student` SECURITY DEFINER RPCs (super_admin gate, tenant scope, not_found honesty, the active-students guard for parents, race-safe UPDATE, write_audit_log intent entries) — applied to BOTH projects atomically with registration; the desktop repositories rewired to the RPCs with envelope mapping; AGENTS.md §15.36 added (the rule + the impersonation-probe sub correction + the 201-[] trap). The removal trifecta is now complete AND live-proven: user ACCOUNTS (T-381/T-383), STUDENTS (T-381 + the 0100 repair), PARENTS (T-384). Gates at close: tsc 0 / eslint 0 / 169 files / 3470 tests (3 failed = the pre-existing CALC-001 cross-platform mirror expectations attributed at the 73rd-session open — commit 7355378). LIVE: t-384-parent-removal-e2e.py **17/17 PASS on production AND 17/17 on the fresh clone** (the RED evidence + the RPC round-trip + the audit entries + zero residue). **NOTE for the concurrent agent:** this session touched crm-page.tsx (ParentsTab), the parent/student Supabase repository methods, migration 0100, AGENTS.md §15 (rule 36 appended at the END — collision-free), and the four recovery docs — files disjoint from the T-337 publication-migration area.

**NEXT:** (1) the standing P0 on production: **T-337 (REALTIME-105)** — the publication migration (next free number **0101** — 0100 is now TAKEN by the soft-delete RPCs); (2) the CALC-001 baseline repair — the 3 cross-platform mirror tests (Tier4OperationSequences re-pay idempotency, ScenarioRunner refund_cleared_payment INV-8, Tier4Boundary refund-at-exact-amount) expect the pre-T-315 installment statuses; realign them to the engine (the equivalence-suite discipline applies); (3) BKUP-500's registered residual: the phantom `window.elImtiyaz` declaration in vite-env.d.ts + system-config.ts's dead optional-chained calls (either implement the config bridge on `elImtiyazDesktop` or delete the phantom + dead call sites); (4) the RLS-500 registered residual: if the owner grants DeleteParent/DeleteStudent to a non-super_admin role via the RBAC matrix, the 0100 RPC gates must widen in the same change.

## 73rd session (2026-09-16, CLOSED) — the removal surfaces + the export workflow COMPLETE: T-381/T-382/T-383 (users + students removal; destination+format export; live-verified on BOTH backends)

The owner's three-part mandate is complete and live-verified. **T-381 (USER-500/STUDENT-500)**: the `delete-user-account` Edge Function (the create mirror with the OPS-310 owner-pinned guard + self-deletion guard + the safe profile-before-auth ordering), `UserAccountRepository.deleteAccount` (Supabase + mock), the AccountsTab row action, and the StudentsTab permission-gated Supprimer action (the surfaced soft-delete). **T-382 (BKUP-500)**: the export destination seam (the REAL `window.elImtiyazDesktop.saveFile` Electron bridge — dormant since the shell was written — with the browser fallback), the dependency-free STORE-method ZIP writer, the two mandated formats (the ENTIRE archive: manifest + every vault archive's encrypted ciphertext + per-archive IVs; the ZIPPED EXCEL FILES: the 13-sheet T-368 workbook in a ZIP), the shared ExportDialog (both choices BEFORE the export starts), the BackupTab "Export / Synchronisation" card + the ConfigurationTab "Exports (Excel & Archive)" card. **T-383**: deployed the 15th EF to BOTH projects; the live harness `t-383-remove-users-e2e.py` **28/28 PASS on production AND 28/28 on the fresh clone**; the role-gate probe PASS on both; the 15-EF anonymous-deny sweep 15/15 on both; zero residue. Gates at close: tsc 0 / eslint 0 / 168 files / 3454 tests (3 failed = the pre-existing CALC-001 cross-platform mirror expectations attributed at session open — commit 7355378 changed the refund/re-pay statuses; the owner's pasted analyze/report matches exactly this). **NOTE for the concurrent agent:** this session worked on the desktop removal/export surfaces + the delete-user-account EF; files touched are disjoint from the T-337 publication-migration area. The EF fleet is now 15 on both projects (the census docs updated).

**NEXT:** (1) the standing P0 on production: **T-337 (REALTIME-105)** — the publication migration (next free number 0100); (2) the CALC-001 baseline repair — the 3 cross-platform mirror tests (Tier4OperationSequences re-pay idempotency, ScenarioRunner refund_cleared_payment INV-8, Tier4Boundary refund-at-exact-amount) expect the pre-T-315 installment statuses; realign them to the engine (the equivalence-suite discipline applies); (3) BKUP-500's registered residual: the phantom `window.elImtiyaz` declaration in vite-env.d.ts + system-config.ts's dead optional-chained calls (either implement the config bridge on `elImtiyazDesktop` or delete the phantom + dead call sites).

## 72nd session (2026-09-16, CLOSED) — the fresh empty infrastructure mandate COMPLETE: T-375…T-380 (the NEW project `vebfehrpzajhstyhinnw` is verified + handed over; production untouched)

The owner's "exact clone, zero data, new credentials" mandate is complete and live-verified. The NEW project now carries: 96 migrations through 0099 (incl. the OPS-314 RLS-parity fix), 83/83 tables, 226/226 policies, 100/100 functions, identical triggers/constraints/indexes/buckets (the deep census), the bootstrap admin (active + super_admin, sign-in 200), the 14-EF fleet + the 13-name secret set (CRON_SECRET fresh; GROQ live-proven), auth-config parity on every non-secret-gated field, and ZERO business data (the migration-0063 chain-DML rows only — present on both projects by design). Evidence: verify_t-376.sql 12/12 (T-377), the 0096 terminator parse-proven both directions (T-378/OPS-313 CLOSED), t-379-ef-fleet-matrix.sh 61/61 (T-379), the credentials map + switch runbook (T-380 — `docs/operations/credentials.md` §9). Production health re-verified read-only at close (13/13 + the sb_secret send-push leg: auth passes → 500 FIREBASE residual, the documented owner-gated state). **NEXT:** (1) the OWNER's cutover decision — point the clients at `vebfehrpzajhstyhinnw` (credentials.md §9.4: desktop runtime dialog / website `.env.local` / android root `.env`) and populate fresh data (the Excel import on the new backend), clearing the owner-gated residuals (Google secret + callback, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON); or (2) on production: **T-337 (REALTIME-105)** — the standing P0 publication migration (next free number 0100+, on production; the NEW project self-heals the same way at its next push). NOTE for the concurrent agent: this session's four tasks registered as T-377–T-380; if a concurrent session registered the same numbers first, renumber at merge (the established collision protocol).

## 70th session (2026-09-14, CLOSED) — the account↔employee linkage mandate COMPLETE: T-371 / WORKFORCE-501

The Admin-Settings accounts are now linked to employees at creation (migration 0097 applied live atomically + the EF redeployed), the account-creation workflow is redesigned around the employee picker (Settings → Comptes), the employee sees their own profile/tasks/responsibilities at sign-in (the ProfilePage dossier + the RLS-verified self-reads), and the tasks id-space split-brain is repaired (assignment keys on the ACCOUNT id — the space the schema/RLS always mandated). Gates: tsc 0 / eslint 0 errors / 161 files 3373 tests / build GREEN; LIVE: verify_t-371.sql 8/8 + the REST E2E 22/22 (zero residue). The live chain is 0095→0096→0097→0098 (0098 = the concurrent agent's student-documents task). Residuals: link real employee accounts through the new UI (owner action); post-creation binding from the employee directory is a natural follow-up task.

## How to select the next task safely

1. Open `task-registry.md` and read the **Progress summary** and the current **Ready** section.
2. Verify the candidate's **dependencies** are all `VERIFIED` (not merely "done") and that no `UNKNOWN-xxx` blocks it (see `unknowns.md` and the dependency graph at the bottom of the task registry).
3. Read the candidate's problem entries in `problem-registry.md` — especially *Expected behavior*, *Proposed resolution* and *Dependencies*.
4. Read `current-state.md` for anything that changed since the registry was last updated.
5. Confirm you can name the canonical implementation involved (`source-of-truth.md`) and the verification you will run (`docs/testing/strategy.md`, `docs/testing/cross-platform.md`).
6. If the task touches financial or academic rules, read `docs/domain/financial-rules.md` / `academic-rules.md` first. If your change needs a rule that is not written there, stop — that is an unknown, not a design choice.
7. Record the selected task: set it `In Progress` in `task-registry.md`, note it in this file under "Currently in progress", and commit the doc update before starting implementation.

## 70th session (2026-09-14, CLOSED) — the website↔desktop student-document synchronization (T-372, SYNC-110) — the owner's "documents must be visible on both platforms" mandate

**CLOSED (T-372 COMPLETE, SYNC-110 CLOSED TESTED + the live legs VERIFIED):** the owner's document-sync mandate — for LINDA ALIOUAT (and every student), a document uploaded from the website is now visible on the desktop and vice versa. The storage layer was ALREADY unified (same bucket, same path convention — UPLOAD-101..104 CLOSED); the defect was the metadata split (SYNC-110): the website's `student_documents` TABLE vs the desktop's `students.documents_json` column, with the desktop's full-array write additionally a last-write-wins clobber. The fix unifies on the canonical table: the granular desktop repository contract (addStudentDocument/removeStudentDocument; the full-array write REMOVED from UpdateStudentInput at the TYPE level), the domain kind set widened to the DB CHECK parity (7 kinds), migration 0098's idempotent backfill (applied LIVE atomically, 93→94 — LINDA's 2 documents unified). LIVE evidence: verify_t-372.sql 7/7 + the cross-platform E2E t-372-doc-sync-e2e.py 11/11 (check D: desktop→website; check F: website→desktop — BOTH directions through the platforms' exact query shapes; RLS scoping intact; zero residue). Gates: desktop 161/3364/0 + tsc 0 + eslint 0 + build; website 48/618/0 + lint + strict build (NO website code change — already table-backed). COORDINATION: the concurrent T-371/WORKFORCE-501 session registered "T-371" on the remote first — this session's task was RENUMBERED T-372 at merge (both registrations kept); migration numbering follows (0097 = theirs, 0098 = this task's backfill; the live chain carries a 0097 GAP that self-heals at their next `db push --include-all`). NEXT after this session: (1) **T-337 (REALTIME-105)** — the standing P0 publication migration (next free number 0099+); (2) the UNKNOWN-022 owner decision (the 4 orphaned binaries); (3) the documents_json retention decision (owner-gated); (4) the remaining owner-gated residuals.

## 70th session (2026-09-14, OPEN) — the admin account↔employee linkage (T-371, WORKFORCE-501) — the owner's self-service mandate

**IN PROGRESS (registered at session OPEN, commit 942d3a2):** link the Admin-Settings-created account to the selected employee at creation, and make the employee's own profile/tasks/responsibilities visible at sign-in. Scope a–g: migration 0097, the create-user-account EF, the domain + mock parity, the AccountsTab redesign, the ProfilePage employee dossier, the task id-space fix, the live E2E. INTERIM NOTE (the 69th session): the wrong-key fallback guardrails for the attendance punch landed as T-374/WORKFORCE-502 (the doomed-write symptom of this family's evidence #3) — dormant belt-and-braces once this task's linkage lands.

## 69th session (2026-09-14, CLOSED) — the commit-9ddde68 Class Formation & Placement Studio backend integration (T-370, ACAD-500) + the owner's live-console round (T-373/T-374/OPS-312 — RENUMBERED from T-371/T-372 at merge: the concurrent 70th sessions took T-371/T-372 on the remote first)

**CLOSED (T-370 COMPLETE, ACAD-500 CLOSED TESTED + the owner's pasted live console fully root-caused):** the one-commit-at-a-time mandate's second target — commit 9ddde68 (the Class Formation & Student Placement Studio) — now has its FULL backend wiring: the `ClassPlacementRepository` contract, `SupabaseClassPlacementRepository` on the LIVE `fn_finalize_class_placements` RPC (UUID pre-validation, exact payload mapping, both cache-refresh seams), `MockClassPlacementRepository` validate-all-then-mutate over the shared store, the provider slot, the hook rewired to ONE atomic call (honest failure surfacing, session reset on success), the `ClassPlacementFinalize` audit action, and **migration 0096 committed BYTE-FAITHFULLY from the live definition** (the ARCH-011 drift closed — file + live database now agree). **The owner's mid-session console paste (05:56 UTC) was root-caused line by line:** the six `students?id=eq.<uuid>` PATCH 400s (`22P02 … "draft-cls-A1"`) were ACAD-500 ITSELF in production — root-caused and closed; the two `workforce_attendance_events` 409s were a WRONG-KEY fallback (`session.userId` as personnel_id → 23503 FK — probe-proven) — T-374/WORKFORCE-502 closed (refusal guards in both surfaces, silent error path surfaced — the interim mitigation of the concurrent T-371/WORKFORCE-501 family); the `chat_messages?channel_id=eq.` + `homework?class_id=eq.` 400s were empty-uuid fetches — T-373/ACAD-501 closed (three repository guards, no server round-trip); the `installments`/`ledger_entries` 500s + `classes!inner` 400 were TRANSIENT (identical queries → 200 on replay) — OPS-312 registered OPEN (monitoring; the T-282 delayed-retry pattern if it recurs). **Verification:** the LIVE REST E2E t-370-placement-e2e.py **19/19 GREEN** through the real PostgREST+RLS+RPC path (draft-id mapping, patch persistence, ONE audit entry, atomic rollback, 23505/22023/23503 rejections, zero-residue cleanup); verify_t-370.sql (owner-gated runbook); the live root-cause probe t-374-attendance-probe.py 7/7; gates at close: **tsc 0 / eslint 0 errors / 163+1 files / 3377 tests / 0 failed (+27 new)**. Evidence: `docs/recovery/t-370-live-verification.md`. **NEXT after this session:** (1) **T-337 (REALTIME-105)** — the standing P0 publication migration (NOTE: 0097 is TAKEN by the concurrent WORKFORCE-501 session, 0098 by the SYNC-110 session — T-337 takes the next free number); (2) the remaining UI-only commit (08f7f13) per the owner mandate; (3) the WORKFORCE-500 residual (the worker-side respondClarification RLS widening); (4) OPS-312 monitoring; (5) the owner-gated residuals when supplied.

## 68th session (2026-09-14, CLOSED) — the commit-74d3ebb Personnel & Workforce subsystem backend integration (T-369, WORKFORCE-500)

**CLOSED (T-369 COMPLETE, WORKFORCE-500 CLOSED TESTED):** the owner's one-commit-at-a-time mandate's first target — commit 74d3ebb (the Personnel & Workforce Subsystem UI) — now has its FULL backend: migration 0095 applied LIVE (staff_absences + salary_adjustments + salary_payments + the two audited 0055-hardened RPCs + RLS + the tasks/leave_requests lifecycle columns + the needs_review CHECK widening), the desktop typecheck repaired 31 errors → 0 (the ONE mock on the new contract, the dead parallel deleted, the split-brain re-pinned), the four Supabase repositories extended, the payroll surface RPC-backed, the UI's client-side simulations deleted. Gates: tsc 0 / eslint 0 / 3350 tests / 0 failed / build GREEN. LIVE: verify_t-369.sql 23/23 (two defects caught pre-commit) + the REST E2E 26/26 (the real PostgREST+RLS+RPC+trigger path; zero business residue). AGENTS.md §15.30 added (the file-shadows-directory-index trap). Evidence: docs/recovery/t-369-live-verification.md. REGISTERED RESIDUAL: the worker-side respondClarification write is RLS-blocked by the 0019 manager-only UPDATE policy — needs its own registered task (a role-scoped own-row leave_requests UPDATE policy).

**IN PROGRESS (registered at session OPEN):** the owner's mandate: the recent commits contain UI only with no backend or middleware logic — implement the missing logic for commit 74d3ebbc821d8e7b825576aa83e8b24766d22828 (the Personnel & Workforce Subsystem: PayrollManagement / StaffAttendanceCenter / RequestsManagement / TaskManagement / employee-profile-drawer + the dashboards), one commit at a time, test the UI end to end, update the docs/registries, push+merge after each commit. Session-OPEN evidence: the desktop typecheck is RED at 31 errors (the UI-only commits changed the domain contracts with no repository-layer update); the payroll flows are client-side simulations; the new workforce loops have no persistence; a second parallel mock file (`mock/workforce.ts`) shadowed the `workforce/index.ts` directory index and split-brained the mock layer (T-314 workflow side-effects write to the dead store). T-369 registered IN_PROGRESS; WORKFORCE-500 registered OPEN. NEXT after this session: (1) **T-337 (REALTIME-105)** — the standing P0 publication migration; (2) the remaining UI-only commits from the same owner mandate (9ddde68 — the Class Placement Studio — needs the same backend-implementation treatment); (3) the attendance-KPI display-honesty residual; (4) the owner-gated residuals when supplied.

## 67th session (2026-09-14, CLOSED) — the unified PDF/Report/Excel generation mandate (T-368, REPT-500..505) — the owner's "every broken PDF + the full Excel export" mandate

**CLOSED (T-368 COMPLETE, REPT-500/501/502/503/504/505 all CLOSED TESTED — REPT-503/505 with the live cross-check evidence):** the trigger was the owner's explicit mandate (fix EVERY PDF generation across finance/payments/receipts/reports/dashboards/students/grades/statistics; REWRITE the reporting logic, don't patch it; support BOTH PDF and Excel; export the ENTIRE application's data + statistics to structured Excel; live credentials supplied for verification). The RED proof: `formatDzdPlain` emits U+202F as the fr-FR grouping separator → pdf-lib Helvetica `drawText` THROWS "WinAnsi cannot encode (0x202f)" — the desktop's payment-receipt + account-statement generators crashed on EVERY realistic amount (the website port had fixed this exact seam in T-194/T-195; the desktop reference never got the back-port — the inverted DRIFT-011). Fixes (hub 47fb21e + 827f8c8): the draw-layer seam repaired (sanitize + `dzdPdf` + two-pass `stampPageFooters` with TRUE "Page i/N" on every page + continuation-page row flow — the 25-payment statement cap and every `y<100` silent truncation removed); NEW `excel/full-export.ts` (13 domain sheets + canonical Résumé indicators, built OVER the existing export-engine); NEW `receipt-pdf/global-reports.ts` (the five PDF twins — one data path, two renderers); the Reports tab gains the "Export complet" card + honest PDF buttons; the legacy `data-export.ts` summary repaired to the canonical sign convention (REPT-505 — payments are NEGATIVE credits; the old formula inflated the outstanding by 2×|paid|). 22 new tests (RED captured first); full gates GREEN (tsc 0 / eslint 0 / 3323 tests / 0 failed). **LIVE leg:** the real generators ran over the real Supabase rows (261 parents / 907 payments / 2064 ledger entries) — the full workbook (318 KiB), a 28-payment statement (2 pages), the 907-payment revenue PDF (23 pages), the 1 507 000 DZD receipt — and `t-368-verify-live-summary.ts` reconciled EVERY Résumé total with direct SQL aggregates EXACTLY. AGENTS.md §15.29 added (the four reporting safeguards). Full report: `docs/recovery/t-368-live-verification.md`.

**Next session (68th+) should pick, in order:** (1) **T-337 (REALTIME-105)** — STILL the standing P0 publication migration (unchanged recommendation since the 60th session; the 0085 pattern; the portal's five postgres_changes subscriptions target non-publication tables); (2) the attendance-KPI display-honesty residual (the 63rd session's registered follow-up — `kpisForRange.attendanceRateToday` defaults to 1.0 with zero records); (3) the owner-gated residuals when supplied (the Firebase WEB app + VAPID key per the runbook, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (4) the standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies, the Android T-333 pricing-profile mirror debt).

## 66th session (2026-09-14, CLOSED) — the portal upload TABLE leg (T-367, UPLOAD-104) — the owner's live console evidence

**CLOSED (T-367 COMPLETE, UPLOAD-104 CLOSED TESTED + live RED→GREEN):** the trigger was the owner's pasted production console evidence (`rest/v1/student_documents → 403` ×2). Root cause live-proven: the row INSERT omitted `tenant_id` (no column default; the 0043 WITH CHECK fails on NULL → SQLSTATE 42501 → HTTP 403 — a 403 on a table URL means a WRITE was RLS-rejected; SELECTs under RLS silently filter). The T-360 storage-path fix was correct but only half the flow — every attempt succeeded in Storage then ORPHANED the object when the row insert 403'd. Fix: `tenant_id: tenantId` in the payload (website 51ca7e5, the chat_messages convention) + the t-367 whole-src guard (6 tests) + the t-359 e2e TABLE leg (checks L/M/M2/N) — live E2E 19/19 GREEN with the owner's exact console signature reproduced as the RED proof (L) and the fixed payload accepted (M, HTTP 201); zero residue; the production table is still empty (0 successful uploads ever — the first real one lands after the owner's Vercel redeploy). AGENTS.md §15.28 now carries the TABLE-write corollary. The Firebase env console line is the documented truthful owner-gated state (no WEB app id exists in the Firebase project; runbook: docs/operations/portal-google-oauth.md production-push section) — NOT a defect; the owner must create the Firebase WEB app + VAPID key from their console (cannot be done programmatically without their Google-account OAuth).

**Next session (67th+) should pick, in order:** (1) **T-337 (REALTIME-105)** — still the standing P0 publication migration (the 0085 pattern; the portal's five postgres_changes subscriptions target non-publication tables); (2) the attendance-KPI display-honesty residual (the 63rd session's registered follow-up — `kpisForRange.attendanceRateToday` defaults to 1.0 with zero records); (3) the owner-gated residuals when supplied (the Firebase WEB app + VAPID key per the runbook above, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (4) the standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies, the Android T-333 pricing-profile mirror debt).

## 65th session (2026-09-14, CLOSED) — the empty-state Excel restore mandate (T-364..T-366, IMPORT-106/107/108/109) — the owner's "be 100% certain" scenario

**ALL THREE TASKS COMPLETE (T-364 + T-365 TESTED, T-366 closeout):** the owner's exact question is answered WITH EVIDENCE — from a COMPLETELY EMPTY desktop database, the real `Suivis clients  2026_2027.xlsx` restores ALL required data (390 students / 253 parents / 891 payments Σ 55,227,100 DZD = the workbook's own TOTAL VERSEMENTS / 1968 installments Σ due 113,518,800 DZD / 1283 ledger entries / 0 rejected / 201 print-layout rows correctly skipped), every per-row financial matches the workbook's own cells, every FK resolves, a mid-import failure rolls back to EMPTY, and the second import is a complete no-op in both repository modes. The suite was built BEFORE the fixes and caught three real re-import/identity defects (IMPORT-107 ledger flush idempotency + atomicity, IMPORT-108 the mock bulkCollect double-booking, IMPORT-109 the BELGRIMAT sibling substring cross-wiring) — all fixed at the repository/adapter layer, no backend change. Gates on the merged tree: tsc 0 / eslint 0 errors / 155 files / 3301 tests / 0. Suites: t-364 8/8 + empty-state-restore 13/13.

**Next session (66th+) should pick, in order:** (1) **T-337 (REALTIME-105)** — still the standing P0 publication migration (the 0085 pattern); (2) the attendance-KPI display-honesty residual (the 63rd session's registered follow-up — `kpisForRange.attendanceRateToday` defaults to 1.0 with zero records); (3) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (4) the standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies, the Android T-333 pricing-profile mirror debt).

## 64th session (2026-09-14, CLOSED) — the cross-platform upload mandate (T-359..T-363, UPLOAD-101/102/103)

**ALL FIVE TASKS COMPLETE (T-359..T-362 TESTED + live-verified, T-363 closeout):** the mandate's premise was inverted with live evidence — the backend was fully configured (10 buckets, 24 policies, chain 91/91, role resolution healthy); the three real defects were client-side storage-path bugs (UPLOAD-101 website tenant-less path / UPLOAD-102 desktop "mock" tenant / UPLOAD-103 Android tenant-less path + swallowed RLS rejection + 4 s timeout), each live-proven RED and each fix live-proven GREEN pre-implementation (scripts/t-359-upload-e2e.py 13/13; post-fix re-verification 13/13 ×2 idempotent, zero residue). Fixes landed per platform with regression suites (website 68ad0b6 / desktop 2fef316 / android 5adc721); AGENTS.md §15.28 pins the permanent storage-path rule. Suites: website 47/612/0 + lint + build + tsc 0; desktop 152/3280/0 + tsc + eslint 0 err; Android 60/566/0 + lint + assembleDebug. No backend change. Commits pushed with merge-forward coordination (4 merges with the concurrent 63rd session, zero conflicts).

The owner's mandate: fix uploads (files, folders, links, images) across mobile + desktop; review/fix the role/permission system and the upload flow end to end; no temporary workarounds; test with the live tokens; document; commit/push/merge after each task; zip + push at the end. T-359 CLOSED the discovery phase with live evidence: the backend is FULLY configured (10 buckets, 24 policies, chain 91/91 zero drift, role resolution healthy — `scripts/t-359-upload-e2e.py` 13/13 GREEN, evidence `t-359-live-verification.md`); the three defects are CLIENT-side storage-path bugs: UPLOAD-101 (website tenant-less path), UPLOAD-102 (desktop hardcoded "mock" tenant), UPLOAD-103 (Android tenant-less path + swallowed RLS rejection + 4s timeout). Fixes: T-360 (website) → T-361 (desktop) → T-362 (Android) → T-363 (closeout + zips + push). COORDINATION with the concurrent 63rd session (T-351..T-358 dashboard wiring): disjoint file sets (upload-flow files only); fetch → merge forward → re-run gates before every push.

## 63rd session (2026-09-14, CLOSED) — the dashboard-statistics investigation mandate (T-350..T-358, DASH-401..407 + DATA-017/018) — the wiring layer root-caused and unified

**ALL EIGHT TASKS COMPLETE (T-350 investigation + T-351..T-357 fixes, all TESTED with suite evidence; the live legs verified: the engine re-check 302 TS≡SQL + the leg-2 probes 6/6 GREEN).** DASH-401..407 CLOSED TESTED; DATA-018 CLOSED TESTED; DATA-017 documented (the import-timestamp boundary). Evidence: `docs/recovery/t-350-live-verification.md` (the full Excel→UI trace, the refuted-claims registry, the semantic table, the live numbers). Suites at close: desktop **152 files / 3275 / 0 + tsc 0 + eslint 0 errors**. The concurrent 62nd session (MATIERE-500) merged forward at every push (one UNION conflict resolved in analytics-tab.tsx).

**Next session (64th+) should pick, in order:** (1) ~~the standing priorities from the 62nd session~~ — RESOLVED at merge time: the concurrent 62nd session COMPLETED T-347 (website 3a01902), T-348 (android 3b3a38d+b551c9f) and T-349 (the hub closeout — MATIERE-500 CLOSED); nothing left to pick up from that session; (2) **the attendance-KPI display-honesty residual** (registered here by the 63rd session): `kpisForRange.attendanceRateToday` defaults to 1.0 when NO records exist today → the Overview KPI shows "Assiduité (aujourd'hui): 100%" with zero data — the same WEAK-019-family inversion DATA-018 fixed for ages (a small task: an honest "—" state, needs a contract-shape decision: nullable field vs a records-count field); (3) **T-337 (REALTIME-105)** — still the standing P0 publication migration; (4) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (5) the standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies).

## 62nd session (2026-09-14, CLOSED) — MATIERE-500: the matière-architecture mandate (T-343..T-349)

**ALL SEVEN TASKS COMPLETE (T-344..T-348 TESTED, T-349 closeout; MATIERE-500 CLOSED — TESTED + the live DB leg VERIFIED LIVE):** the owner's commit 158d806 ("idk mattier is wrong") + the restructuring mandate produced the ADR-018 canonical subject architecture on all three platforms + the live database — subjects = identity, NEW `subject_configurations` = context (subject × year × level × direction: coefficient, subject_code, passing_grade, is_extracurricular, grading_recipe incl. the contrôle-continu cc weight), assessment rows snapshot the resolution (never retroactive), ONE canonical resolver per platform replacing the 14 fallback chains, NEW `exam_sessions`, the seeded default recipe {1,1,2,0} bit-identical to the historical engine. Migration 0094 applied LIVE atomically (19/19 + 19/19, 4 production rows byte-identical). Suites: desktop 147/3221/0 + tsc + eslint; website 46/607/0 + tsc + lint + strict build + LIVE RLS probes; Android 62/579/0 on the merged tree + the 7/7 t345 corpus equivalence through the real Kotlin mirror. Commits: hub 7bde1cb→closeout; website 3a01902; android 3b3a38d+b551c9f. Zips delivered.

**Next session should pick, in order:** (1) the 63rd session's remaining DASH fixes if the concurrent agent has not finished them (T-352..T-357 — school-wide observation, year scoping, the tranche twin, the departments stream, the revenue-bucket parity, honest demographics); (2) **T-337 (REALTIME-105)** — still the standing P0: the publication migration (now next free number 0095 — 0094 was taken by this session's subject_configurations), the 0085 pattern, the RED control script exists; (3) the owner-side data enablement for the new architecture: populate `subject_configurations` through the SubjectConfigurationsPanel with the real CEM/lycée coefficients (the 51-row seed covers the seeded contexts) and record the cc marks through the grade entry — the radar's academic leg and the per-context coefficients come alive only with data; (4) the standing recommendations (the transport.ts doc-comment cosmetic, the 2 pre-existing corpus discrepancies [023/025 studentId null-vs-""], the T-047 Group-B remainder); (5) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR).

## 61st session (2026-09-14, CLOSED) — STATS-400: the executive-statistics mandate (T-338..T-342) — the unified engine proven desktop ≡ android ≡ SQL

**ALL FIVE TASKS COMPLETE (T-338..T-341 VERIFIED, T-342 closeout):** the canonical executive-statistics engine (T-338), the Executive Command Center UI + the vanity purge (T-339), the verbatim Android mirror + Room 14→15 (T-340), and the two-level equivalence proof (T-341 — the corpus + the 299-check LIVE TS≡SQL diff, evidence docs/recovery/t-338-live-verification.md). STATS-400 CLOSED VERIFIED LIVE (251 registry entries, 21 VERIFIED). The owner's mid-session "push before you continue" request honored (the WIP backup 47a7cd7 + every completion pushed same-session).

**Next session (62nd) should pick, in order:** (1) **T-337 (REALTIME-105)** — still the standing P0: the publication migration 0094 (the 0085 pattern; the RED control script scripts/realtime-probe.mjs already exists) + optional useGradesRealtime; (2) the cosmetic desktop follow-up: transport.ts doc comment "BOUMRDS" → "BOUMRDES" (the code is authoritative — one line + the corpus already pins it); (3) the Android T-333 pricing-profile mirror OR the executive radar's data-entry enablement (the school must record assessments/attendance for the triple-risk radar to evaluate profiles — currently 3 assessment rows on live); (4) the standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies); (5) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR).

## 60th session (2026-09-13, CLOSED) — GRADE-102: the website grades-display fix (T-336) + the REALTIME-105 discovery

**T-336 COMPLETE + VERIFIED LIVE (the owner's exact symptom root-caused and fixed):** the retrieval layer was proven correct first (11/11 live data-path checks — the portal's exact PostgREST query under the bound parent's RLS returns the canonical assessments with the joined subject); the defect was per-child ID ROUTING — `activeStudentId` was only auto-selected inside switcher components that render ONLY for 2+ children, so single-child parents (the most common family shape) kept a null id FOREVER and every per-child view (grades/attendance/homework/finance) rendered its empty state with recorded data (the dashboard's local `kids[0]` fallback masked it). Fix: ONE canonical auto-select effect in the AppShell (+ stale-id reset) + the term-scoped KPI parity (per-term canonical GPA, the desktop/SQL/Android semantics) + the "Moyenne à paraître" hint for partial marks (i18n ×3). Website 45 files / 599 / 0 (+14) + tsc 0 + eslint clean + strict build green; live RED/GREEN browser round-trip; zero-residue. GRADE-102 CLOSED VERIFIED LIVE (250 registry entries). NEW discovery registered from the same investigation: **REALTIME-105** — the portal's five realtime postgres_changes subscriptions target tables NOT in the `supabase_realtime` publication (live probe: `assessments` 0 events vs the `audit_logs` member control 1 event); the T-033 fallback is the only effective freshness layer; fix registered as **T-337** (migration 0094, the 0085 pattern).

**Next session (61st) should pick, in order:** (1) **T-337 (REALTIME-105)** — the publication migration + optional `useGradesRealtime` hook; the RED control script already exists (scripts/realtime-probe.mjs); (2) the Android T-333 mirror (the per-service pricing profile in Kotlin — the standing parity debt now that desktop+website carry it); (3) the current-state's standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies); (4) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR). Owner-side data note: the 3 admin-entered assessment rows belong to families without portal accounts — bind those parents via the canonical approval flow (T-331/T-332) if the owner wants to SEE them on the portal.

## 59th session (2026-09-13, CLOSED) — DATA-016: the exhaustive per-service pricing profile (T-333..T-335)

**All three tasks COMPLETE + the LIVE leg VERIFIED (21/21 GREEN, zero-residue):** T-333 (the website canonical derivation + usePricingCatalog + the i18n'd 7-section card + 20 tests; ALSO the 66 pre-existing tsc fixture errors repaired — tsc is now a usable website gate), T-334 (the desktop verbatim half — output-shape-identical profiles, installmentMonths mapped verbatim from the LIVE grid, the drawer card, 18 tests), T-334a/T-333a (the ledger-honest construction: adjustmentsDebit nets the 0063 double-remise-cancel — caught by the live leg BEFORE closeout), T-335 (the live probe: the 6 catalog tables through the exact PostgREST+RLS path + the ALIOUAT vector's every field: year 2026-2027, 4am/4AM/cem, class 4ème Année Moyenne, catalog 340 000 tranches 136k/102k/102k months 9/12/4, conditions, FI/V2/2V, provenance run_msp3foah_c254f9, net 350 000 delta +10 000). DATA-016 CLOSED (248 registry entries). No migrations — the pricing tables were already parent-readable (0019).

**Next session (60th) should pick, in order:** (1) the Android T-333 mirror (the per-service pricing profile in Kotlin — `core/ServicePricingProfile.kt` consuming the same canonical shape; the 45th-session StatisticsEngine precedent; the corpus category `service_pricing_profile` with the ALIOUAT scenario in BOTH runners) — the standing parity debt now that desktop+website carry it; (2) the current-state's standing recommendations (the T-047 Group-B remainder, the ai_request_logs usage surface, the 4 pre-existing corpus discrepancies); (3) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (4) the live deploy leg when the owner re-supplies the PAT for pushes (this session's commits are local-pushed if the PAT works — see the closeout).

## 52nd session (2026-09-12, CLOSED) — the T-313 closeout audit: REG-007 (the 8 shipped tsc errors in the NEW T-313 test fixtures) repaired, fixtures domain-typed: T-313a

**T-313a CLOSED (TESTED; test files + docs only, zero runtime change):** the owner's completion-audit question triggered a re-verification of the pushed 328dad9 instead of a trust-based "yes" — the commit IS pushed and the DAG directive HELD (empty diff on DAG paths; no parallel-agent commits at audit time), the FULL suite re-ran **135/3095/0**, and every T-313 fix was confirmed in the tree — **but the same tree had 8 tsc errors in the two NEW T-313 test files** (the 51st closeout's "tsc 0 errors" was false: vitest/esbuild transpiles WITHOUT typechecking, and tsc was never re-run after the tests were written). Fixed: the four duplicated fixtures extracted to domain-input-typed shared constants (`YEAR_INPUT: CreateSchoolYearInput`, `CLASS_INPUT`/`SUBJECT_INPUT`/`ASSIGN_INPUT: Omit<…>`) so contract drift now fails at COMPILE time; the invalid cast routed through `unknown`; **AGENTS.md §15.25** added (a green `vitest run` is NOT a typecheck — the ladder re-runs after ANY file change); REG-007 registered + CLOSED (registry totals re-synced 230→235); re-verification in the CORRECT order: tsc 0 / T-313 25/25 / FULL 135/3095/0.

**Next session (53rd) should pick, in order:** (1) **COORDINATE FIRST: `git fetch` + `git log origin/main` + `git status`** — the parallel DAG agent may have landed their T-313 DAG leg (dag-canvas external-sync restore, movement-checked saves) as a sibling commit; rebase review BEFORE touching any workflow file, and re-run the workflow suites on the merged tree; (2) the TeacherDashboard subject-class visibility follow-up (REGISTERED in REG-006's resolution note: scope myClasses via class_subjects.teacher_id in a registered task with the pinned test updated — the owner's review wanted subject-teaching classes visible; the name-matching shortcut is gone and must NOT return); (3) the T-047 teachers modeling ADR (owner-gated: personnel-role view vs a teachers table — the teachers slot is the LAST high-value mock-backed slot in the Pedagogy section; with ACAD-104 fixed, class_subjects writes now persist and the ADR would complete the year-scoped teacher provisioning); (4) the T-047 Group-B remainder (releve, shifts, schedules, performanceReviews, onboarding, aiConfig, backups) — adapter work onto existing canonical tables; (5) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR).

## 51st session (2026-09-12, CLOSED — non-DAG scope) — the 9e70078 shallow-fix redo: T-313 (the owner's mandate + the DAG parallel-agent scope split)

**T-313 CLOSED for the non-DAG scope (TESTED + VERIFIED LIVE; the DAG leg deliberately untouched — the owner delegated it to a parallel agent):** (1) **ACAD-104 FIXED + LIVE-VERIFIED** — `createAcademicYear`/`createClass`/`createSubject`/`assignSubjectToClass` now stamp `tenant_id` (the T-023 in-file precedent; four fail-loud ERR_VALIDATION guards; zero schema change); the live round-trip (scripts/verify_t-313_live.sh, the desktop's exact PostgREST+RLS path) proved RED (old payload → 403/42501, the owner's exact denial) vs GREEN (tenant-stamped → 201 + read-back + cleanup). `class_subjects` had ZERO production rows — the flow never once persisted; it now does. (2) **REG-006 CLOSED** — the fake-success wrapper + prototype stripping REMOVED (provider passes repositories by reference, pinned by NEW tests); RBAC-301 strict homeroom scoping restored; `isSuperAdmin` pinned to the canonical enum (the "admin" conflation deleted); the render-triggered auto-provision effect removed (explicit modal stays); scratch artifacts untracked + .gitignore hardened. (3) **NEW permanent lesson AGENTS.md §15.24** — the "corrupted source lines" were a FALSE diagnosis caused by the sandbox Bash display layer eating `[h`/`[m` bracket-letter sequences; byte-level verification proved the lines intact; never diagnose source corruption from terminal display output. (4) t-230 source pins whitespace-normalized (formatter-robust). Suites: desktop **135 files / 3095 tests / 0 failures + tsc + eslint 0 errors** (+25 NEW T-313 tests).

**Next session (52nd) should pick, in order:** (1) **COORDINATE FIRST: `git fetch` + `git log origin/main` + `git status`** — the parallel DAG agent may have landed their T-313 DAG leg (dag-canvas external-sync restore, movement-checked saves) as a sibling commit; rebase review BEFORE touching any workflow file, and re-run the workflow suites on the merged tree; (2) the TeacherDashboard subject-class visibility follow-up (REGISTERED in REG-006's resolution note: scope myClasses via class_subjects.teacher_id in a registered task with the pinned test updated — the owner's review wanted subject-teaching classes visible; the name-matching shortcut is gone and must NOT return); (3) the T-047 teachers modeling ADR (owner-gated: personnel-role view vs a teachers table — the teachers slot is the LAST high-value mock-backed slot in the Pedagogy section; with ACAD-104 fixed, class_subjects writes now persist and the ADR would complete the year-scoped teacher provisioning); (4) the T-047 Group-B remainder (releve, shifts, schedules, performanceReviews, onboarding, aiConfig, backups) — adapter work onto existing canonical tables; (5) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR).

## 47th session (2026-09-11, CLOSED) — owner mandate COMPLETE ("do it yourself — here are all the tokens you need to test if it works, make sure it works"): T-303..T-305 (3 tasks)

**All three owner-gated live steps landed and were VERIFIED LIVE with the re-supplied token sheet (sbp_ access token + the full Supabase key set):** (1) **T-303** — migration 0085 applied LIVE (the atomic apply_0085_live.sh; post-check: `public.audit_logs` in `supabase_realtime` CONFIRMED — the live registration row pre-existed with the cosmetic name-label quirk `audit_mutation_notifications`, the ARCH-009 class; chain 82/82 ZERO DRIFT) + the END-TO-END realtime RLS probe 6/6 GREEN (the production-shaped postgres_changes INSERT subscription received the attributed audit event: actor_name + actor_role=super_admin + actor_id + action + entity_type + note marker — the T-299 broadcast genuinely works live now) + the T-244-style token round re-verified (dual-key auth health 200/200, anon RLS 0 rows ×5, EF census 14/14 ACTIVE, publication membership). (2) **T-304** — the ai-proxy cap-40 deploy (THE standing P0 owner-gated step since the 43rd session) CLOSED LIVE: the t277 matrix re-run ALL GREEN with P2 FLIPPED 400 invalid_tools → 200 + Groq SSE chunks (openai/gpt-oss-20b) — edge-mode copilot agent-stream calls with the full 27-tool registry now work in production; P3 200 + 46 SSE lines; P4/P5 hold. (3) **T-305** — the conflict guard ARMED in production traffic (OFFLINE-400 residual (b) closed): `MockStore.stageAndNotify` passes the pre-edit checkpoint row as `basePayload` on every update enqueue, covering the ENTIRE offline edit surface (CRM parents/students + financials payments/installments/ledger); insert/delete never carry a base. Final evidence: desktop **130 files / 3025 tests / 0 failures** + tsc + eslint 0 errors; Android untouched-green **57 suites / 507 / 0 / 0 errors / 1 documented skip** (baseline re-verified after re-provisioning JDK21+SDK35). OFFLINE-400 residuals (a)+(b) CLOSED; the standing P0 deploy CLOSED. Open residuals (honest): the Android 3-way mirror (gated on Android gaining a base-snapshot push path) + expenses/personnel/workflows restored-but-not-staged.

**Next session (48th) should pick, in order:** (1) the T-047 Group-B ports (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding — adapter work onto the existing canonical tables); (2) the ai_request_logs usage surface (queries-only admin analytics card: requests/day, tokens, error rate per feature — the EF now logs REAL traffic through the cap-40 deployment); (3) the 4 pre-existing corpus discrepancies (017/023/024/025 — NaN display, null-vs-empty studentId, undefined legacy fields — low value, untouched); (4) the Android 3-way mirror IF the owner green-lights the base-snapshot push path (Room schema + SyncService signature + the Kotlin three-way mirror + the resolver UI — a feature-scale change, currently gated by design); (5) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main); (6) the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR).

## 46th session (2026-09-11, CLOSED) — owner mandate COMPLETE ("Fix and stabilize the offline-first architecture"): T-294..T-302 (9 tasks)

**OFFLINE-400 CLOSED (TESTED):** all six sub-gaps landed with suite evidence — (1) the T-295 pure field-diff engine + the T-296 red/green AuditDiffDrawer with actor attribution (Name + Account ID + Role; mapAuditRow + the RPC path now carry actor_role — every row used to land NULL); (2) the T-297 Android verbatim mirror (core/FieldDiff.kt + the shared AuditDiffSheet replacing the FABRICATED "Inspecteur JSON" in both screens); (3) the T-298 3-way engine + the drain's no-silent-overwrite guard (conflicts park on the queue entry, push NEVER called) + the 3-column resolver (Base/Votre version/Serveur, A/B/manual choices, merged preview) + BOTH notifications and audit entries; (4) the T-299 realtime attributed broadcast (migration 0085 = publication membership ONLY, RLS untouched — the recorded security deviation; the desktop toaster + observeActivity; the Android audit_logs route + pullAudits making the attributed feed live); (5) the T-300 REAL offline restore (inspectArchive read-only browser → apply → restored-from marker → post-restore staging into the EXISTING queue → the no-data-loss reconnect drain; tamper → corrupted verdict, never a partial apply); (6) the T-301 toggleable scheduler (persisted preference, default ON, every scheduled tick gated; manual runs never gated). Final evidence: desktop 129 files / 3020 tests / 0 failures + tsc + eslint clean; Android 57 suites / 507 / 0 failures / 0 errors. Commits: hub 28c5048, 86dcf77, cba89e0, 595fc97, 9c75293, 8fab553, ee1170e + the closeout; android a8a046a + 0491a9b. **RESIDUALS (owner-gated, honest):** (a) migration 0085's LIVE application (supabase db push with the sbp_ token — not re-supplied this session); (b) conflict-guard producers — update-path screens should pass basePayload on enqueues to arm the guard in production traffic; (c) the Android 3-way mirror; (d) expenses/personnel/workflows restored-but-not-staged (no dispatcher push paths yet).

**Next session (47th) should pick, in order:** (1) the owner-gated live applies + verifies when tokens are re-supplied (migration 0085 db push + the realtime RLS live probe + re-running scripts/verify_t-244_mig_tokens.sh for the chain drift check); (2) arming the conflict guard in production traffic (pass basePayload from the edit screens that enqueue updates — the CRM/financials edit flows); (3) the ai-proxy cap-40 deploy (UNCHANGED, owner-gated since the 43rd); (4) the T-047 Group-B ports (pricing FIRST); (5) the 4 pre-existing corpus discrepancies (017/023/024/025); (6) the T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

## 45th session (2026-09-11, CLOSED) — owner mandate COMPLETE ("100% Visual, Mathematical & Analytical Parity (Desktop to Android)" — the 13-chart parity inventory): T-288..T-293 (6 tasks)

**PARITY-003 closed (TESTED):** every visualization in the owner's 13-item inventory now has a native Jetpack Compose twin computed from the same canonical engine — the 5 missing derivation families ported verbatim into `core/StatisticsEngine.kt` (weekly rhythm [counter-activity], collection heatmap [weekday×month, 5-step levels], YoY + the slicer engine [null-delta honesty], tranche waves [T-248 regex/pct/next-target], demographics [grade/gender/age/capacity with the default-30]); `DashboardKpi` +10 engine-computed fields (the fixed-3-method parallel derivation RETIRED → deriveMethodMix); 8 new Canvas primitives + ElChartPalette (the desktop dashboard-theme hex mirror) + the 13 cards (the Analytique tab rebuilt as the desktop Analytics-tab twin; weekly rhythm in the Overview; tranche-wave meters in the Financials Tranches tab; capacity gauges + gender chips in ClassDetailScreen). **The proof:** the `analytics_visuals` corpus category (3 scenarios with then-blocks generated by the REAL TS derivations), the op in BOTH runners, and **CrossPlatformEquivalenceTest.kt — 3 scenarios × 5 families, every value identical**; StatisticsEngineTest 35/35 (+12 new); Android 54/467/0 + lint 0 errors; desktop tsc + eslint 0 err + vitest 122/2897 + build green; full corpus 308/310 EQUIVALENT (the 2 = the documented pre-existing 023/025). Residuals registered: the bar/area view toggle (desktop-only interaction), the mobile full-stream slice vs the desktop academic-year selector (shared-convention note), the YoY previous-year source (the same local stream — honest n/a when absent). Push landed 2026-09-11 in the 45th-session continuation (the owner re-supplied the PAT; android `181d060` + hub `df09226` API-verified on origin/main; pre-push re-verification: Android testDebugUnitTest 467/0, desktop tsc + vitest 122/2897 + build green; the 9 release-variant failures proven pre-existing at parent 5c03a9e — not a parity regression). Zips + commits + push all landed.

**Scope (registered BEFORE implementation, PARITY-003):** every desktop visualization in the owner's §3 inventory (revenue trend explorer + cumulative + 3-month MA, weekly operating rhythm Dim→Jeu stacked by method, recovery funnel, amount histogram, method mix donut, category ranked bars, collection heatmap, top-debtors Pareto, debt aging 100% composition, YoY comparison, attendance, tranche wave progress T1/T2/T3, class demographics & capacity gauges) must exist natively in Jetpack Compose AND compute through the same canonical engine (`core/StatisticsEngine.kt` — 5 derivation families to add verbatim), proven by a NEW `CrossPlatformEquivalenceTest.kt` + an `analytics_visuals` corpus category generated from the REAL desktop TS derivations. Status at open: tasks registered, environment re-provisioning (JDK 21 + SDK 35 + desktop npm install — container reset). The 44th session's engine base (T-284/T-285) is the foundation — this session adds the VISUAL parity layer on top.

**Next session (46th) should pick, in order:** (1) **P0 — the one owner-gated deploy** (UNCHANGED from the 43rd/44th): the ai-proxy cap-40 deploy (`SUPABASE_ACCESS_TOKEN=sbp_… supabase functions deploy ai-proxy --project-ref hkvkefubghbbotgnteir --no-verify-jwt`, then re-run `scripts/t277-live-matrix.sh`); (2) the ai_request_logs usage surface; (3) the T-047 Group-B ports (pricing FIRST); (4) the small Android hygiene follow-up from the 44th (restore `adb shell pm clear` to run_deploy.sh); (5) the 4 pre-existing corpus discrepancies (017/023/024/025); (6) the owner-gated residuals when supplied; (7) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

## 44th session (2026-09-11, CLOSED) — owner mandate COMPLETE ("review the changes and redo them properly — the previous agent implemented them too shallowly; integrate the logic at the CORE level, create a comprehensive equivalence test against the database; true data + logic equivalence between desktop and mobile"): T-283..T-287 (5 tasks)

**PARITY-002 closed (TESTED + the live leg VERIFIED):** the previous agent's Android dashboard patch was audited (hard-coded desktop numbers as UI fallbacks, inline statistics, population σ, integer-division mean/median, a 2-of-11 category mix, a fabricated funnel, ZERO tests — and the pushed tree did not even COMPILE: 3 unresolved references), then REDONE at the core level: `core/StatisticsEngine.kt` (the verbatim ADR-002 mirror of the desktop analytics-derivations, source commit in the header) feeds the dashboard, debt, and PDF surfaces through ONE canonical per-installment INV-4 derivation; every UI fallback number was deleted (honest empty states). The proof — three legs: 24 desktop-fixture-pinned engine tests; the `deriveAnalyticsStats` corpus op in BOTH runners (category `analytics_statistics` 3/3 EQUIVALENT centime-exact; full corpus 303/307, the 4 discrepancies proven pre-existing by a stash re-run); and the **LiveDatabaseEquivalenceTest PASSED LIVE** — the Android engine output == the server-side SQL truth on EVERY value (891 / 55 227 100 DZD / 61 983 mean / 61 000 median / 44 951 σ / Août / bins 3-2-63-341-482 / 96%-4% mix / 91–180 j 26.9M+196 fam + 180+ j 31.4M+183 fam / funnel 379 / outstanding 58 355 700 / **49%** / 24 classes; canonical script scripts/verify_t-285.sql; evidence t-285-live-verification.md). Two parity traps discovered + pinned forever: money-roundings round at DZD granularity (dzRound), and the funnel consumes the census Σ of per-bucket family counts (NOT distinct parents, NOT per-family worst-bucket). Android suites at close: 53 files / 455 / 0 + compile + lint; desktop typecheck + eslint + vitest green (1 unrelated timeout flake re-verified green in isolation). Zips + push with the owner PAT.

**Next session (45th) should pick, in order:** (1) **P0 — the one owner-gated deploy** (UNCHANGED from the 43rd): the ai-proxy cap-40 deploy (`SUPABASE_ACCESS_TOKEN=sbp_… supabase functions deploy ai-proxy --project-ref hkvkefubghbbotgnteir --no-verify-jwt`, then re-run `scripts/t277-live-matrix.sh` — P2 flips 400→200; until then edge-mode agent-stream calls with the 27-schema registry 400; BYOK/mock/single-shot unaffected); (2) **the ai_request_logs usage surface** (queries-only admin analytics card: requests/day, tokens, error rate); (3) **the T-047 Group-B ports** — the STANDING recommendation (pricing FIRST per the T-160 priority; releve/shifts/schedules/performanceReviews/onboarding — adapter work onto the existing canonical tables); (4) the small Android hygiene follow-up from the 44th: restore `adb shell pm clear` to run_deploy.sh (stale DatabaseSeeder seed data persists across deploys — the previous agent's own troubleshooting documents the "0 or 150 000 DA" symptom); (5) the 4 pre-existing corpus discrepancies (017/023/024/025 — NaN display, null-vs-empty studentId, undefined legacy fields — low value, untouched); (6) the ADR-015 residuals + the owner-gated residuals when supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (7) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 43rd session (2026-09-10, CLOSED) — owner mandate COMPLETE ("do not change the usernames and passwords admin@elimtiyaz.dz / elimtiyaz@admin2026 — bring them back" + the pasted production console "fix this problem … and make the models list fetched and selected then push again"): T-280..T-282 (3 tasks)

**OPS-310 closed (VERIFIED LIVE):** the owner's canonical admin credential restored (GoTrue self-service `PUT /auth/v1/user`; both directions probe-verified — the owner value grants a JWT, the old probe value `Elimtiyaz2026Admin!` is dead). The PERMANENT pin is in place: credentials.md §1 carries the owner directive (NEVER rotate/re-set; `invalid_credentials` on the pinned value → STOP and ask), the three live-matrix scripts carry the owner value as `ADMIN_PW`, and AGENTS.md §15 rule 23 forbids the re-set anti-pattern.

**AI-312 + OPS-311 closed (TESTED, 22 new tests):** the console triage — the 5× Groq 429 = zero retry on the BYOK path → `executeOpenAIStream` now retries transients with backoff + `Retry-After` (abort-aware), the runtime's persistent-429 settles as a human French verdict, and the four model fields are now SELECT dropdowns over the live-fetched list (`ModelSelectField`, auto-discovery re-keyed on the SAVED config so it fires on tab open); the intermittent audit 500 = transient server blip (live-probed clean) → ONE 600ms-delayed retry on the three audit read paths (`log()` never retried). The early 400 in the owner's console = the DOCUMENTED P0 EF cap (see below). Suites at close: desktop 122 files / 2897 / 0 + tsc + eslint 0 err + build green.

**⚠️ STANDING RULE from the owner (43rd session): the admin credential is `admin@elimtiyaz.dz` / `elimtiyaz@admin2026` — DO NOT change usernames or passwords. Probe scripts already carry this value; a failed sign-in on it means ASK THE OWNER, never a re-set.**

**Next session (44th) should pick, in order:** (1) **P0 — the one owner-gated deploy**: the ai-proxy cap-40 deploy (`SUPABASE_ACCESS_TOKEN=sbp_… supabase functions deploy ai-proxy --project-ref hkvkefubghbbotgnteir --no-verify-jwt`, then re-run `scripts/t277-live-matrix.sh` — P2 flips 400→200; until then edge-mode agent-stream calls with the 27-schema registry 400 [this is the early-400 line in the owner's console] — BYOK/mock/single-shot unaffected). If the owner re-supplies the token IN a session, the session agent runs it; (2) **the ai_request_logs usage surface** (the EF logs REAL traffic now — a small admin analytics card: requests/day, tokens, error rate per feature; queries only); (3) **the T-047 Group-B ports** — the STANDING recommendation (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding — adapter work onto the existing canonical tables); (4) the ADR-015 residuals: record_attendance / dispatch_task execution legs when their canonical paths are designed (the honest-settle branch holds them at "non exécutable" meanwhile); (5) the AI-309 residual (the DB permission-catalog migration for non-SuperAdmin AI roles — only if the school wants AI-authorized non-admin roles); (6) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (7) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 42nd session (2026-09-10, CLOSED) — owner mandate COMPLETE ("I want a much more capable AI system with a proper tool ecosystem: powerful tools for analyzing data, generating statistics and charts, drawing diagrams, working with documents… complex workflows… a real AI-powered system, not a shallow chatbot"): T-272..T-279 (8 tasks)

**AI-311 closed (TESTED + live-matrix evidence):** the copilot's tool registry grew 12 → 27 across five suites (core / analysis / visualization / document / workflow) composed by tool-registry.ts into the single no-slicing wire array. Tool results can now carry validated ARTIFACTS the user sees: SVG charts (7 types, zero-dependency engine), structural diagrams (canonical family/class), and downloadable documents (PDF through the canonical generators — statement / class report / debt sheet / payment plan; XLSX/CSV exports with embedded capped rows). The analysis suite computes over canonical streams (revenue trends + OLS + moving averages; descriptive stats + Tukey outliers; payment/attendance/grade anomaly detection; class benchmarking; demographics). The workflow suite runs campaigns (prioritized tiers), batch reminders (ONE validated card → canonical sendReminder per parent, honest per-parent results), exact-sum payment plans (advisory PDF), and composite intervention risk scoring. The system prompt teaches the capability map; MAX_TOOL_STEPS 8. ADR-016 records the architecture + the enrichment-not-derivation boundary. A 51-test capability suite pins everything (incl. the full pipeline harness and the EF-cap ≥ registry source guard). Desktop 120 files / 2875 / 0 + tsc + eslint 0 err + build green. Zips + push with the owner PAT.

**Next session (43rd) should pick, in order:** (1) **P0 — the one owner-gated deploy**: the ai-proxy cap-40 deploy (`SUPABASE_ACCESS_TOKEN=sbp_… supabase functions deploy ai-proxy --project-ref hkvkefubghbbotgnteir --no-verify-jwt`, then re-run `scripts/t277-live-matrix.sh` — P2 flips 400→200; until then edge-mode agent-stream calls with the 27-schema registry 400 — BYOK/mock/single-shot unaffected). If the owner re-supplies the token IN a session, the session agent runs it; (2) **the ai_request_logs usage surface** (the EF logs REAL traffic now — a small admin analytics card: requests/day, tokens, error rate per feature; queries only); (3) **the T-047 Group-B ports** — the STANDING recommendation (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding — adapter work onto the existing canonical tables); (4) the ADR-015 residuals: record_attendance / dispatch_task execution legs when their canonical paths are designed (the honest-settle branch holds them at "non exécutable" meanwhile); (5) the AI-309 residual (the DB permission-catalog migration for non-SuperAdmin AI roles — only if the school wants AI-authorized non-admin roles); (6) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (7) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 41st session (2026-09-10, CLOSED) — owner mandate COMPLETE ("review the AI implementation critically, fix all mistakes/inconsistencies/incomplete functionality, deliver a production-quality integration" + "apply the migration tokens and make sure everything works"): T-266..T-271 (6 tasks)

**AI-310 closed (TESTED; the server leg VERIFIED-LIVE) + REG-005 triaged:** the session opened on an unregistered patch (6ce49b9 — "chore" message, 14-file AI rewrite, 5 red tests + 4 type errors: deleted edgeLLMAdapter, deleted readRawStored, fake guardrail, tool slicing, Bearer-null). T-266 attributed + triaged it (AI-309's sound auth fix registered; the damage repaired; baseline green). The copilot then went from 6 shallow tools to 12 production tools (overdue/debt ledger with aging, collection analytics, payment history, attendance with drop-off risk, class performance with at-risk lists — all canonical streams), gained REAL proposal validation (structured errors, no proposal from invalid input), a canonical send_reminder execution leg (repos.debt.sendReminder), the honest settle for unexecutable types, the clarification question card, the abort button with partial-answer commit, and conversation persistence. The ai-proxy EF was hardened + redeployed v20, GROQ_API_KEY set live, and the LIVE agent-stream round-trip verified for the first time (9/9 probes — content + tool-call reassembly on the wire; t-269-live-verification.md). The 2026 Groq catalog discovery: llama/qwen ids all 404 → defaults repinned to openai/gpt-oss-120b/-20b everywhere. ADR-015 records the copilot data policy. A 31-test deep-integration suite pins all of it. Migration tokens: chain 81/81 ZERO DRIFT (open + close), EF fleet 14/14, all supplied tokens verified live. Desktop 119 files / 2824 / 0 + tsc + eslint 0 err + build green. Zips + push with the owner PAT.

**Next session (42nd) should pick, in order:** (1) **the AI-309 residual** — a migration inserting use_ai/manage_ai_config into the DB permission catalog for non-SuperAdmin roles (only if the school actually wants AI-authorized non-admin roles — otherwise skip; the client-side SuperAdmin grant covers the current single-admin reality); (2) **the T-047 Group-B ports** — the STANDING recommendation (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding all have canonical tables already — adapter work only); (3) **the ai_request_logs usage surface** — the EF now logs REAL traffic (live tokens + latency from the Groq key); a small admin analytics card (requests/day, tokens, error rate per feature) would make the AI spend visible (queries only — no new table); (4) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (5) the AI-310 residuals recorded in ADR-015 (record_attendance/dispatch_task execution legs when canonical paths are designed; conversation is client-persisted only by policy); (6) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 39th session (2026-09-10, CLOSED) — owner mandate COMPLETE ("do all of this then push" — the Agentic AI Architecture): T-260..T-263 (4 tasks)

**AI-308 closed (TESTED; the EF leg live-verified):** the desktop now runs a model-agnostic agentic copilot — Groq/OpenRouter/custom-OpenAI with native SSE streaming + function calling, BYOK keys AES-256-GCM-encrypted with LIVE model discovery from the provider's `/models` endpoint, six domain tools grounded in the REAL repositories + canonical calc engines (ledger summaries, academic profiles, KPIs — adjustments only ever PROPOSED for 1-click human validation through the canonical repos.payments.adjust), and a universal drawer (Ctrl+J / topbar / palette Enter-to-ask). The ai-proxy EF gained the agent-stream mode and was deployed live v17 (7-probe matrix GREEN — OPTIONS 200 / GET 405 / POST-no-auth 401 / fleet 14/14 / chain 81=81 zero drift / RLS anon 0-rows / GoTrue health). Desktop 116 files / 2782 / 0 (+37 NEW agent tests) + tsc + eslint 0 err + build; website/Android untouched (zero contract impact). All blueprint adaptations registered (provider-inside-ToastProvider, additive topbar, RBAC gates preserved, propose-not-write, canonical name renderers). Zips + push with the owner PAT.

**Next session (40th) should pick, in order:** (1) **the AI-308 residuals that need no owner input:** the copilot conversation persistence (localStorage-backed history), the abort/stop button wired to the runtime's existing AbortSignal plumbing, and the non-financial proposal dispatch (record_attendance / dispatch_task / send_reminder) — each is a small, well-scoped task; (2) **the T-047 Group-B ports** — STILL the standing recommendation (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding all have canonical tables from 0009/0010 already — adapter work only); (3) the analytics/backend residuals that would extend the Analytics tab (`DashboardRepository.revenueByCategory()`, cycle-level aggregation); (4) an authenticated live agent-stream round-trip through the EF IF the owner configures a server-side GROQ_API_KEY secret (probing instructions in t-262-live-verification.md); (5) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (6) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 40th session (2026-09-10, CLOSED) — owner mandate COMPLETE ("fix it and push it" — the post-39th-launch 406 console-noise fix): T-264..T-265 (2 tasks)

**OPS-309 closed (TESTED):** the approval-queue matcher's five zero-row `.single()` lookups are now `.maybeSingle()` — the ~15 red PostgREST 406s per approval-queue refresh (owner-pasted production console at the 39th-session launch) are gone; enrichment semantics byte-identical; NEW 7-test regression suite (t-264-approval-406-noise.test.ts). Desktop **117 files / 2789 tests / 0** (+7) + tsc + eslint 0 errors (424 warnings, unchanged) + vite build green; website/Android untouched; no migrations (the chain 0001–0084 + the 39th session's 7-probe live evidence stand). **Groq BYOK key note:** the owner-supplied gsk_ key could NOT be verified from the agent sandbox (Hong Kong egress → Groq geo-block → 403 on every endpoint — NOT evidence of an invalid key; a bad key returns 401). Validation path for the OWNER: Settings → AI configuration tab → paste the key → the live model discovery list appears (one click, from Algeria). The EF server-side GROQ_API_KEY secret remains owner-gated (sbp_ token or dashboard). Hygiene: rotate the chat-transmitted Groq key + GitHub PAT when convenient. Observation carried forward: the ONE-OFF 500 on an audit-log list query (`…at.desc&limit=200`) at the 39th-launch startup did NOT reproduce — chase only if it recurs (RLS/audit_logs probe).

**Next session (41st) should pick, in order:** (1) **the AI-308 residuals that need no owner input** (unchanged recommendation — now the TOP pick): copilot conversation persistence (localStorage-backed history), the abort/stop button on the runtime's existing AbortSignal, the non-financial proposal dispatch (record_attendance / dispatch_task / send_reminder); (2) **the T-047 Group-B ports** (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding — adapter work only onto the 0009/0010 canonical tables); (3) the analytics/backend residuals (`DashboardRepository.revenueByCategory()`, cycle-level aggregation — the Analytics tab's natural extensions); (4) the live agent-stream EF round-trip IF the owner sets the server-side GROQ_API_KEY secret (their key is supplied — only the sbp_-token/dashboard step is missing; probing instructions in t-262-live-verification.md); (5) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (6) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 36th session (2026-09-09, CLOSED) — owner mandate COMPLETE (AI-review 3-zone dashboard integration + migration-token re-verification): T-243..T-245 (3 tasks)

**UI-306 closed (TESTED):** the dashboard Overview is now the review's 3-zone layout with every widget fed by the REAL repository contract — 4 sparkline KPI cards (trend/delta only where a real series exists), the hero revenue spline, the recovery funnel from debtAging family counts, the weekly rhythm from the canonical payments stream (Dim→Jeu), the calendar (kept), and the Zone-B InsightsRail (AI census card + REAL collection-rate gauge + À-relancer feed). The review's synthesized sample numbers were deliberately NOT rendered (§15.16) — each adaptation documented in the UI-306 problem entry. **T-244 (18/18 GREEN):** chain 0001–0084 zero drift, EF fleet 14/14 ACTIVE, dual-key health, RLS anon 0-rows, anonymous EF 401, ALLOWED_ORIGINS canonical set, committed-key consistency. Desktop 112/2694/0 + tsc + lint 0 err + build; website 40/544/0 + lint + strict build; Android untouched (zero contract impact). All repos pushed + zipped.

**37th session CLOSED (2026-09-09):** the AI-review completion mandate (T-246..T-254) — the Part-2 design system + the review's remaining five screens (drill-down, tranche-wave, quick-pay/change-calculator, tuition matrix, readiness checklist, parent-drawer 1-click collect) all TESTED with REAL-data derivations; live token round 18/18 GREEN; desktop 113/2711/0 + website untouched-green. **PUSH NOTE (resolved same-day): the owner re-supplied a valid PAT — 4d28a79 + 2632cfd are on origin/main (fetch-confirmed; all three repos in-sync; zips in download/).**

**38th session CLOSED (2026-09-09):** the owner's "more Power BI–type visualizations" mandate (T-255..T-259, UI-307 TESTED) — the dashboard gained the "Analytique" tab: 9 visual families (statistics strip, trend explorer with cumulative + 3-month MA, method donut, category ranking, weekday×month collection heatmap, amount histogram, debtors Pareto, aging 100% composition, like-for-like YoY) + method/category slicer chips that CROSS-FILTER every payments-derived card. All REAL-data derivations (the encaissé definition = the repository's own status=paid + calendar-month-of-collectedAt). Desktop 114/2745/0 + tsc + eslint 0 err + build; website/Android untouched; zero migrations (the live chain 0001–0084 stands). Container reset at session open — repos re-cloned from the confirmed origin HEADs, nothing lost.

**Next session (39th) should pick, in order:** (1) **the T-047 Group-B ports** — STILL the standing recommendation (pricing FIRST per the T-160 priority — the financial-drift surface; the new tuition matrix UI makes the pricing port's value even more visible; releve/shifts/schedules/performanceReviews/onboarding all have canonical tables from 0009/0010 already — adapter work only); (2) the analytics/backend residuals that would extend the new Analytics tab: `DashboardRepository.revenueByCategory()` (lights up the drill-down Departments tab AND a per-category trend overlay) and cycle-level revenue aggregation (the category pills + cycle-level slicers) — both are repository extensions, not UI work; (3) the admissions-pipeline backend if the owner confirms the funnel semantics; (4) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (5) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 35th session (2026-09-07, CLOSED) — owner mandate COMPLETE: Personnel/Workforce RBAC overhaul ("strict role-based workspaces, no mock workarounds, account provisioning, deep testing, desktop + mobile")

**T-233..T-242 (10 tasks) — the RBAC boundary is now enforced strictly on EVERY layer, all live-verified.** RBAC-300 → VERIFIED, RBAC-301 → TESTED, RBAC-302 → VERIFIED, TEST-306 → FIXED; ARCH-001 slots #7/#8/#9 closed (purchaseRequests/deliveries/inventory onto their canonical 0011 tables + migration 0084). The matrices: desktop (T-234) and Android (T-237) in lockstep — teachers lost the module-ENTRY permissions (CRM + Pédagogie hidden on both platforms) and work strictly from their in-Personnel workspaces (desktop overlays T-235; Android "Mon espace" T-237 with homeroom-scoped classes); clerks gained ViewFinancials/ManageClasses/PromoteStudent; route guards on /crm//academics//financials close the URL bypass. The DATA layer: migration 0083 (live, atomic) — teachers can never UPDATE student profiles; students/assessments SELECT scoped to classes-they-teach; parents SELECT dropped. Account provisioning PROVEN live (T-241: 14/14 — teacher+driver accounts created via the EF by a super_admin, both signed in immediately, profiles active + roles assigned + audit; teacher scoping probes 0/0/0; students UPDATE 0 rows with a succeeding admin control; escalation 403). Evidence: docs/recovery/t-241-live-verification.md. Suites: desktop 112 files/2687/0 + tsc + lint; Android 50 suites/427/0 (re-provisioned JDK 21 + SDK 35 toolchain). Live chain 0001–0084 zero drift. All repos pushed + zipped.

**Next session (36th) should pick, in order:** (1) **the T-047 Group-B ports** — the remaining mock slots after this session: releve, shifts, schedules, performanceReviews, onboarding, warehouseTasks, pricing, aiConfig, backups (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding all have canonical tables from 0009/0010 already — adapter work only); (2) the Android operations screens for the now-persisted purchase/delivery/inventory tables (the T-238/239/240 ports have no Android consumers yet — Buyer/Driver/Warehouse Android dashboards would read the same canonical data; cross-platform §10); (3) the owner-gated residuals the moment the owner supplies them (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR for the DAG action set); (4) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main); (5) the admin password rotation reminder (re-set during T-241's probe round — owner action).

---

## 34th session (2026-09-07, CLOSED) — owner mandate COMPLETE: "fully solve the DAG automation system"

**T-223..T-232 (10 tasks) — the ENTIRE DAG pipeline is live and verified end-to-end.** DAG-100 → VERIFIED, DAG-101 → RESOLVED. The full evidence chain: t-232-live-verification.md (18/18 final matrix through the desktop's exact PostgREST+RLS contract) + the per-task matrices (25/33/15/10/10/10 live, multi-hop resume cycles) + the desktop suite 105 files/2578 tests (incl. the 27-test pure-engine suite with dry-run equivalence) + the Android 416-test gradle suite. Migrations 0001–0082 zero drift; EF fleet 14/14 (workflow-execute v27 on the pure engine + workflow-resume-scheduler v1). The server engine, the SQL publish gate (cycles can NEVER be published through any writer), real actions (in-app notifications via 0077, tasks, restriction, wa.me), real context (compute_parent_summary; server-wins-over-client), persistent delays (workflow_pending_resumes + atomic-claim scheduler), the EF dry-run (prediction parity), the desktop Exécuter/Test serveur wiring, and the Android pull contract are ALL in production state. Honest residuals: owner-gated RESEND/FCM secrets, honest-skip financial mutations, no WhatsApp API (link preparation only).

**Next session (35th) should pick, in order:** (1) the owner-gated residuals the moment the owner supplies them: RESEND_API_KEY lights up send_email, FIREBASE_SERVICE_ACCOUNT_JSON lights up FCM push (the in-app rows already deliver), the WhatsApp Business API turns the honest wa.me preparation into real sends; (2) a service-role financial-RPC ratification pass (ADR) for apply_discount/create_invoice/generate_document/account_adjustment — the honest-skip reason names exactly what is missing; (3) the T-047 Group-A remainder (12 slots — pricing FIRST per the T-160 priority); (4) scheduler crash-recovery hardening (stale claimed rows — the inert filter is documented in the EF source); (5) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main).

---

## 33rd session (2026-09-07, CLOSED) — owner mandate COMPLETE (payment-modal 16:9 fix + payments-journal issuer/timestamp + FULL DAG automations): T-219..T-222 (4 tasks, desktop-hub scope) Desktop financials: UI-305 closed at the design-system level — dialog variants are now height-capped flex columns (max-h-[88vh]; the old grid layout made the body's flex-1 inert and pushed footers off-screen) + NEW 2xl wide-form tier (max-w-6xl ≈ 16:9 stage) + the payment modal restructured into a responsive 12-col two-column form with the payer recap pinned in the always-visible footer (T-219); the payments journal now identifies the ISSUER (parent avatar/name/family code/linked student + collector attribution) and the EXACT dd/MM/yyyy HH:mm transaction timestamp (T-220). Automations: DAG-100 closed — the workflow builder is now a functional system (T-221): 29 node subtypes (was 17) incl. the educational trigger/action set; NodeInspectorDrawer with the VISUAL PREDICATE BUILDER (conditions finally AUTHORABLE); a PURE branch-aware dry-run engine driving BOTH the canvas "Tester" visualisation AND the mock executor (single source of truth — the linear single-flag executor is gone); zoom/pan/minimap/snap-to-grid/topological auto-layout; 3 one-click educational templates (Relance échelonnée / Alerte assiduité / Clôture trimestrielle). Suite **100 files / 2513 / 0** (+6 files/+74 tests) + typecheck + eslint 0 err (401 warnings, net −3 vs HEAD). Registry: +UI-305 +DAG-100 (201 entries, 155 TESTED); T-218's missed status flip truth-synced. Live chain untouched: 77/77 = 0001–0080 zero drift; website + Android untouched. All repos pushed + zipped.

**Next session (34th) should pick, in order:** (1) **the workflow-execute EF port** — port the branch-aware execution semantics (dry-run engine semantics: per-branch closure, route_switch first-passing-route, topological node_results order) into the Supabase Edge Function so the SERVER engine matches the client's dry-run predictions (DAG-100's registered residual; the desktop's mock executor is already the reference); (2) the real action executors behind the action nodes (notifications insert via the 0077 notify_parent_user RPC, tasks insert, WhatsApp wa.me URL construction, is_financially_restricted account restriction) — mock-simulated today; (3) the T-047 Group-A remainder — 12 slots left (pricing FIRST per the T-160 priority — the financial-drift surface + it would light up the still-empty service_enrollments table the portal's T-211 card reads); (4) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main); (5) the owner-gated residuals (FIREBASE_SERVICE_ACCOUNT_JSON / RESEND_API_KEY / web-push env vars / AUTH-200 client) whenever the owner supplies them.

---

## Previous sessions

**32nd session (2026-09-07, CLOSED) — owner mandate COMPLETE (portal detail enrichment + full Trimestre labels + migration-token consistency): T-209..T-218 (10 tasks).** Portal: Profile renders the parent identity rows (T-209) + per-child identity/enrollment cards (T-210) + the enrollments surface (T-211: service_enrollments [the previously-dead hook's first consumer] + the REAL per-student installment fee schedule [1 276 live rows] + academic-year context); dashboard child cards carry level·class·status (T-213); T1/T2/T3 → full localized "Trimestre 1/2/3" on every surface with the t-202 mobile pattern for the wider labels (T-212). Backend: migration **0080** (T-214) tightened service_enrollments_select (INFO-300 tenant-wide leak → staff/parent-own/student-self), applied live atomically — **chain 77/77 = 0001–0080 zero drift**; Management-API quirk #9 persisted. Tooling: policy-census script (T-215) — **189/189 zero policy drift**; closing MIG-TOKENS round (T-216) 10/10 GREEN (dual-key, EF fleet 13/13, ALLOWED_ORIGINS echoing, anonymous 401). Desktop: workforceAttendance ported (T-217 — the dashboards trio complete; the three-same-named-AttendanceRepository trap documented). Suites: website 40/544/0 + lint + strict build; desktop 94/2439/0 + typecheck + lint 0 err; Android unchanged. All repos pushed + zipped.

**Next session should pick, in order:** (1) the T-047 Group-A remainder — 12 slots left (releve, pricing, aiConfig, backups, shifts, schedules, performanceReviews, onboarding, purchaseRequests, deliveries, inventory, warehouseTasks; pricing first per the T-160 priority — the financial-drift surface); (2) a service_enrollments WRITER (the table is empty — a desktop pricing/enrollment writer would light up the portal's T-211 card with real data); (3) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main); (4) the owner-gated residuals (FIREBASE_SERVICE_ACCOUNT_JSON / RESEND_API_KEY / web-push env vars / AUTH-200 client) whenever the owner supplies them.

**31st session (2026-09-06, CLOSED) — owner mandate COMPLETE (mobile-first UI overflow fixes + full-stack consistency verification): T-199..T-208 (10 tasks).** The portal's layout was live-measured at real phone widths for the first time (a seeded tagged UI-TEST family in the live DB + the @supabase/ssr cookie sign-in + DOM-geometry probes — recipe persisted in docs/testing/strategy.md §6). Five NEW defect families registered and fixed same-session with source-scan guards: UI-300 (dashboard grid blowout, Critical — 935px@320), UI-301 (unbreakable Intl currency KPI values, High), UI-302 (non-wrapping header rows ×4, Medium), UI-303 (tab-label clipping, Medium), UI-304 (raw English kind enums, Low). Viewport matrix **36/36 GREEN** (9 views × 4 widths, 0px overflow everywhere incl. sub-tabs + chat). Desktop parity per §10 (18 latent bare grids + debt-KPI break-words; suite **92/2422/0**); Android scanned — structurally immune, unchanged (zero commits). Website suite **35 files / 514 / 0** + lint + strict build. The T-204 live round re-proved the migration-token consistency the owner asked for: chain 76/76 = 0001–0079 zero drift, dual-key health (ADR-009), EF fleet 13/13 ACTIVE (1:1 hub source — the older "14/14" note was stale), ALLOWED_ORIGINS canonical set echoing (ACT-203 intact), anonymous EF → 401. UI-TEST family cleaned (0 residuals; audit rows kept). All three repos pushed + zipped for the owner. The actionable OPEN set remains owner-gated (Firebase service account, Resend key, web-push env vars, AUTH-200 client) or device-gated — same as the 30th-session close.

**30th repair session (2026-09-05, CLOSED) — owner mandate COMPLETE ("fix the messaging system, not the chat UI — messaging is not working at all — across mobile/website/desktop + the Supabase backend end to end; fix every type of broken PDF generation; apply the migration tokens; consistent everywhere").** Root cause found and fixed: the chat CONVERSATION layer was complete, but the messaging DELIVERY layer did not exist — recipients without an open app were never notified of anything. Delivered as T-189..T-198: the migration tokens (0072–0074 applied live, chain now **76/76 = 0001–0079 zero drift**) + ACT-203 closed live (the canonical ALLOWED_ORIGINS set deployed; the script repaired for the dead PATCH/PUT + masked-GET secrets API) + FIREBASE_PROJECT_ID set + §7 all green (T-189); migration **0075** chat→notification fan-out (verify 8/8 + a FULL live two-user round-trip **10/10**: channel → message → notification → RLS bell reads → symmetric reply → markRead persists — T-190); migration **0076** restoring the live-drifted notifications_select (`using (true)` data leak, REG-004 discovered by the round-trip — T-191); migration **0077** canonical notify_parent_user RPC + the desktop debt reminders that never delivered (4 stacked silent failures — sendReminder no-op, wrong columns, no target, nonexistent audit RPC — T-192, round-trip 7/7); migration **0078** homework→parent fan-out (the "notifié aux parents" promise now true — T-193, verify 6/6); **ADR-014** + website client-side receipt AND statement PDFs + migration **0079** dropping the orphaned receipts table/bucket (CROSS-101 + UNKNOWN-004 + T-066 closed — T-194/T-195); Android chat_channel deep-link routing (T-196). Suites: desktop 91/2420/0 + tsc + lint 0 err; website 29/496/0 + lint + strict build; Android re-run with the re-provisioned toolchain. +4 NEW problems (MSG-101/200/201, REG-004 — all TESTED with live evidence). Still owner-gated: FIREBASE_SERVICE_ACCOUNT_JSON (real FCM), RESEND_API_KEY, web-push Firebase env vars, AUTH-200's OAuth client step, T-173a alert volume (UNKNOWN-020).

**Next session should pick, in order:** (1) a POLICY-census hardening script (the REG-004 lesson — pg_policy expressions vs the local chain, machine-checked at session openings; small, high-value); (2) the T-047 Group-A remainder (workforceAttendance, pricing, releve, aiConfig, backups, shifts, schedules, performanceReviews, onboarding, purchaseRequests, deliveries, inventory, warehouseTasks); (3) T-044 pass 3 screen migrations (COORDINATE FIRST: `git fetch` + `git log origin/main`); (4) the owner-gated residuals above (whenever the owner supplies the Firebase service-account JSON / Resend key, push+email go live on top of the notification rows that now land automatically).

## 29th-session history (retained):

**29th repair session (2026-09-05, ACTIVE) — owner follow-up report “it's still [not working]” (minutes after redeploying the T-184 fix) DELIVERED as T-185..T-188.** The paste proved ACT-201 fixed LIVE (the EF URL now resolves correctly) and exposed the second layer: **ACT-203** — the production preflight fails the CORS access-control check because the live `ALLOWED_ORIGINS` EF secret allowlists only the dev origin (live-probed: OPTIONS 200 + ACAO `http://localhost:5173`; the EF CODE is correct — _shared/cors.ts echoes any allowlisted origin). Fix is owner-token-gated: `SUPABASE_ACCESS_TOKEN=sbp… bash elimtiyaz-desktop/scripts/update_allowed_origins.sh` (idempotent, merge-only, self-verifying; instant — NO redeploy). The desktop paste exposed **AUTH-301** (T-185, TESTED: refreshSession’s empty-password `signIn(email, "")` rebuild 400’d on every refresh → forced logout at every session expiry; now the shared buildSession, no second grant — 6/6) and **SEC-114** (T-186, TESTED: frame-ancestors removed from the meta CSP — the spec ignores it there and Chromium warned on every launch; guard inverted). Website **ACT-204** (T-187, TESTED: TypeError → the dedicated “Impossible de joindre le serveur” message, 5/5). Desktop 90 files/2410/0 + tsc + lint 0 errors; website 29 files/488/0 + lint + strict build. All three repos pushed; zips regenerated.

**Next session should pick, in order:** (1) if the owner supplied the sbp_ token — run `update_allowed_origins.sh` + the 0071–0074 live applies + the full §7 credentials checklist re-run; (2) the T-047 Group-A remainder (workforceAttendance, pricing, releve, aiConfig, backups, shifts, schedules, performanceReviews, onboarding, purchaseRequests, deliveries, inventory, warehouseTasks); (3) T-044 pass 3 screen migrations (COORDINATE FIRST: `git fetch` + `git log origin/main`); (4) the owner-gated residuals (T-173 part a alert VOLUME / UNKNOWN-020, AUTH-200 first sign-in, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, web-push Firebase env vars — optional, not activation-blocking).

**28th repair session (2026-09-05) — CLOSED: owner mandate COMPLETE:  "Finish all the remaining tasks" + the mid-session URGENT queue-jump "Fix this one right now: the activation code is not working" (DELIVERED as T-184 ahead of the batch).** Progress so far: T-174 (MIG-TOKENS + 0069 live registration repair, ARCH-015 — committed), T-175 (calendar port → calendar_events + migration 0070, live-verified 5/5, UNKNOWN-021 registered — committed), **T-184 (the owner's activation-code report — COMPLETED and committed: website `/undefined/functions/v1/bind-activation-code` 404 root-caused to a DIRECT build-time env read in a client component [ACT-201, Critical] and fixed via the `@/lib/env` T-096 fallback chain; desktop `bindActivationCode` structured-error surfacing [ACT-202]; website 28/483 + strict build, desktop 86/2371 + tsc + lint 0 errors; live URL-routing probes prove the fixed client reaches the real EF — the owner must REDEPLOY the portal on Vercel).** **T-176 + T-177 (workflows + workflowRuns ports — migration 0071 + SupabaseWorkflowRepository/SupabaseWorkflowRunRepository + canonical execute EF path, 13/13 suite, full 86/2371 — COMMITTED, live apply of 0071 owner-token-gated: `SUPABASE_ACCESS_TOKEN=… bash scripts/apply_0071_live.sh` + verify_t-176.sql).** **T-178 (leaveRequests port — migration 0072 [RequestType-widened CHECK + reviewed_by_name] + SupabaseLeaveRequestRepository, 11/11 suite, full 87/2382 — COMMITTED, live apply of 0072 owner-token-gated: `SUPABASE_ACCESS_TOKEN=… bash scripts/apply_0072_live.sh`).** **T-179 (suppliers port — migration 0073 [category + fractional rating] + SupabaseSupplierRepository, 10/10 suite, full 88/2392 — COMMITTED, live apply of 0073 owner-token-gated: `SUPABASE_ACCESS_TOKEN=… bash scripts/apply_0073_live.sh`).** **T-180 (tasks port — migration 0074 [created_by_name + author_name] + SupabaseTaskRepository, 12/12 suite, full 89/2404 — COMMITTED, live apply of 0074 owner-token-gated: `SUPABASE_ACCESS_TOKEN=… bash scripts/apply_0074_live.sh`)**. **T-181 (Android Room dismissedAt + server-dismissed eviction — migration v13→v14 + pull-side eviction, 13 new tests, full 48/410/0 + lint — COMMITTED android 4589a19; T-173 part b CLOSED, part a [alert VOLUME / UNKNOWN-020] remains owner-gated).** Next: T-182 (registry closeout) → T-183 (zip + push with the owner's PAT). The original session plan details: T-173 part b (Android Room dismissedAt), T-047 ports per the T-160 scoping order.

**27th repair session (2026-09-05) — CLOSED: owner mandate COMPLETE ("clone the repos, fix the syncing problem + the weird 1,000 notifications; cross-platform; read the audit docs; follow AGENTS.md").** Delivered as two tasks: **T-171** (SYNC-200 — the owner's screenshot decoded: the red 1 170 badge was the sync queue's terminal-failed count [1 170 = 390 students × 3 mock-era Excel imports, queued 03/08/2026 pre-Supabase-wiring; server sync_queue holds exactly the 3 544 synced rows and ZERO failure rows → the pre-push audit upsert failed RLS on the baked-in placeholder tenant "default"; data never at risk — 390 students / 0 duplicate codes live]. Shipped: `retryFailed()` + `discardFailed()` [17-test suite], drain-time placeholder-tenant re-scope + foreign-UUID-tenant local skip [SYNC-102 actor-skip preserved], persisted `lastSyncAt` [localStorage, set on every completed online drain incl. no-ops], honest no-op toast, SyncTab retry/discard actions + per-row error tooltips, SyncIndicator tooltip retry action; full suite 2 336/0) and **T-172** (NOTIF-200 — the overdue-alert flood: 958 unread, 0 dismissed, never resolved. Shipped the alert lifecycle in BOTH the run-overdue-scan EF [redeployed live; verification run: alerts_resolved=267, active 958→691, active_without_live_overdue=0; auth matrix 401/401/401/200] and the desktop generator [13/13, best-effort mirror per NOTIF-100], + the Android `dismissed_at IS NULL` pull filter [17/17]; ADR-013 records the decision; alert VOLUME deliberately deferred → T-173/UNKNOWN-020). Android 397/0; website untouched (476/0 baseline re-verified). **ONE owner action remains: on their desktop, Settings → Synchronisation → "Supprimer les échecs" (purges the 1 170 mock-era residue whose payloads cannot pass server FK validation — the data is already in Supabase).**

**Next session should pick, in order:** **T-173** (the NOTIF-200 residual: the overdue-alert VOLUME product decision [digest vs per-installment — needs the owner + an ADR; UNKNOWN-020] + the Android Room `dismissedAt` migration so server-resolved alerts stop lingering locally), then the carry-over from the 25th session — **T-047 ports** (the scoping is DONE — start with the calendar adapter [website reads `calendar_events`, desktop writes mock], then workflows/workflowRuns; 19 of the 23 remaining slots need NO product input), then **T-044 pass 3 screen migrations** (unblocked by pass 3a's ElScrollableTabRow — but COORDINATE FIRST: check `git fetch` + `git log origin/main` before touching the hub screens), then the owner-gated residuals. Carry-over owner residuals: the AUTH-200 first-sign-in verification (ONE click), the ONE "Supprimer les échecs" click above, RESEND_API_KEY + from-domain for workflow emails, FIREBASE_SERVICE_ACCOUNT_JSON for real pushes, and the T-039/T-069/T-127/T-129 device-gated live round-trips.

**26th repair session (2026-09-05) — CLOSED: owner mandate COMPLETE ("thoroughly review and refactor the AI patch so it is smoothly, safely and natively integrated; implement the same feature across website + Android + backend; no regressions").** The mandate was delivered as four tasks: **T-164** (desktop — the patch's false "missing DB tranches" diagnosis disproven by live read-only audit [1 276 installment rows, 259/259 charged parents]; the REAL root causes were in `SupabaseDebtRepository.refreshProfile` [hollow installments + column-stripped ledger select = DATA-013]; the drawer's inline financial logic extracted to the canonical `domain/calc/payment/billing-breakdown.ts`; the drawer now consumes `repos.installments.observeByParent` — the same stream as the payment modal; the patch's 8 test regressions [notification contract + DESK-CSP-202] + the T-145 audit-branch loss repaired; 16/16 new tests incl. the 285 000/125 000 owner vector; full suite 2302/0), **T-165** (migration 0069 — adjustment/reversal description CHECK guard, NO backfill needed, applied live: convalidated=true), **T-166** (website — 5th "Facturation" tab via the read-side canonical port `src/lib/canonical/billing-breakdown.ts` + `describeAdjustment` diagnostics; 468/0; port-honesty registry updated), **T-167** (Android — `core/BillingBreakdown.kt` mirror + ParentDetailViewModel derivation + "Prestations facturées" card; 388/0; compileDebugKotlin green). DATA-013/DATA-014/REG-003 registered with evidence; change-log + current-state updated. **LESSON (REG-003): an unregistered junk-named patch had landed 8 red tests + a weakened CSP on main — the session-opening full-suite run is what caught it; keep that ritual non-negotiable.**

**Next session should pick, in order:** the carry-over from the 25th session — **T-047 ports** (the scoping is DONE — start with the calendar adapter [website reads `calendar_events`, desktop writes mock], then workflows/workflowRuns; 19 of the 23 remaining slots need NO product input), then **T-044 pass 3 screen migrations** (unblocked by pass 3a's ElScrollableTabRow — but COORDINATE FIRST: check `git fetch` + `git log origin/main` before touching the hub screens), then the owner-gated residuals. Carry-over owner residuals: the AUTH-200 first-sign-in verification (ONE click), RESEND_API_KEY + from-domain for workflow emails, FIREBASE_SERVICE_ACCOUNT_JSON for real pushes, fresh activation codes for YOUCEFI AYA / ABADA YAHIA (their 2026-09-03 codes were never persisted), and the T-039/T-069/T-127/T-129 device-gated live round-trips. Suggested follow-ups from the 26th session (small, low-risk): (a) consider a TTL/focus-refresh for `SupabaseDebtRepository`'s one-shot profile cache (the drawer's installment stream is fresh; the profile totals rely on the pre-existing one-shot cache — same behavior as the payment modal's debt meter, so parity is preserved, but a future session could unify it with the CacheFreshness pattern); (b) the Android lint baseline may pick up new warnings from the ParentDetailScreen card — run lintDebug at the next Android session and re-pin if needed.

**25th repair session (2026-09-04) — CLOSED: mandate "keep going" (continuation session run as one of ~10 concurrent agents on the shared repos — coordination-first task selection).** No Supabase credentials this session → the opening ritual's LIVE chain check was owner-token-gated; the chain check ran LOCALLY (65 files 0001–0068, no numbering gaps/duplicates, append-only guard OK, registrations embedded in 0066–0068). No push credentials → all commits live on branch `session25-agent-work` in each repo (zip-for-push handoff). Batch (10 tasks, coordination-safe): **T-155** (pristine baselines all three repos), **T-156** (registry truth-sync: the stale T-104 "Ready" row flipped with completion evidence, T-020/T-082 tombstone, DATA-005 note, CROSS-004/REG-002 headers), **T-157** (the DebtMeter's dormant "Crédit parent" row wired through the ADR-010 derivation — repos.debt.observeParentProfile → displayParentCredit → DebtMeter.unallocatedCredit; +2 source-scan guards, t-104 suite 10/10), **T-158** (all 4 exhaustive-deps findings resolved + dead vars; DISCOVERY: the desktop lint warning baseline had silently drifted 307→384 — re-pinned to 379, exhaustive-deps class at ZERO), **T-159** (JDK 21 + SDK 35 re-provisioned after the container reset; recipe re-created at scripts/android-env.sh; DISCOVERY: secrets-gradle-plugin 2.0.1 resolves .env/.env.example against the ROOT project only — app/.env is never read; §8.1 documented; suite reproduced 44/372 before any new work), **T-044 pass 3a** (the ADDITIVE prerequisite only — screen migrations deliberately left to concurrent agents: DS `ElScrollableTabRow`, LazyRow pill row with legacy `ModernSecondaryTabRow` behaviour parity, DS tokens only; 5 semantic Robolectric tests; TWO live pitfalls persisted: the semantics `selected` val-reassignment collision, LazyRow composing only visible pills vs ui-test assertions; suite 45 files/377/0 + lintDebug green), **T-160** (T-047 scoping delivered — `docs/architecture/t-047-repository-migration-scoping.md`: 23 of 45 desktop slots remain mock-backed; the canonical chain already has tables for 19 of them → adapter-only; teachers = modeling ADR; clubs/psychology/orthophonie = the only real product decisions; verified cross-platform drift: website reads `calendar_events` while desktop's calendar slot is mock, Android pull-syncs `workflow_runs` while desktop's workflow slots are mock), **T-161** (website full verification: lint clean, 26 files / 457 tests, strict build — zero drift), **T-162/T-163** (closeout + zips). Suites at close: desktop 82 files / 2286 +5s / 0 failures + typecheck + lint 0 err/379 warn; Android debug 45 files / 377 / 0 + lintDebug; website 26 / 457 / 0 + lint + strict build. Hub commits: a02edd7, 9e1c974, 0aca2a9, 61fe19b, 84b5aff, 6596c0b; Android: 1d1e07d, 49b83aa; website: none (verification-only).

**Next session should pick, in order:** **T-047 ports** (the scoping is DONE — start with the calendar adapter [website reads `calendar_events`, desktop writes mock], then workflows/workflowRuns [Android pull-syncs `workflow_runs`]; 19 of the 23 remaining slots need NO product input), then **T-044 pass 3 screen migrations** (unblocked by pass 3a's ElScrollableTabRow — but COORDINATE FIRST: the hub-screen migration, the MainScreen route-model rewrite and the legacy-tree deletion were the contested recommendations in the 25th session; check `git fetch` + `git log origin/main` before touching those screens), then the owner-gated residuals. Carry-over owner residuals: the AUTH-200 first-sign-in verification (ONE click), RESEND_API_KEY + from-domain for workflow emails, FIREBASE_SERVICE_ACCOUNT_JSON for real pushes, fresh activation codes for YOUCEFI AYA / ABADA YAHIA (their 2026-09-03 codes were never persisted), and the T-039/T-069/T-127/T-129 device-gated live round-trips.

**24th repair session (2026-09-03) — CLOSED: owner mandate COMPLETE ("fix the 3 owner-reported issues + apply the migration tokens + verify everywhere + zip for push").** Session-opening ritual: chain check **63/63 = 0001–0066 ZERO DRIFT** (fresh token) + live census (0 activation codes, 2 new parent signups — the owner was actively testing). **The three owner-reported issues are ALL live-verified FIXED:** (1) **ACTIVATION** — 4 stacked defects found and fixed (codes never persisted [missing tenant_id], silent phantom fallback, the deployed EF 401'd pending users via extractAuthContext and never activated, the screen's error-object regex): T-145 (issuance), T-146 (ADR-011 — ONE canonical EF with activation semantics, the website's drifted copy deleted), T-147 (live round-trip **19/19**: pending user → 200 + bind + active + parent role + 2 audit rows; consumed code → 404; active → 409; anonymous → 401), T-153 (precise localized error mapping). (2) **MESSENGER** — ADR-012 (amends ADR-008): migration **0067** (open_parent_admin_channel RPC + both chat insert policies tightened — parent↔parent now structurally impossible) applied live atomically, verify_t-148.sql **14/14**; website "Contacter l'administration" (T-149, 6/6). (3) **PARENT/CHILD NAMES** — migration **0068** (259 parents displayed their first child's name; now "Famille {lastName} — phone" per the importer's canonical convention) applied live atomically, verify_t-151.sql **11/11** (students untouched — checksum-proven). Chain at close: **65/65 = 0001–0068 ZERO DRIFT**. Suites: desktop **82 files / 2284 tests** + typecheck + lint 0 errors; website **25 files / 457 tests** + lint + strict build; EF fleet **14/14 ACTIVE** (bind-activation-code redeployed 2 rounds). Registry: OPEN 7→6 (CROSS-004/CROSS-009 closed), +3 new problems (ACT-200, CHAT-200, DATA-012 — all TESTED with live evidence). NEW discoveries persisted: PostgREST upserts cannot target the partial role_assignments_active_uidx index (select-then-insert rule); extractAuthContext must never gate pre-activation endpoints; audit_logs is append-only even for service role (verification rows stay as evidence); the pair-idempotent DM code means staff create_direct_channel(parent) resolves the SAME channel the parent opened.

**Next session should pick, in order:** **T-044 pass 3** (unchanged from the 23rd session's recommendation — Android design-system: extend the DS with a scrollable tab-row component first, then migrate the hub screens; 29 legacy importers remain), then the MainScreen bottom-nav route-model rewrite, then the final legacy-tree deletion. Carry-over owner residuals: the AUTH-200 first-sign-in verification (ONE click), RESEND_API_KEY + from-domain for workflow emails, FIREBASE_SERVICE_ACCOUNT_JSON for real pushes, and the three NEW owner-gated end-of-chain confirmations from this session (one desktop "Code d'activation" click; one real parent's "Contacter l'administration" click; re-issue fresh activation codes to the parents who received phantom ones — YOUCEFI AYA and ABADA YAHIA's codes were never persisted server-side, their parents must be given NEW codes from the fixed desktop).

**23rd repair session (2026-09-03) — CLOSED: owner mandate COMPLETE ("finish all the remaining tasks + apply the migration tokens + ensure everything works across all platforms + zip for push").** Session-opening ritual: chain check **63/63 = 0001–0066 ZERO DRIFT** (fresh token) + EF fleet 13/13 ACTIVE + 26-probe anonymous-deny matrix all 401 + auth health/JWKS/RLS/portal-render/census/secrets (T-140). **T-141: AUTH-200 CLOSED** — the opening check DISCOVERED the owner had set the Google OAuth client_id+secret between sessions; the agent ran the runbook step-3 enable PATCH and live-verified the authorize endpoint 302 → accounts.google.com (was 400 Unsupported provider since 2026-08-31; the #1 Critical user-facing blocker; VERIFIED awaits the owner's first real sign-in — ONE click on the production portal). **T-142:** all three pristine baselines green (desktop 79/2271 + typecheck + lint; website 24/440 + lint + strict build; Android 44/372 + 42/367 + lint). **T-143 = T-043 COMPLETE** (the equivalence consolidation, 4 scoped passes per ADR-006: the _tier4 mirror + 3 retired trees deleted, −~2900 lines; the canonical mirror is now in the typecheck graph — 2 latent type defects fixed; corpus access documented in docs/testing/cross-platform.md §2.1; ADR-006 → Implemented with the decision-2 deviation recorded). **T-144 = T-044 passes 1–2** (DUP-004 closed — the legacy theme family deleted, the screenshot test migrated to the production theme, WEAK-013 closed; the settings module FULLY migrated to the design system — 37→29 legacy importers; the hub-screen blockers documented: needs a scrollable DS tab component + the route-model rewrite). Registry: OPEN 11→7, TESTED 135→138. NEW discoveries persisted: the owner is an active actor on AUTH config too; the canonical mirror was never type-checked; roborazzi's captureRoboImage doesn't write PNGs in the repair container; the container network stalls large transfers (JDK download needs a curl retry loop). **Owner residuals: ONE click — sign in with Google on the production portal to flip AUTH-200 to VERIFIED; RESEND_API_KEY + from-domain for workflow emails; FIREBASE_SERVICE_ACCOUNT_JSON for real pushes; the Google consent screen is in TESTING mode (parents must be test users, 100 cap).**

**Next session should pick, in order:** **T-044 pass 3** (Android design-system: first EXTEND the design system with a scrollable tab-row component — the 6-tab FinancialsHub prerequisite — then migrate the hub screens + dashboard/crm/academics/financials/personnel/chat/routing screens using the settings module as the reference pattern; 29 files remain), then the MainScreen bottom-nav route-model rewrite, then the final legacy-tree deletion. Carry-over owner residuals: the AUTH-200 first-sign-in verification, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, the Vercel web-push env vars (runbook's production-push section), the T-039/T-069/T-127/T-129 device-gated live round-trips. Android toolchain recipe: `/home/z/my-project/scripts/android-env.sh` (includes the .env secrets-plugin quirk + the NEW curl retry-loop quirk).

**22nd repair session (2026-09-03) — CLOSED: owner mandate COMPLETE ("re-apply the migration tokens with the fresh access token, verify everything works correctly across all platforms, keep the migration applied + consistent everywhere").** COMPLETED: T-130 (MIG-TOKENS 34/34 with the fresh sbp_ token — chain 62/62 zero drift at open), T-131 (PUSH-104 close: real Resend send for workflow send_email via the shared _shared/send-email.ts + approve-signup email hardened incl. the dead-URL discovery — both EFs deployed live, 12/12 tests), T-132 (PARENT-102: missing_target_parent guard + audit, deployed live, 7/7), T-133 (registry truth-sync: NOTIF-101 flipped with live pg_policies evidence + 25 MORE stale table rows synced + the ARCH-011/WEAK-030 duplicates and this file's corrupted block removed — OPEN 62→12), T-134 (DATA-005 desktop: 18 parent-name render sites canonicalized, 8/8, PDFs included), T-136 (website: lint 0 / 440/440 / strict build / live render 200), T-137 (desktop: 79 files / 2271 / 0, lint-delta 0), T-135 (Android toolchain re-provisioned: debug 44/372/0, release 42/367/0, lint green — TWO discoveries persisted: the cmdline-tools _latest URL quirk + the .env merge mechanism), T-139 (ARCH-014: the OWNER applied 0066 parent_first_name_backfill live DURING the session — reconstructed + committed + verified 6/6; chain now 63/63), T-138 (this closeout). Suites at close: desktop 79/2271/0, website 24/440/0 + strict build, Android 44/372 + 42/367 + lint, EF fleet 13/13 ACTIVE + all deny anonymous. LESSON (ARCH-014): re-run the chain check at session CLOSEOUT — the live project has another active actor.

**20th repair session (2026-09-02) — CLOSED: owner mandate COMPLETE ("fix this auth thing or tell me what to do" + "apply the migration tokens, consistent everywhere").** Opening chain check: 62/62 zero drift. The auth answer for the owner: **the portal's Google sign-in needs ONE remaining step that only the school's Google account can do — create the Google Cloud OAuth client (~10 min, runbook `docs/operations/portal-google-oauth.md` step 1) and either run the step-3 PATCH or hand the client id/secret to the next agent session.** Everything agent-side is DONE and live-verified this session: production origin added to site_url + uri_allow_list (T-119 — without it the redirect would have bounced to localhost even with the provider enabled), the provider-disabled UX (T-116, 19th session), the pre-sign-in error alert + console noise eliminated (T-120), the FCM env warning actionable (T-121). Also completed: **T-122** (MIG-TOKENS 15/15), **T-069** (REALTIME-104 — the previously-thrice-deferred Android realtime backbone; unit-tested 11/11), **T-124** (registry hygiene), **T-123** (lint-baseline investigation — no change warranted). Deferred with reasons: T-102-follow-up (4th deferral — but its infrastructure dependency is NOW LANDED), T-043 (full dedicated session per its own note).

**Next session should pick, in order:** **T-102-follow-up** (Android chat read-side + online sends — T-069's channel infrastructure is landed; the chat_messages routing entry is a one-line addition; the backend is complete since 0061 and desktop/website are live), then **T-043** (equivalence-framework consolidation per ADR-006 — big, schedule a full session). Carry-over follow-ups: **AUTH-200 (STILL the #1 user-facing blocker — the owner's 10-minute Google OAuth client step; everything else is done)**, web-push env vars on Vercel (Firebase WEB app id + VAPID — runbook's production-push section), the T-039 cross-device live round-trip (needs two live devices), and the T-069 live websocket round-trip (needs a real device/emulator). Android toolchain recipe: `/home/z/my-project/scripts/android-env.sh` (includes the .env secrets-plugin quirk).

Previous state: the 16th repair session (2026-09-01) CLOSED with: — the 16th repair session (2026-09-01) is CLOSED: T-105 COMPLETE (the owner-mandated Excel-corpus equivalence — the corpus now matches the source workbook EXACTLY on every platform: migration 0063 applied live + registered [chain 60/60 = 0001–0063, zero drift]; live verification 259/259 × 6 + write-path ops 14/14; cross-platform desktop 259/259, Android 259/259, website 262/262, triple comparator 304/304).** DATA-010 (double-remise) + DATA-011 (row-242 family never imported) registered VERIFIED; DATA-008 extended to corpus-level closure; DATA-003 classification corrected. Full evidence: docs/recovery/t-105-live-verification.md.

## 58th session (2026-09-13, CLOSED) — the four-part owner mandate COMPLETE: T-326..T-332

The desktop-wide portal layout (UI-315), the per-child dossier (all children, notes + canonical history + enrollments), the payment-coverage parity chain (PARITY-004 — one derivation on both platforms), and the approvals→existing-parent workflow all landed LIVE-VERIFIED (the 16/16 approval round-trip with zero duplication + the 9/9 coverage probe — docs/recovery/t-332-live-verification.md). The ARCH-012 drift (0088/0091/0092/0093) was reconciled: the chain is live 90 = local 90. Suites: website 565/565 + lint + strict build; desktop 3144/3144 + tsc + eslint 0 err. All repos pushed + zipped.

## Current recommendation

**Session 59 should pick, in this order:** (1) verify the 57th session's pushed android commits land on origin/main and run the Android equivalence suite with the desktop as a sibling (the full gate ran WITHOUT the sibling checkout — `AndroidEquivalenceTest` needs `AgentGithubUplaod` at ../ per §8); (2) the **proof-scanner round-trip auto-fill** (UI-312 residual: scanner result → settle dialog via a savedStateHandle/shared-VM channel); (3) the **Android mirror of the T-330 coverage chain** (PaymentRepository.allocationsForPayment exists on desktop+website; the Android payment detail could read payment_allocations via the same precedence — a small parity task); (4) continue **DUP-003 pass-3** — the untouched screens still import the legacy `ui/components` layer (StudentDetail, ParentDetail, the hub tabs, Settings); (4) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, ADR-005 ratification); (5) T-047 Group-B ports (pricing FIRST per T-160). TEST-300 lesson: run the FULL suite on a pristine tree before closeout and record per-file evidence.

### Session 57 outcome (2026-09-13, Android UI/UX overhaul — owner mandate, ALL DONE)

T-320..T-324 COMPLETE + T-325 closeout: 7 android commits (5f38922..0471e0b); the reference conversation applied with explicit APPLY/ADAPT/REJECT discipline (3 comparison reports BEFORE coding); +UI-310..314 (all TESTED, 243 detailed entries); the full Android gate green (58 classes / 516 / 0 / 1 skip + assembleDebug); RealtimeSyncT069Test helper race fixed (third occurrence, pinned); toolchain re-provisioned (JDK 21.0.5 + SDK 35, scripts/android-env.sh); zips + push with the owner PAT; session-numbering race with the CALC-002 thread documented (android commit messages say "55th", hub says "57th" — task IDs authoritative).

## Current recommendation

**Next session (37th) should pick, in this order:** (1) the **T-047 Group-B ports** (pricing FIRST per the T-160 priority — the financial-drift surface; releve/shifts/schedules/performanceReviews/onboarding have canonical tables from 0009/0010 already — adapter work only); (2) the **admissions-pipeline backend** if the owner confirms the funnel semantics (feeds the now-generic RecoveryFunnelCard with true admissions stages — the UI-306 residual); (3) the Android operations screens for the T-238/239/240 tables (Buyer/Driver/Warehouse dashboards — cross-platform §10); (4) the owner-gated residuals the moment they are supplied (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp Business API, the financial-RPC ratification ADR); (5) T-044 pass 3 screen migrations (COORDINATE FIRST: git fetch + git log origin/main). TEST-300 lesson for every session: run the FULL suite on a pristine tree before closeout and record per-file evidence. NOTE: the Android gradle gate in THIS container needs JDK 21 + SDK 35 re-provisioned if the container resets (recipe at /home/z/my-project/scripts/android-env.sh, re-runnable). The 36th session's desktop-only mandate left Android/website untouched — verify both at open.

Session outcome (2026-09-01, sixteenth session — owner-mandated Excel-corpus equivalence, ALL DONE): session-opening chain check 59/59 (0001–0062) JSON-diffed, zero drift; T-105 VERIFIED (migration 0063 applied live atomically with registration — double-remise cancelled with compensating adjustments [223 parents, +9,709,700 DZD], the missing row-242 MAMER family created exactly per the workbook [255,000 devis/versements, balance 0], per-student devis alignment with last-tranche absorption, full waterfall replay; importer fixed: no REMISE ledger entry + C3 tranche reconciliation so a fresh import cannot reintroduce the bugs; regression suite t-105-import-shape 5/5 on the real workbook). Verification: verify_t-105.sql 259/259 × 6 (M1 paid, M2 netdue, M3 balance vs the workbook — was 61/258; C3/C4/C5 internal), verify_t-103.sql 8/8 still TRUE, verify_t-105-ops.sql 14/14 (payment/registration/pending-check/refund through the canonical RPCs — every read surface updates consistently), cross-platform equivalence desktop TS 259/259 == backend RPC, Android Kotlin 259/259 == backend (304/304 scenarios), website 262/262, triple comparator 304/304; desktop 69 files / 2192 tests; website 18 files / 415 tests; chain 60/60. New problems: DATA-010 + DATA-011 (both VERIFIED same session). DATA-008 extended, DATA-003 classification corrected. Session closed with all three repos zipped for the owner to push.

Session outcome (2026-09-01, fifteenth session — owner-reported finance divergence, ALL DONE): session-opening chain check 58/58 (0001–0061) JSON-diffed, zero drift; T-103 VERIFIED (migration 0062 applied live atomically with registration — payments-row fix 90k→100k per the Excel source, 54 missing transport charges inserted (+2.06M), dettes folded into Tranche 1 (METAH NADA 7,000 / DAHMANI FARES 8,000), SIDI MAMER's overstated schedule corrected to the devis net (−36,500), full waterfall backfill → 1,310 payment_allocations covering 860 payments + expected/excess/remark on all 888 payments; desktop read paths canonicalized: INV-4-family installmentRemaining/totalOutstanding (due − paid − pending), Tranches + student tabs on the canonical helper, net dossier totalDue with Crédit-parent card, mapPaymentRow hint fields); live verification 8/8 (C1–C8 all true, 0/258 residual three-way mismatches — was 197/181); desktop 68 files / 2187 tests ALL PASS (+10 tests); typecheck clean; lint 0 errors; chain re-verified 59/59. New problems: DATA-008 (VERIFIED same session), DATA-009 (OPEN → T-104). DATA-001/002/003/004 → VERIFIED. Session closed with all three repos zipped for the owner to push.

Session outcome (2026-08-31, thirteenth session — owner-requested ~10-task batch, ALL DONE): portal .env.local configured ("Missing configuration" fixed, live headless render verified, Google button enabled); live migration chain verified 57/57 = 0001–0060 at open; T-041 TESTED (migration 0059, live 10/10); T-030 TESTED (migration 0060, live 9/9, website 135/135); T-058 TESTED (append-only migration guard machine-enforced 3 ways: npm test, check:migrations, 9/9 shell matrix); T-050 TESTED (both platforms; Android 234/234, desktop 2165/2165; ARCH-012 registered from pristine-run evidence); T-036 PUSH-103 portion TESTED (website 144/144); T-026 TESTED (canonical overdue rule, 10/10); T-054 TESTED (regenerateForCycle real + trigger column, 7/7); T-062 TESTED (dead code removed, 5/5, APK 29.8 MB); T-063 TESTED (absence threshold ≥3/current-term, 10/10); T-064 TESTED (config-dialog security + placeholder detection complete, 9/9). Android suite 275/275 (+41); desktop 66 files / 2171 tests; website 15 files / 144 tests. AUTH-200 registered (owner runbook). 12 problem entries flipped to TESTED (DRIFT-006/007, WEAK-006/007/008, BUSINESS-007, ATT-103, SEC-004/005, DEAD-007/008/009).

Session outcome (2026-08-30, ninth session — owner-requested): live backend health re-verification executed (migrations 0049-0052 confirmed applied + registered; MV values byte-identical to the payments cross-check; FCM functions register_fcm_token + deactivate_fcm_tokens verified present with SEC-106 caller logic; chat_read_receipts policy + trigger + function verified present; RLS blocks anon reads on all 6 core tables; auth census down to 1 real user) → 3 new problems registered (ARCH-009, ARCH-010, DRIFT-013), 6 tasks completed (T-088, T-080, T-089, T-091, T-087, T-092 — all TESTED), 2 new tasks opened (T-093, T-094). Desktop suite 47 files / 2029 tests ALL PASS (+18 tests, +2 files vs session 8); website suite 8/105 (unchanged); Android suite 219/219 (unchanged); live Supabase migrations 0049-0052 all applied + registered. All verification scripts persisted under `/home/z/my-project/scripts/`.

Session outcome (2026-08-30, seventh session): T-016 TESTED — desktop reconciler wires `crossCheckBalanceSum` + `crossCheckParentCredit` (BUSINESS-001); T-027 TESTED — canonical attendance rate in attendance-view + bulletin (desktop narrative-modal) (WEAK-019); T-061 TESTED — payment-proof trigger scoped to INSERT + method/proof changes (WEAK-200, migration 0045); T-031 TESTED — parent self-update trigger role-gated (SEC-008, migration 0046); T-029 TESTED — `approve_account_request` rejects silent re-binding (PARENT-101, migration 0047); T-071 TESTED — RLS INSERT policies tightened for chat/notifications (CHAT-100/101, NOTIF-101, migration 0048); T-079 VERIFIED — admin-created login accounts live round-trip (create → sign-in → change-password → sign-in with new password); T-004 VERIFIED — live curl matrix on the 4 cron EFs (all 4 deny anonymous; valid CRON_SECRET accepted). NEW discovery: BUG-NEW-001 → T-083. Desktop suite 45 files / 2011 tests ALL PASS; website suite 8 files / 96 tests ALL PASS; live Supabase migrations 0044-0048 applied + cron EFs deployed + CRON_SECRET set + create-user-account EF deployed + 5 SQL verification scripts run live.

Session outcome (2026-08-29, sixth session): T-002 TESTED — Android auth fail-closed (SEC-101), email-substring role inference deleted, roles via `current_user_roles()` RPC with support_staff fallback (SEC-102), real SDK JWT in Session.accessToken (WEAK-101), demo sandbox restricted to unconfigured+debug; CROSS-100's Android half closed (demo chips removed). Suite 219/219 (+12 regression tests). NEW discovery: ARCH-008 — the Android lint gate has never been green (315 pre-existing NewApi errors, no baseline ever) → T-082. Live sign-in matrix owed (needs a real Supabase). Website side: T-065 TESTED — WEAK-023 + DRIFT-010 comment corrections with 2 source-scan guards (92/92, strict build green, no lint change); website AGENTS.md synced with the T-009/T-049 state.

Session outcome (2026-08-29, fifth session): T-081 TESTED (Android build gate restored: 4 compile errors + the equivalence-harness path defect; 202/202 baseline + 45/45 equivalence green for the first time), T-019 TESTED (sync pushes propagate 4xx/5xx; root-cause correction recorded: the swallowing layer was guard's catch-Throwable, not the SDK), T-049 TESTED (website strict build + the GenericSchema/never defect fixed; 90/90). Android toolchain bootstrapped at /home/z/my-project/tools (recipe in change-log).

Session outcome (2026-08-29, third session): T-079 completed — admin-created user accounts (owner feature request): Settings → Comptes tab, `UserAccountRepository` (domain + Mock + Supabase), `create-user-account` EF (super_admin only), `admin_create_user_account` RPC (migration 0044, EXECUTE service_role-only). Client stack TESTED (19-test suite incl. the create → sign-in round-trip; full suite 1988/1988; typecheck clean); EF + migration IMPLEMENTED (live deploy pending — no Deno/Postgres/Supabase in this environment).

Session outcome (2026-08-29, second session): T-003 TESTED (SEC-103 resolved — changePassword now delegates to the repository; audit entry no longer forged; 12-test regression suite + full suite 1969/1969). T-002 checked and confirmed infeasible headlessly (toolchain evidence above).

Batch outcome (2026-08-29): T-001 TESTED (SEC-100 resolved; passwords still need rotation in deployed environments), T-009 TESTED (SEC-007 resolved; Google OAuth is the only website auth path), T-010 IMPLEMENTED (ARCH-002; sandboxed launch log still needed on a desktop host). New discovery registered: DEAD-201 (desktop lint unrunnable) → T-078.

Suggested order for the first sessions (all P0, dependency-free):

1. ~~**T-001** — desktop credentials (SEC-100)~~ — DONE 2026-08-29 (TESTED)
2. ~~**T-002** — Android SUPER_ADMIN fallback (SEC-101/102)~~ — DONE 2026-08-29 (TESTED, sixth session — live sign-in matrix pending)
3. ~~**T-009** — website mock-auth removal (SEC-007)~~ — DONE 2026-08-29 (TESTED)
4. ~~**T-003** — desktop changePassword no-op (SEC-103)~~ — DONE 2026-08-29 (TESTED, second session; was the T-002 fallback)
5. **T-005** — tenant-scoped RBAC resolver + admin policies (TENANT-100/101)
6. **T-006** — SECURITY DEFINER RPC caller verification (SEC-110/106/112/111)
7. Then the rest of Phase 0 (T-005, T-006, T-007, T-008, T-071) before any Phase 1 financial work. ~~T-010~~ DONE 2026-08-29 (IMPLEMENTED — needs a sandboxed launch log to reach TESTED). ~~T-004~~ DONE 2026-08-29 (TESTED — live curl matrix pending). ~~T-078~~ DONE 2026-08-29 (TESTED — lint gate restored, 307-warning baseline documented).

## What NOT to pick next (and why)

- **T-028, T-037, T-038, T-042, T-045, T-059, T-066, T-067, T-070, T-072** — blocked on unresolved business/architecture decisions (see `unknowns.md`).
- **T-041 (promotion flow), T-024 (Android promotion), T-040 (justification review), T-039 (Android pull)** — depend on earlier infrastructure tasks (T-025, T-023).
- **T-047** — needs product scoping (Needs Investigation).
- Anything involving financial write paths **before** reading ADR-002/ADR-004 and the domain rules — the audit's central lesson is that this codebase breaks most when agents change financial flows without checking the canonical implementation first.

## Evidence required before starting any task

- The task's problem entries read end-to-end.
- The named tests exist or you know which you must write.
- A one-paragraph implementation plan consistent with `docs/agents/workflow.md` (DISCOVER → UNDERSTAND → PLAN → VERIFY PLAN before any code).
- Any deviation from the task's stated resolution recorded in the task entry before implementation.

## Windows packaging closeout — 2026-09-16

T-506 / PACK-100 is IMPLEMENTED: Windows x64 installer and portable executables were built and hashed locally. No real-Windows smoke test was possible here. Next for this owner request: run the installer/portable launch, dashboard/CRM and PDF-save smoke test on Windows, then attach evidence to T-506. Binaries remain gitignored local deliverables; the recipe and icons are tracked. The standing backend recommendation T-337 is unchanged.
---

## Newly registered P0/P1 academic integration work

- **T-402 (P0): Full Batch Promotion Integration and Unified Academic Model.** Audit and repair every promotion surface, canonical history/grade-level write, Android sync, downstream consumers, and class-formation handoff. The legacy promotion path must remain dead.
- **T-401 (P1): Full Filière / Spécialité Integration Across the System.** Establish one canonical Niveau → Filière → Spécialité → Classe/Section model with DBA-owned persistence/validation and integrate it across every relevant UI, search, filter, statistic, placement, history, and import/export path.

These tasks supersede any attempt to solve either requirement as a single-page UI patch.


### T-404 — Automatic Timetable Generation & Constraint Scheduling — COMPLETE (TESTED, 2026-09-22)

T-404 is implemented end to end (migrations 0109/0110 + ADR-020 + the canonical domain/solver/repository/UI + the packaging gates). Full evidence: `docs/recovery/t-404-live-verification.md`. The remaining steps to flip it VERIFIED are owner actions on suitable hosts: (1) execute the built Windows x64 portable .exe (`release/El-Imtiyaz Desktop-0.1.0-win-x64-portable.exe`, SHA256 in `release/SHA256SUMS.txt`) on a real Windows machine and generate/review/publish a timetable there; (2) optionally build the NSIS setup variant on a wine-equipped or Windows host (`npm run package:win` — the packager is wine-aware). Registered follow-ups for the next agent: the legacy mock timetable façade removal (teacher.ts TimetableEntry + the mock CRUD + the academic-year drawer's "Couverture EDT" KPI reading the mock store — rewire the KPI to `observePublishedEntries`), SCHED-102 (the misleading SchoolDay comment goes away with the façade), and the website parent-portal consumption of the published timetable (the RLS path already exists — parents read published entries by design).
