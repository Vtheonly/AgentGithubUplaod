# Current State — Project Snapshot (2026-09-14, SEVENTIETH session — the website↔desktop student-document synchronization: T-372, SYNC-110)

## Current state snapshot (2026-09-14, 70th session CLOSE)

**The owner's document-sync mandate is COMPLETE, TESTED, and LIVE-VERIFIED in BOTH directions (T-372; SYNC-110 CLOSED TESTED + the live legs VERIFIED — 276 registry entries).** The mandate: documents uploaded from either platform must be stored and synchronized through the same backend/storage system so both the website and desktop see ALL documents for every student (the LINDA ALIOUAT example: website-uploaded birth certificate website-only; desktop-uploaded ID desktop-only). **The discovery split the stack in two:** the STORAGE layer was ALREADY unified (both platforms upload to the same `student-documents` bucket, same `<tenant>/<student>/<file>` path — the 64th/66th sessions' UPLOAD-101..104 work), but the METADATA layer was built TWICE: the website lists documents from the canonical `student_documents` TABLE (0005/0019/0043) while the desktop's DocumentsTab read/wrote the `students.documents_json` JSONB column (0038) — each platform blind to the other's store, and the desktop's full-array `updateStudent({documents})` write a last-write-wins clobber that could never coexist with concurrent website inserts. **The fix (hub):** the domain kind set widened to the DB CHECK parity (the website's 7 kinds verbatim — the old 4-value union could not even represent a birth certificate); the GRANULAR repository contract `addStudentDocument`/`removeStudentDocument` (the `documents` field REMOVED from `UpdateStudentInput` — the clobber path is now a compile error); the Supabase repository seeds documents from the table (embedded into `Student.documents`) with best-effort uploader-name resolution (the chat-repository pattern); the mock implements the same contract in-memory; the DocumentsTab offers the 7 canonical kinds; **migration 0098** backfilled the legacy `documents_json` entries into the table (idempotent, category→kind mapped, storage-path-keyed) — applied LIVE atomically with its registration (93→94; LINDA's 2 documents unified). **Website: NO code change** (already table-backed); its suites re-run as the parity evidence (48/618/0 + lint + strict build). **Verification:** verify_t-372.sql LIVE 7/7 (BEGIN…ROLLBACK, zero residue); the cross-platform E2E `t-372-doc-sync-e2e.py` 11/11 — check D: a parent sees a staff-inserted row through the website's EXACT query (desktop→website); check F: the staff seed query sees a parent-inserted row (website→desktop); RLS scoping intact; zero residue; desktop gates 161 files / 3364 / 0 + tsc 0 + eslint 0 + build. **AGENTS.md §15.31 added** (the canonical metadata-store rule: a shared storage bucket synchronizes nothing — every client's document metadata goes through the `student_documents` table; full-array JSON writes on shared entities are forbidden). **Coordination (THREE concurrent sessions):** the T-370 (Class Placement Studio) and T-371 (WORKFORCE-501) sessions ran in parallel — the T-371 session registered "T-371" on the remote minutes before this session's push, so this task was RENUMBERED T-372 at merge (both registrations kept; migration numbering follows: 0097 theirs, 0098 this task's; the live chain's 0097 gap self-heals at their next `db push --include-all`). **Registered residuals (honest):** UNKNOWN-022 (the 4 UPLOAD-104-era orphaned binaries — each a duplicate of a later successful upload; owner decision), the `documents_json` retention decision (owner-gated; no client reads it now), the desktop cache freshness (documents appear after the TTL/focus reseed — realtime push would ride T-337). Full report: `docs/recovery/t-372-live-verification.md`. **Next:** T-337 (REALTIME-105) remains the standing P0 (next free migration number 0099+); the concurrent sessions' scopes (T-370/T-371) continue per their registrations.

# Current State — Project Snapshot (2026-09-14, SIXTY-EIGHTH session — the commit-74d3ebb Personnel & Workforce backend integration: T-369, WORKFORCE-500)

## Current state snapshot (2026-09-14, 68th session CLOSE)

**The owner's one-commit-at-a-time UI-only-commits mandate has its FIRST target complete: commit 74d3ebb (the Personnel & Workforce Subsystem UI) now has its FULL backend, live-applied and live-verified (T-369; WORKFORCE-500 CLOSED TESTED — 275 registry entries).** The mandate: the recent commits contain UI only — implement the missing logic behind each one individually, test end to end, push+merge after each commit (the concurrent-agent discipline held — zero conflicts across the session's pushes). **The session-OPEN baseline was RED at 31 tsc errors** (the UI-only commits changed the domain contracts with no repository-layer update — owned and repaired by this task, not a separate cleanup). **The backend (migration 0095, applied LIVE atomically with its registration):** `staff_absences` (the two-way justification loop with a DB state-transition trigger), `salary_adjustments` (the immutable append-only audited salary history), `salary_payments` (the monthly ledger, unique per tenant+personnel+period, NO delete policy), the two 0055-hardened audited RPCs (`adjust_personnel_salary` — role/tenant-guarded, FOR UPDATE, the four adjustment semantics, write_audit_log; `record_salary_disbursement` — idempotent upsert, the period's one-offs netted from salary_adjustments), the tasks/leave_requests lifecycle columns (completed_by/completion_note/reviewed_by/review_note; amount_requested/clarification_request/clarification_response) + the CHECK widenings — including the DISCOVERY that 0010's tasks CHECK never allowed `needs_review` (the T-180 header's claim was false; a worker's submit-for-validation would have been DB-rejected). **The desktop wiring:** the ONE mock implementation extended to the new contract (the dead parallel `mock/workforce.ts` DELETED — it shadowed the `workforce/index.ts` directory index and split-brained T-314's side-effects into a dead store; broken reactivity, no audit hooks, phantom pers-* seed ids), the four Supabase repositories extended to the contract, the `PersonnelRepository` payroll surface (`adjustSalary`/`recordSalaryPayment`/`observeSalaryPayments` — the canonical RPC path; the client never computes the next base), the Payroll UI rewired (the paid-state `useState` simulation and the client-side salary math DELETED; the per-period statuses derive from the reactive payments stream). **Verification:** verify_t-369.sql LIVE 23/23 (BEGIN…ROLLBACK, zero residue — two real defects caught and fixed PRE-commit: the floored-cut delta invariant, the trigger's is_excused guard); the LIVE REST E2E 26/26 through the real PostgREST+RLS+RPC+trigger path with a staff JWT (raise/bonus artifacts, audit rows, the idempotent netted disbursement, the full absence loop + the illegal-transition 400, the RLS tamper-proof check, the leave clarification loop, the task review columns, zero business residue); gates at close: **tsc 0 (was 31) / eslint 0 errors / 161 files / 3350 tests / 0 failed (+27 new: 23 backend-semantics + 4 UI end-to-end) / build GREEN**. **AGENTS.md §15.30 added** (the file-shadows-directory-index trap + the append-only cascade rule + the PostgREST no-op-update semantics). **Registered residual (honest):** the worker-side `respondClarification` write is RLS-blocked by the 0019 manager-only leave_requests UPDATE policy — the mock and the manager-side path work fully; widening needs its own registered task (a role-scoped own-row UPDATE policy). Full report: `docs/recovery/t-369-live-verification.md`. **Next:** T-337 (REALTIME-105) remains the standing P0; the same treatment awaits the remaining UI-only commits (9ddde68 — the Class Placement Studio) per the owner's mandate.

# Current State — Project Snapshot (2026-09-14, SIXTY-SEVENTH session — the unified PDF/Report/Excel generation mandate: T-368, REPT-500..505)

## Current state snapshot (2026-09-14, 67th session CLOSE)

**The owner's unified PDF/Report/Excel generation mandate is COMPLETE, TESTED, and LIVE-VERIFIED against the real Supabase data (T-368; REPT-500/501/502/503/504/505 all CLOSED TESTED — REPT-503/505 with the SQL cross-check evidence — 274 registry entries).** The mandate: fix EVERY PDF generation across every section (finance, payments, receipts, reports, dashboards, student records, grades, statistics); REWRITE the reporting logic rather than patch it; support BOTH PDF and Excel reports; and export the ENTIRE application's data and statistics into structured Excel. **The RED proof (probe + 9 exact-signature test failures):** `formatDzdPlain` emits U+202F (the fr-FR digit-group separator) → pdf-lib's Helvetica `drawText` THROWS `WinAnsi cannot encode (0x202f)` — the desktop's payment-receipt and account-statement generators crashed on EVERY amount >= 1000 DZD (i.e. every real tuition amount; Arabic-script parent names threw `(0x0628)` the same way). The website port had fixed this exact seam in T-194/T-195 — the desktop REFERENCE never got the back-port (the DRIFT-011 divergence pattern, inverted direction). **The repairs (hub 47fb21e — T-368a):** the draw-layer seam (null-safe `sanitizePdfText`, `dzdPdf` WinAnsi-safe amounts, sanitize-in-`drawKeyValue`, two-pass `stampPageFooters` writing the TRUE `Page i/N` on EVERY page, `continuationHeader` row flow) — the account statement's 25-payment cap and every `y<100` silent truncation REMOVED (long tables flow onto "(suite)" pages), the receipt notes render fully, the report-document footnote flows instead of dropping. **The new system (hub 827f8c8 — T-368b/c):** NEW `excel/full-export.ts` — ONE workbook, 13 domain sheets (Résumé/Parents/Élèves/Personnel/Paiements/Tranches/Journal/Dépenses/Notes/Présences/Créances/Classes/Services) with the canonical computed indicators (collection rate, installment coverage, debt totals, attendance rate, per-category charges), built OVER the existing export-engine (§6 reuse — no second Excel implementation); NEW `receipt-pdf/global-reports.ts` — the five PDF twins (revenue, debt aging, roster, personnel, expenses) as thin adapters over the SAME reactive streams the XLSX exports consume (one data path, two renderers); the Reports tab gains the "Export complet" card + honest PDF buttons (the T-088 fake-badge gap closed by implementation). **REPT-505 (the mid-session discovery):** the legacy `data-export.ts` Résumé computed `charged + adjusted − paid` against SIGNED ledger amounts — domain payments are NEGATIVE credits, so the outstanding was inflated by 2×|paid| on every real export while the parents-sheet balance was correct; now the canonical sign convention everywhere (abs() displays, signed-sum outstanding). **Test evidence:** 22 new tests across three t-368 suites (RED captured first; the completeness pins use content-stream text extraction — zlib inflate + hex/literal Tj decode — asserting ALL 60 receipt numbers DRAWN, true page counts, drawn footnotes); full gates GREEN: tsc 0 / eslint 0 errors (555 baseline warnings) / **159 files / 3323 tests / 0 failures** (baseline 3301). **The LIVE leg (the owner's tokens, read-only service-role):** the ACTUAL generators ran over the REAL rows (261 parents / 391 students / 907 payments / 1280 installments / 2064 ledger entries / 24 classes): the full workbook (318 KiB), the busiest parent's statement (28 payments → 2 pages), the revenue report (907 payments → 23 pages), and the largest real receipt (1 507 000 DZD → 1 page); `t-368-verify-live-summary.ts` then reconciled EVERY Résumé total against direct SQL aggregates — ALL exact (facturé 115 835 800 / encaissé 58 270 500 / ajustements signés −3 401 200 / solde 55 831 700). **AGENTS.md §15.29 added** (the four permanent reporting safeguards: dzdPdf at the draw seam, the ledger sign convention, the PostgREST 1000-row pagination rule, the content-stream text-extraction technique). Full report: `docs/recovery/t-368-live-verification.md`. **No backend change** (no migration, no RPC, no RLS — §15.4 held; the fixes are client-side rendering + new export surfaces over existing engines). **Residuals (honest):** the payslip's gross=net identity (no deduction engine exists — an unknown, never invented; documented); the live attendance_records table is empty (the Présences sheet renders honestly empty); the website carries no staff global-reports surface by design (parent portal). **Next:** T-337 (REALTIME-105) remains the standing P0 (the recommendation stands since the 60th session).

# Current State — Project Snapshot (2026-09-14, SIXTY-SIXTH session — the portal upload TABLE leg: T-367, UPLOAD-104)

## Current state snapshot (2026-09-14, 66th session CLOSE)

**The owner's live console evidence is root-caused, fixed, and live RED→GREEN-proven (T-367; UPLOAD-104 CLOSED TESTED — 269 registry entries).** The owner pasted the production portal console: `rest/v1/student_documents → 403` (twice — once per upload attempt) alongside the Firebase env warning, weeks of "the upload is still broken" after the 64th session had closed the storage-path family (UPLOAD-101/102/103) as TESTED. **The root cause (live DB forensics + the live RED proof):** the portal's `UploadDocumentDialog` row INSERT omitted `tenant_id` — the column has NO default (hub 0005) and the 0043 parent INSERT WITH CHECK requires `tenant_id = current_tenant_id()`; the omitted column lands as NULL → SQLSTATE 42501 → PostgREST answers **HTTP 403 on `/rest/v1/student_documents`** (a SELECT under RLS never 403s — it silently filters; **a 403 on a table URL means a WRITE was RLS-rejected**: the browser-console diagnostic signature, now pinned in AGENTS.md §15.28). The T-360 storage-path fix was correct but only HALF the flow — every attempt succeeded in Storage then ORPHANED the object when the row insert 403'd; `uploaded_by` was verified CORRECT (the portal sends `user_profiles.id` = exactly `current_user_profile_id()`), the tenant was the single failing term, and the sibling writer (`chat_messages`, `tenant_id: channel.tenant_id`) already followed the right convention. **The verification gap that let UPLOAD-101 close early:** the 64th session's `t-359-upload-e2e.py` probed the STORAGE leg only — the row-INSERT leg was never exercised. **The fix (website 51ca7e5):** one line — `tenant_id: tenantId` in the insert payload (the dialog already receives the prop since T-360) + the NEW t-367 whole-src regression guard (6 tests, the t-184/t-199/t-360 family pattern) + the t-359 e2e TABLE leg (checks L0/L/M0/M/M2/N — the probe now covers the FULL two-step flow). **Live E2E 19/19 GREEN:** L RED = the pre-fix payload → HTTP 403 `{'code': '42501', 'message': 'new row violates row-level security policy for table "student_documents"'}` (the owner's exact console signature, reproduced); M = the fixed payload → HTTP 201; M2 = parent read-back 200 (correct tenant_id + uploaded_by — the 0043 SELECT policy leg); N = cross-tenant tenant_id still 403 (the WITH CHECK not weakened); Z = zero residue (parents 261 / students 391 / **docs 0 before AND after — the production table is STILL empty: no parent upload has ever succeeded; the first real one lands after the owner's Vercel redeploy picks up 51ca7e5**). **Gates:** website 48 files / 618 tests / 0 (baseline 47/612) + tsc --noEmit 0 + eslint clean + strict build green. **No backend change** (§15.4 held — the fix is the client payload the chat_messages writer already used; the live chain remains 91/91 zero drift, verified at session open). Hub commits: eab7bfe (registration BEFORE the fix, per §13) + f829033 (the e2e TABLE leg) + the closeout. **The Firebase env console line is NOT a defect:** the warning is the documented truthful owner-gated state (T-121/AUTH-202) — the Firebase project has NO WEB app (the known app id is the ANDROID app, a different app in the same project) and the web-push VAPID key has never been issued; neither can be created programmatically without the owner's Google-account OAuth. Owner runbook: `docs/operations/portal-google-oauth.md` production-push section (Firebase console → Project settings → add a WEB app → copy the `1:259221439109:web:…` id; Cloud Messaging → Web Push certificates → Generate; set both as Vercel env vars + redeploy). Until then web push stays disabled BY DESIGN and the portal is otherwise fully functional. **Next:** T-337 (REALTIME-105) remains the standing P0 (the 65th-session recommendation stands).

# Current State — Project Snapshot (2026-09-14, SIXTY-FIFTH session — the empty-state Excel restore mandate: T-364..T-366, IMPORT-106/107/108/109)

## Current state snapshot (2026-09-14, 65th session CLOSE)

**The owner's empty-state Excel restore mandate is COMPLETE and TEST-PROVEN (T-364..T-366; IMPORT-106/107/108/109 CLOSED TESTED — 268 registry entries).** The mandate: "be 100% certain that when the Excel file from Suivis clients is imported while the desktop database is completely empty, all of its data is imported correctly and completely — no missing records, no broken relationships, no incorrect calculations, no partial imports." **The answer, with evidence (the NEW two-layer integration suite, 13 tests, reading the REAL workbook's own cells via exceljs as ground truth independent of the pipeline):** from a COMPLETELY EMPTY desktop database, importing the real `Suivis clients  2026_2027.xlsx` restores ALL required data — **390 students / 253 parents / 891 payments (Σ 55,227,100 DZD = the workbook's own TOTAL VERSEMENTS column) / 1968 installments (Σ due 113,518,800 DZD, 1560 tuition + 408 transport, the ZIREG LEA 4-tranche reference row 239,500) / 1283 ledger entries / 0 rows rejected / 201 Devis-BON print-layout rows correctly skipped**; every one of the 390 rows' charges/payments/balance matches the workbook's own cells; every FK resolves; a mid-import flush failure rolls the store back to EMPTY; and importing the same file a SECOND time is a COMPLETE NO-OP in both repository modes. **The suite was built BEFORE the fixes so it could catch real defects — and it caught three (T-364, all financial write-path, none of them in the first-import path T-350 had already proven correct):** **IMPORT-107** (re-import DOUBLED the ledger in mock mode / silently dropped financials on Supabase — the direct repository flush bypassed the migration-0027 (tenant, source_type, source_id) identity contract the sync-queue RPC path had always honoured; fixed at three levels: adapter pre-dedupe, `bulkAppend`/`bulkCollect` ON CONFLICT DO NOTHING with loud Err propagation and a truthful cache), **IMPORT-108** (the mock had never implemented the optional `bulkCollect` contract — every mock-mode import fell back to the generic `collect()`, double-booking the ledger, double-allocating installments, and ignoring the deterministic IMP- receipts; now mirrored from the Supabase contract), **IMPORT-109** (`findExistingStudent` trusted the FIRST substring-search result — "LINA BELGRIMAT" substring-matches the sibling "MILINA BELGRIMAT", so re-import cross-wired the two siblings' identities and financial entries; now an EXACT order-independent match: parentId + displayName, fallback parentId + exact firstName/lastName). The T-012 (fail-fast) and T-015 (receipt-allocation) regression suites' fake clients were extended with the upsert surface and re-verified GREEN (11/11). **Gates on the merged tree (the concurrent 64th-session closeout merged forward, zero conflicts): tsc 0 errors / eslint 0 errors / 155 files / 3301 tests / 0 failures** (baseline 152/3280 — +3 files / +21 tests); t-364 8/8 + empty-state-restore 13/13. **No backend change** (no migration, no RPC, no RLS — §15.4 held; the money values and derivation logic untouched — only idempotency, atomicity, and identity resolution at the repository/adapter layer). Evidence: `elimtiyaz-desktop/src/tests/integration/empty-state-excel-restore.test.ts` + `src/tests/infrastructure/t-364-import-flush-idempotency.test.ts` + the change-log's 65th-session entry.

# Current State — Project Snapshot (2026-09-14, SIXTY-FOURTH session — the cross-platform upload mandate: T-359..T-363, UPLOAD-101/102/103)

## Current state snapshot (2026-09-14, 64th session CLOSE)

**The owner's cross-platform upload mandate is COMPLETE and LIVE-VERIFIED (T-359..T-363; UPLOAD-101/102/103 CLOSED TESTED — 264 registry entries).** The mandate: "fix uploads (files, folders, links, images) across mobile + desktop; review/fix the role/permission system and the upload flow end to end; no temporary workarounds." **The discovery inverted the premise with live evidence (T-359):** the backend was NEVER missing anything — all 10 buckets exist with correct limits/MIME allow-lists, all 24 storage.objects policies exist, the migration chain is 91/91 zero drift, and the role/permission system resolves correctly for both staff and parents (has_role('parent') true under a bound parent's JWT; cross-tenant prefixes denied). The `student_documents` table held **0 rows** because the parent upload flow had never once succeeded. The real defects were three CLIENT-side storage-path bugs: **UPLOAD-101** (the website's document dialog uploaded to `{studentId}/…` — no tenant prefix → RLS-rejected every time), **UPLOAD-102** (the desktop's UnifiedPaymentModal hardcoded `tenantId: "mock"` → every payment-proof upload RLS-rejected → check/transfer collection BLOCKED at the source), **UPLOAD-103** (the Android repository uploaded `{entityId}/{fileName}` — no tenant prefix → RLS-rejected — AND the read-oriented guard's catch-all converted the rejection into a silent local-file success: the proof never reached the server while the UI said "Prêt pour la synchronisation Supabase Storage"; the 4 s read timeout also aborted real 10 MB uploads mid-flight). **Every defect was live-proven RED and every fix format live-proven GREEN on the same accounts BEFORE any code changed** (`scripts/t-359-upload-e2e.py`, 13/13; evidence `t-359-live-verification.md`; post-fix re-verification 13/13 ×2 idempotent runs, zero residue). The fixes: the website builds the tenant-scoped path from the active child's tenant (68ad0b6); the desktop resolves `session.tenantId` with the T-053 loud guard + the phantom `therapy-attachments` bucket removed (2fef316); the Android contract carries the tenant + `StorageBuckets.objectPath()`, fails closed on a null tenant, REUSES the canonical `SyncErrorClassifier` (transport → the sanctioned offline-first local fallback; permanent 4xx → `Result.Err` surfaced, never a fake success), gets a 60 s upload timeout, and the scanner UI states the honest offline/remote status (5adc721). **AGENTS.md §15.28 now pins the permanent rule** (the tenant path prefix is mandatory on every platform; storage writes must not be wrapped in catch-alls that convert rejections into offline fallbacks; uploads get real timeouts). Two HTTP quirks persisted: RLS denials surface as HTTP 400 with `statusCode: 403`; GoTrue's admin user-delete takes the user ID in the PATH (the `?email=` form is silently ineffective — discovered when the verify script's re-run hit `email_exists`). Suites at close: website 47 files / 612 / 0 (+5 t-360) + lint + strict build + tsc 0; desktop 152 files / 3280 / 0 (+5 t-361) + tsc 0 + eslint 0 errors; Android 60 files / 566 / 0 (+8 T-362) + lintDebug + assembleDebug green. **No backend change** (no migration, no policy touched, no RLS weakened — §15.4 held). Coordination with the concurrent 63rd session honored throughout (fetch → merge forward → re-run gates before every push; 4 merges, zero conflicts).

# Current State — Project Snapshot (2026-09-14, SIXTY-THIRD session — the dashboard-statistics investigation: T-350..T-358, DASH-401..407 + DATA-017/018)

## Current state snapshot (2026-09-14, 63rd session CLOSE)

**The owner's dashboard-statistics investigation mandate is COMPLETE and LIVE-VERIFIED (T-350..T-358; DASH-401..407 CLOSED TESTED; DATA-017 documented; DATA-018 CLOSED TESTED — 261 registry entries).** The mandate: "investigate all three possibilities (Excel import / statistics engine / data fed into statistics)… trace the ENTIRE data flow… a lot of testing… one consistent source of truth." **T-350 (the investigation)** verified every claim from the owner's pasted diagnostic report against the current tree + the LIVE database: the ENGINES were already canonical (T-338/T-341, re-verified this session: **302 checks TS≡SQL**), the IMPORT is money-exact (CSV 55,227,100 DZD = payments table = ledger, 891 entries; 390 students parity; 0 rows dropped by the keyword rule), and **5 of the 12 pasted claims were REFUTED with evidence** (the negative findings are recorded so no future agent re-chases them: row-drops, payments data loss, remise/erosion-0%, REGLEMENTS/DETTES mixing, the matview split-brain misattribution). The REAL root causes sat in the WIRING layer: **DASH-401** (topDebtors `.get()` race — "Aucun débiteur" on first render + the top-10-only risk-engine feed), **DASH-402** (`observeForClass("")`/`observeByStudent("")` empty-ID queries → 0 assessments/0 attendance → the whole Diagnostic Actif surface dead), **DASH-403** (installment stats unscoped by academic year — the year selector changed the KPIs but not the waves), **DASH-404** (the label-regex tranche twin saw ZERO tuition rows — transport-only header), **DASH-405** (the Departments placeholder while the payments stream sat one prop away), **DASH-406** (four more `.get()` races in the Reports tab's export handlers), **DASH-407** (NOW-relative vs range-anchored revenue buckets — a mock↔Supabase parity break). **The fixes**: reactive subscriptions everywhere (T-351), school-wide `observeAll` streams on both repository layers (T-352), ONE academic-year billing-window semantic across the page + the Supabase repo + the mock (T-353 — the live proof: 2025-2026 = 58,354,700 / 2026-2027 = 248,000 / the two windows partition the stream), the label twin retired for the canonical `trancheNumber` column (T-354), the Departments tab from the REAL ENCAISSÉ slice (T-355), the shared `buildWindowAnchoredBuckets` helper + the house exclusive-midnight convention (T-356), and the pinned `IMPORTED_BIRTH_DATE_PLACEHOLDER` constant routing 390 placeholder birthdates to "Non renseigné" (T-357). **DATA-017 documented**: all 891 import payments carry the import date 2026-08-11 (the workbook has NO payment-date columns) — the monthly/YoY/weekly time-series degenerate to one spike by SOURCE LIMITATION; no dates are fabricated. Suites at close: **desktop 152 files / 3275 tests / 0 failures + tsc 0 + eslint 0 errors** (baseline at open: 144/3198/0 — +8 files / +77 tests); every commit merged forward with the concurrent 62nd session (MATIERE-500: T-344/T-345/T-346 landed mid-session — the analytics-tab conflict resolved by UNION, subjectConfigurations + riskDebt both kept). Evidence: `docs/recovery/t-350-live-verification.md` (the full data-flow trace + the refuted-claims registry + the live leg-2 probes + the semantic table). Residuals: the attendance-KPI "100% when no records" default (a display-honesty follow-up, registered in the closeout); the concurrent 62nd session's T-347..T-349 continue (their scope: website + Android mirrors).
# Current State — Project Snapshot (2026-09-14, SIXTY-SECOND session — the matière-architecture mandate: T-343..T-349, MATIERE-500)

## Current state snapshot (2026-09-14, 62nd session CLOSE)

**The owner's matière-architecture mandate is COMPLETE (T-343..T-349, MATIERE-500 CLOSED — TESTED + the live DB leg VERIFIED LIVE).** The trigger was the owner's own commit 158d806 ("idk mattier is wrong") — 5 academic screens patched symptom-by-symptom while the model could not express a CEM Arabe course at all (12 subjects each frozen to ONE cycle, 14 scattered coefficient fallback chains, NO contrôle-continu anywhere). The fix is the ADR-018 canonical architecture, now on ALL THREE platforms + the live database: `subjects` = identity; NEW `subject_configurations` = context (subject × academic year × level × direction: coefficient, subject_code, passing_grade, is_extracurricular, grading_recipe incl. the contrôle-continu cc weight); assessment rows SNAPSHOT the resolution at entry (never retroactive); ONE canonical resolver per platform (desktop `src/domain/calc/academics/subject-config.ts` sha d902b8f0938c, website verbatim port, Android `core/SubjectConfig.kt` mirror); the formula generalized to Σ(mark×weight)/Σ(weight) with the seeded default {1,1,2,0} BIT-IDENTICAL to the historical (D1+D2+2×Ex)/4 engine; NEW `exam_sessions` table. **Migration 0094 applied LIVE atomically** (19/19 dry-run + 19/19 post-apply; the 4 production assessment rows byte-identical; the CREATE OR REPLACE overload trap caught pre-write). **Suites at close:** desktop 147 files / 3221 / 0 + tsc 0 + eslint (T-345/T-346); website 46 files / 607 / 0 + tsc 0 + lint + strict build + LIVE RLS probes (T-347); Android 62 files / 579 / 0 on the merged tree (T-348 — Room 15→16, the cc-aware formula, the corpus runner ops). **The cross-platform corpus:** all 7 t345 subject_configuration scenarios EQUIVALENT through the real Kotlin mirror; the triple comparator 319 scenarios / 317 equivalent / 2 pre-existing unrelated discrepancies (023/025). **The desktop UI:** the SubjectConfigurationsPanel (the subject × level context matrix + the upsert dialog + the cc weight + the non-retroactivity banner). **The Android grade entry:** the Contrôle continu field renders when the recipe enables it, snapshots onto the row, and travels to the 0094 upsert RPC. All three repos pushed (hub 7bde1cb→closeout; website 3a01902; android 3b3a38d+b551c9f) + zipped. **Residuals:** the concurrent 63rd (DASH wiring) and 64th (upload-defect) sessions completed while this closeout merged — their sections below are authoritative for their scopes; REALTIME-105 (T-337) stands; the transport.ts doc-comment cosmetic stands; the owner should now populate subject_configurations through the panel for the real CEM/lycée coefficients (the 51-row seed covers the seeded contexts).

# Current State — Project Snapshot (2026-09-14, SIXTY-FIRST session — the executive-statistics mandate: T-338..T-342, STATS-400)

## Current state snapshot (2026-09-14, 61st session CLOSE)

**The owner's executive-statistics mandate is COMPLETE and LIVE-VERIFIED (T-338..T-342, STATS-400 closed — the vanity purge + the ONE unified engine proven desktop ≡ android ≡ SQL).** The mandate: "Implement the specified statistics in BOTH the mobile app and the desktop version… no independent per-client calculations — one unified calculation source… no approximations, placeholders, hardcoded numbers, or discrepancies vs. the database" + the kill list. **T-338** built the canonical derivation family (`executive-statistics.ts`: tranche waves by category × tranche_number, discount erosion via the STRUCTURED metadata markers with the 0063 cancel netting, the 4-tier debt triage + top-10 call list, family concentration, transport yield through TOWN_ALIASES, service yield, enrollment dynamics with the spread-based SECTION IMBALANCE detector, the triple-risk summary) — 44/44 tests. **T-339** shipped the Executive Command Center UI ("Pilotage Exécutif" DEFAULT, the radar front-and-center) and DELETED the vanity statistics everywhere (histogram, heatmap, revenue spline, capacity analyzer, X/Y badges, the demographics capacity contract slice) — desktop 144 files / 3198 / 0 + tsc + eslint. **T-340** mirrored it VERBATIM in Kotlin (core/ExecutiveStatistics.kt, ADR-002 source-commit-pinned) + Room MIGRATION_14_15 + the Compose cards + the same purge — Android FULL suite **558 / 0 / 1 pre-existing skip** (the 45-test verbatim mirror included). **T-341** proved equivalence at BOTH levels: the corpus (the `executive_statistics` category, desktop runner 311/0/1 pre-existing, the Android CrossPlatformEquivalenceTest asserting 8 families deep-equal — desktop ≡ android) AND the LIVE SQL truth (`verify_t-338.sql` + `verify_t-338_live.ts`: **299 checks / 0 mismatches** over the production stream — evidence: docs/recovery/t-338-live-verification.md). **The verified live picture of the school:** tuition T1 74% collected (10.8M DZD remaining) → T2 33% → T3 31% (the staircase); transport waves 96–99%; erosion 8% (318 remises 9.7M fully netted by the 0063 cancels); chronic >45j 58.35M across 196 families (the call list = top-10 exposures, max 360k); total outstanding 58.6M / 197 debtor families; 137 riders over 21 normalized routes; sibling index 1.50; 8 imbalanced grades (1AP 51-vs-1, spread 50 — exactly the owner's example); services yield honestly empty. The corpus caught TWO mirror bugs en route (the BOUMRDES/BOUMRDS transcription trap — the desktop doc comment lies; the ISO-millis dueDate drift), and the live diff caught two SQL-truth bugs (the call-list LIMIT 10, the transport double-attribution). All three repos pushed (hub 2f7a997→7c198dd + closeout; android 47a7cd7→12994e7; website in sync). **Residuals:** the desktop transport.ts doc comment says "BOUMRDS" while the code uses "BOUMRDES" (cosmetic follow-up); the triple-risk radar's academic/attendance legs show their honest "non évaluable" state until the school records assessments/attendance (3 rows / empty on live); T-337 (REALTIME-105 publication migration) remains the standing next task.

# Current State — Project Snapshot (2026-09-13, SIXTIETH session — the website grades-display mandate: T-336, GRADE-102/REALTIME-105)

## Current state snapshot (2026-09-13, 60th session CLOSE)

**The owner's grades-display mandate is COMPLETE and LIVE-VERIFIED (T-336, GRADE-102 closed).** The owner reported "exam grades already entered and recorded in the system but the website is not displaying them correctly" and asked for desktop-parity handling (multiple subjects/exams/grades, no hidden records). The evidence-based triage PROVED the retrieval layer correct first (11/11 live checks: the portal's exact PostgREST query under a bound parent's RLS returns the canonical `assessments` rows with the joined subject — reading the canonical table since 03f6365), then LIVE-REPRODUCED the real defect in the browser: **per-child ID routing** — `activeStudentId` was only auto-selected inside switcher components that render ONLY for 2+ children (the dashboard renders no switcher; its local `kids[0]` fallback masked the bug), so single-child parents — the most common family shape — kept a null id FOREVER and the grades/attendance/homework/financial views all rendered their empty states with recorded data. The fix: ONE canonical auto-select effect in the AppShell (null or stale id → `kids[0]`; the duplicated switcher effects removed). Desktop-parity companions: the MOYENNE/MATIÈRE KPIs now follow the term filter (per-term canonical GPA — the desktop AcademicTab / SQL `fn_calculate_student_term_gpa` / Android `termGpas` semantics; the all-terms tab = the Android `yearlyGpa` convention) and partial marks show the desktop/Android "Moyenne à paraître" hint (i18n ×3) instead of a silent "—". LIVE RED/GREEN browser round-trip (single-child ATTOUCHE parent with the admin-entered 11/11/11 grade: "Aucune note" BEFORE → full display AFTER); multi-subject/multi-term probe all-rendering; zero-residue cleanup. Suites: website **45 files / 599 tests / 0 (+14) + tsc 0 + eslint clean + strict build green**; desktop/Android untouched (read-side verification only). **NEW discovery registered (REALTIME-105, OPEN → T-337):** the portal's five realtime postgres_changes subscriptions target tables NOT in the `supabase_realtime` publication (live probe: `assessments` 0 events vs the `audit_logs` member control 1 event) — the T-033 window-focus/5-min fallback is the only effective freshness layer (matching the desktop's fetch-on-mount for grades); fix = migration 0094 following the 0085 pattern. Owner-side data note: the 3 admin-entered assessment rows belong to families without portal accounts — bind those parents via the canonical approval flow (T-331/T-332) to see them on the portal.

## Current state snapshot (2026-09-13, 59th session CLOSE)

**The owner's "every single minute detail" per-service pricing mandate is COMPLETE and LIVE-VERIFIED (T-333..T-335, DATA-016 closed).** The session opened on the owner's screenshot (the per-service view showing only "Scolarité — 350 000,00 DA — 1 éléments — 100% du total") and the clarification: "every single minute detail of what the price covers: which year, which level, the tranche framework, every included service, every condition, every component that contributes to the final price, and the entire process… structured and detailed enough that the engine cannot misinterpret, translate, or incorrectly reconstruct the profile and price." The byService aggregate (T-164/T-168) never derived any of it. Now ONE canonical `servicePricingProfiles()` derivation exists on BOTH platforms — website `src/lib/canonical/service-pricing-profile.ts` (wire rows + the NEW usePricingCatalog hook reading the 6 pricing tables, all parent-readable under 0019) and desktop `domain/calc/payment/service-pricing-profile.ts` (domain models + PricingConfig, now carrying the LIVE tranche months mapped verbatim — three variants 9/1·5, 9/12·3, 9/12·4 exist in the grid) — producing output-shape-identical, JSON-serializable profiles: academicYear, per-child coverage (grade code+label+cycle+class), the official catalog reference (annual + T1/T2/T3 with due months / zone schedule / per-grade FI / service unit price), every active condition (discount rules + early-payment bonus with deadline + late penalty), the applied remises with provenance, the tranche framework (FI/V2/2V with due dates + INV-4 remaining), and the LEDGER-HONEST price construction (gross + debit − credits = net, the explicit delta vs catalog — the T-335 live leg caught that the 0063 double-remise-cancel must net the imported remise: LINDA ALIOUAT's honest net is 350 000, delta +10 000 vs the 4am catalog 340 000). Both surfaces render a 7-section expandable card (website i18n'd fr/ar/en — 50 new keys ×3; desktop FR). The LIVE verification (scripts/t-335-live-verification.py, 21/21 GREEN, zero-residue): the pricing catalog reads through the exact PostgREST+RLS path the portal uses, and the ALIOUAT profile derives with every field. ALSO this session: the website's 66 PRE-EXISTING latent tsc errors in test fixtures repaired (REG-007 class) so `tsc --noEmit` is now a usable gate for that repo. Suites at close: website 43 files / 585 tests / 0 failures (+20) + lint + strict build + tsc 0; desktop 143 files / 3162 tests / 0 failures (+18) + tsc + eslint 0 errors. No migrations (read-side only).



## Current state snapshot (2026-09-13, 58th session CLOSE)

**The owner's four-part mandate (desktop-wide portal + per-child dossier + payment-coverage parity + approvals→existing-parent) is COMPLETE and LIVE-VERIFIED (T-326..T-332, ARCH-012/REG-008/UI-315/PARITY-004 all closed).** The session opened on TWO unregistered "idk" patches (website d515ecb + desktop 670bc82) that carried the right ideas but broke 6 guards, shipped ~40 hardcoded strings + editor junk + 2 latent tsc errors — treated as REFERENCE ONLY per the owner's instruction, then properly redone. The session-opening chain diff caught FOUR live-applied-but-uncommitted migrations (0088/0091/0092/0093 — ARCH-012), reconstructed from live definitions with a 6/6 rollback-wrapped probe; the chain is now live 90 = local 90 ZERO DRIFT. The desktop-wide layout's REAL defect was found in the shell: the DesktopRail stacked ABOVE the content (flex-col wrapper) — fixed with lg:flex-row + a sticky full-height rail + a deliberate width hierarchy. The per-child dossier (identity + medical_notes + the canonical student_academic_histories timeline + financial enrollments) is fully i18n'd (fr/ar/en) and reads the 0091 parent-policy. Payment coverage now flows through ONE canonical chain on BOTH platforms (payment_allocations → ledger join → single line — PARITY-004 closed with 891 live payments / 1,342 rows zero over-allocation). The approvals parent-picker flags already-bound parents pre-submit (Parent.authUserId mapped). **The crown jewel: the LIVE approval round-trip 16/16 GREEN** — register → pending → admin → EF approve with target_parent_id → the EXISTING parent bound + activated + audited with ZERO duplicate parents/students/installments/payments/ledger rows, the portal login path resolving under the user's own RLS, and zero-residue cleanup (evidence: docs/recovery/t-332-live-verification.md). Suites at close: website 41 files / 565 / 0 + lint + strict build; desktop 143 files / 3144 / 5 skipped + tsc + eslint 0 errors. All three repos pushed + zipped (hub + website; the Android repo untouched this session). NEW quirks persisted: audit_logs is append-only (DELETE forbidden — keep test audit rows), the Management SQL endpoint returns 201 on success, GoTrue admin-create returns 200, temp-table RLS DO-blocks need the GRANT convention.

## Current state snapshot (2026-09-13, 57th session CLOSE)

**The owner's UI/UX-overhaul mandate ("apply the useful ideas from the previous conversation to the CURRENT app; preserve business logic; test; document; zip + push") is COMPLETE (T-320..T-325, all TESTED).** Three repos cloned (android + hub + website); the 5,485-line reference conversation parsed and EVERY idea compared against the current tree via 3 research reports with APPLY/ADAPT/REJECT verdicts BEFORE coding — the reference's fabricated content (fictional discount fields deleted by CALC-001, an invented 20-town transport list, reduced dialogs, dropped activity feed/timeline) was identified and EXCLUDED. **Shipped (7 android commits):** the 4-step CRM registration wizard with a full success screen (codes + copy + WhatsApp share) + REAL per-zone transport dropdown (new `PricingRepository.observeTransportPricing()` + `TransportPricing` model) + the blank-grade zero-tuition guard; CounterPayment banner-on-all-steps + success reset + the receipt-erasure defect fixed (`refreshFamilyData`) + the ENGINE-derived allocation preview; PaymentDetail humanized hero; the expense flows' EXACT-centimes parse (the 15.07→1506¢ float bug) + permission-aware actions + markPaid confirmation; the DEAD Matières navigation wired (the directory was unreachable from the hub); per-tab empty states everywhere; the T-231 workflow nodeResults finally rendered; humanized statuses/roles; PhoneUtils.openWhatsApp(message?). **Verification:** per-module `compileDebugKotlin` green ×5, then the FULL gate `./gradlew testDebugUnitTest assembleDebug` — **58 test classes / 516 tests / 0 failures / 1 env-gated skip — BUILD SUCCESSFUL** (JDK 21.0.5 + SDK 35 re-provisioned from scratch after the container reset; recipe re-runnable at scripts/android-env.sh). One test fixed en route: RealtimeSyncT069Test's poll helper raced the 7-table assertion set (third occurrence of the helper/assertion-decoupling pattern — AGENTS.md §37 class, now pinned). **Registries:** +UI-310..UI-314 (all TESTED) = 243 detailed entries; T-320..T-325 recorded. **Residuals:** proof-scanner round-trip auto-fill (UI-312 entry); the legacy ui/components layer remains wired in untouched screens (DUP-003 continues); gradle.properties daemon heap lowered 2048m→1400m for the 4GB container (two OOM daemon kills this session).


## Current state snapshot (2026-09-12, 52nd session CLOSE)

**The owner's completion-audit question ("did you do everything, refactor the entire last commit excluding the DAG, and push?") is CLOSED with re-verified evidence (T-313a):** the pushed commit 328dad9 WAS complete and pushed (git status clean, origin/main = 328dad9, no parallel-DAG-agent commits at audit time; `git diff 9e70078..328dad9` on DAG paths EMPTY — the scope directive held; the ACAD-104 tenant stamping, RBAC-301 strict homeroom scoping, auto-provision removal, and all three T-313 test suites verified IN the tree; FULL suite re-run **135 files / 3095 / 0 / 5 skips**, identical to the 51st closeout; android 5842a97 + website f3d9965 untouched and in sync) — **BUT the audit caught one false claim: "tsc 0 errors."** The same tree returned **8 tsc errors in the two NEW T-313 test files** (fixtures missing required domain fields; `termStructure: { terms: 3 }` against a string-literal union; an invalid direct cast) — invisible to `vitest run` because esbuild transpiles WITHOUT typechecking, and the 51st session never re-ran tsc after writing the tests. **Fixed this session (REG-007, CLOSED — TESTED):** the four duplicated inline fixtures extracted to shared constants ANNOTATED WITH THE DOMAIN INPUT TYPES (`YEAR_INPUT: CreateSchoolYearInput`, `CLASS_INPUT: Omit<AcademicClass,…>`, `SUBJECT_INPUT: Omit<Subject,…>`, `ASSIGN_INPUT: Omit<ClassSubject,"id">`) so future domain-type drift fails at COMPILE time at those lines; the cast routed through `unknown`; the permanent rule recorded as **AGENTS.md §15.25** (a green `vitest run` is NOT a typecheck — every ladder rung re-runs after ANY file change, test files included). Verification re-run in the CORRECT order (after the last edit): tsc **0 errors**, T-313 files 25/25, FULL suite **135 files / 3095 / 0 / 5 skips** (zero behavior change by design — test files + docs only). Registry: REG-007 added (235 detailed entries, totals line re-synced — it had drifted to 230 and never recorded REG-006/ACAD-104); T-313a in the task registry. **Open residuals (unchanged):** the T-313 DAG step remains the parallel agent's scope (NEXT SESSION: `git fetch` + check their commits FIRST); the `teachers` slot remains mock-backed in Supabase mode (T-047 owner-gated ADR); TeacherDashboard subject-class visibility remains a REGISTERED follow-up (class_subjects.teacher_id scoping).

> **51st session (2026-09-12) delta:** the 9e70078 shallow-fix redo — ACAD-104 root cause + REG-006 regressions (T-313, non-DAG scope). See below.

# Current State — Project Snapshot (2026-09-12, FIFTY-FIRST session — the 9e70078 shallow-fix redo, non-DAG scope: T-313)

## Current state snapshot (2026-09-12, 51st session CLOSE)

**The owner's shallow-fix redo mandate is CLOSED for the non-DAG scope (T-313, TESTED + VERIFIED LIVE; the DAG leg is delegated to a PARALLEL agent and untouched):** the unregistered commit 9e70078's symptom-level cover-ups were root-caused and replaced with real fixes. **(1) ACAD-104 — the ACTUAL SuperAdmin denial, FIXED + LIVE-VERIFIED:** `createAcademicYear` / `createClass` / `createSubject` / `assignSubjectToClass` now stamp `tenant_id: getTenantId()` with fail-loud ERR_VALIDATION guards (the T-023 in-file precedent; zero schema change; RLS stays authoritative). Live round-trip via the desktop's exact PostgREST+RLS path: RED control (old tenant-less payload → 403/42501 "new row violates row-level security policy") vs GREEN fix (tenant-stamped payload → 201 created, row read back, cleanup 204). `class_subjects` had ZERO rows in production — the assignment flow had never once persisted; it now works. **(2) REG-006 regressions repaired:** the fake-success wrapper (ERR_FORBIDDEN → fabricated `cs-` rows while stripping every prototype method off `repos.subjects`) REMOVED — the provider passes repositories through by reference (reference-identity + prototype-integrity + anti-wrapper guards are NEW tests); the RBAC-301 strict homeroom scoping restored (`me !== null && c.homeroomTeacherId === me.id` — the name-matching heuristic removed); `isSuperAdmin` pinned to the canonical `Role.SuperAdmin` (the "admin" string conflation — a zero-producer escalation surface — deleted); the render-triggered auto-provision effect in the TeachersSubTab removed (the explicit "+ Ajouter un enseignant" modal stays); the 9e70078 scratch artifacts untracked + .gitignore hardened. **(3) A NEW permanent lesson (AGENTS.md §15.24):** the "corrupted source lines" registered by the 50th-session continuation were a FALSE diagnosis — the sandbox Bash display layer eats `[h`/`[m` bracket-letter sequences; byte-level verification proved both lines intact. Never diagnose source corruption from terminal display output. **(4) Source pins made formatter-robust:** t-230 pins whitespace-normalize before matching (prettier reformatting broke 4 pins without semantic change). Suites at close: **desktop 135 files / 3095 tests / 0 failures + tsc + eslint 0 errors** (baseline at open: 2 red) — +25 NEW T-313 tests (tenant contract 9, session semantics 11, provider pass-through 5). **Open residuals (honest):** the T-313 DAG step (workflow.id-only external sync, movement-checked saves) is the parallel agent's scope — the NEXT SESSION MUST `git fetch` + check their commits BEFORE continuing; the `teachers` slot remains mock-backed in Supabase mode (T-047 owner-gated modeling ADR); TeacherDashboard subject-class visibility is a REGISTERED follow-up (needs class_subjects.teacher_id scoping + pinned-test update in a registered task).

> **50th session (2026-09-12) delta:** the owner's local-sync verification + theme-engine merge review + lint cleanup (T-312). See below.

# Current State — Project Snapshot (2026-09-11, FORTY-SEVENTH session — the owner-gated live applies + verification: T-303..T-305)

## Current state snapshot (2026-09-11, 47th session CLOSE)

**The owner's "do it yourself — here are all the tokens you need to test if it works, make sure it works" mandate is CLOSED (all three session goals VERIFIED LIVE or TESTED):** **(1) T-303 — migration 0085 applied LIVE + the realtime attributed broadcast proven end-to-end:** the 46th session's owner-gated residual (a) is closed — `scripts/apply_0085_live.sh` (NEW, token-free, the MIG-TOKENS §15-rule-10 atomic pattern) applied the publication membership to the live project (post-check: `public.audit_logs` in `supabase_realtime` CONFIRMED; the pre-existing registration row's name label `audit_mutation_notifications` is the ARCH-009 cosmetic class — versions are authoritative, chain 82/82 ZERO DRIFT). The end-to-end realtime RLS probe (production observeActivity shape: sign-in with the owner-pinned credential → postgres_changes INSERT subscription → a benign canonical write_audit_log probe) received the attributed event with **6/6 checks PASS (actor_name + actor_role=super_admin + actor_id + action + entity_type + note marker)** — the T-299 broadcast now genuinely works LIVE. The T-244 token round re-verified: dual-key auth health 200/200, anon RLS 0 rows ×5, EF census 14/14 ACTIVE. **(2) T-304 — the ai-proxy cap-40 deploy (the standing P0 since the 43rd session) is CLOSED LIVE:** `npx supabase functions deploy ai-proxy --project-ref hkvkefubghbbotgnteir --no-verify-jwt` with the re-supplied sbp_ token; the t277 live matrix re-run ALL GREEN — the 27-schema agent-stream probe FLIPPED 400 invalid_tools → 200 + Groq SSE (edge-mode copilot with the full 27-tool registry now works in production; the early-400 console class is closed); P3 200 + 46 SSE lines; P4/P5 hardening regressions hold. **(3) T-305 — the conflict guard ARMED in production traffic (residual (b) closed):** `MockStore.stageAndNotify` passes the pre-edit checkpoint row as `basePayload` on every `update` enqueue (2 production-file hunks — zero new modules); the guard now covers the ENTIRE offline edit surface (CRM parents/students + financials payments/installments/ledger); insert/delete never carry a base. Suites at close: **desktop 130 files / 3025 tests / 0 failures + tsc + eslint 0 errors** (+5 NEW t-305 tests); **Android 57 suites / 507 / 0 / 0 errors / 1 documented skip** (untouched source, baseline re-verified after the container reset re-provisioned JDK21+SDK35). **Open residuals (honest):** the Android 3-way mirror (gated on Android gaining a base-snapshot push path — its only update producer today is the deterministic refund-revert enqueue); expenses/personnel/workflows restored-but-not-staged (no dispatcher push paths yet). Token hygiene: the sbp_ token rode env vars + chat only — never any repo; the probe scripts live outside the repos.

> **46th session (2026-09-11) delta:** OFFLINE-400 CLOSED — the offline-first stabilization (T-294..T-302). See the 46th snapshot below.

# Current State — Project Snapshot (2026-09-11, FORTY-SIXTH session — the offline-first stabilization CLOSED: T-294..T-302)

## Current state snapshot (2026-09-11, 46th session CLOSE)

**The owner's "Fix and stabilize the offline-first architecture" mandate is CLOSED (OFFLINE-400, TESTED):** all six sub-gaps landed with suite evidence on BOTH platforms. **(1) The field-level red/green diff engine (T-295/T-296/T-297):** a NEW pure engine `src/domain/calc/diff/field-diff.ts` (nested objects by sorted key order, index-wise arrays with added/removed tails, null-vs-absent-vs-\"\" boundary semantics, cycle-safe, deterministic) drives the REVIVED AuditDiffDrawer (per-field rows: old value red/struck, new value green, added green-only, removed red-only, kind badges, the collapsible forensic raw view) with the FULL actor attribution block (Name + Account ID + Role — mapAuditRow + the RPC write path now carry actor_role; every row used to land NULL); the Android verbatim mirror (`core/FieldDiff.kt`, ADR-002 source-commit header) + the shared `ui/components/AuditDiffSheet.kt` REPLACED the fabricated \"Inspecteur JSON\" in both Android audit screens. **(2) The 3-way conflict resolver (T-298):** a NEW pure `three-way.ts` (recursive merge3: convergent collapse, one-side auto-merge with path bookkeeping, same-length arrays index-wise, both-changed leaves conflict with the BASE placeholder; resolveThreeWay applies per-path local/remote/manual choices; omitted choices keep base — no silent winners) + the SyncService drain's no-silent-overwrite guard (update+basePayload entries park in the NEW \"conflict\" status, the record ON the queue entry — IndexedDB-survivable, no parallel store; the guard fail-opens on fetch errors) + the production conflict-detector (7 entity kinds → tables+unique keys; the idempotent camel→snake payload projection) + the 3-column ConflictResolverModal (choice chips, live merged preview, submit disabled until every conflict is chosen) opening from the SyncIndicator AND the SyncTab + the attributed sync.conflict_detected/resolved audit entries + BOTH notifications. **(3) The realtime attributed broadcast (T-299):** migration 0085 adds audit_logs to the supabase_realtime publication (membership ONLY — RLS untouched, the recorded security deviation: realtime still filters per-subscriber by the 0019 SELECT policies) + AuditRepository.observeActivity + formatAttributedActivity + the topbar AuditActivityToaster (self-suppression) on the desktop; the Android audit_logs routing entry + AuditLogDto/mapper + pullAudits make the T-297 attributed feed LIVE. **(4) The REAL offline restore (T-300):** inspectArchive (the read-only offline archive browser with the honest corrupted verdict) → the apply (store.replaceOperationalState — the offline operating mode) → the restored-from marker + the tab banner → post-restore STAGING into the EXISTING SyncService queue (five canonical-push collections diffed per notify; expenses/personnel/workflows restored-but-not-staged — documented) → the no-data-loss reconnect drain (offline pushes 0 and keeps the queue; the online service drains the SAME persisted queue). **(5) The toggleable scheduler (T-301):** the persisted on/off preference (default ON; every SCHEDULED tick gates on it; manual runs never gated; instant runtime toggling both directions) + the \"Planification automatique\" card. Suites at close: **desktop 129 files / 3020 tests / 0 failures + tsc + eslint 0 errors; Android 57 suites / 507 tests / 0 failures / 0 errors** (113 new tests this session: 29+11+28+16+15+11+13 desktop + 26+9+4+1 Android). Migration chain 0001–0085 committed (0085's LIVE application is the owner-gated residual). Zero-duplication held: every module revived/extended in place; the two NEW engines had no existing implementation (rg-verified at registration). **RESIDUALS (honest):** migration 0085's live application needs the sbp_ token; the conflict guard needs update-path producers passing basePayload (today's only enqueue site is the insert-heavy Excel import); the Android 3-way mirror; the unstaged trio in the offline mode.

> **45th session (2026-09-11) delta:** the 13-chart visual/mathematical/analytical parity (T-288..T-293, PARITY-003). See the 45th snapshot below.

# Current State — Project Snapshot (2026-09-11, FORTY-FIFTH session — the 13-chart visual/mathematical/analytical parity CLOSED: T-288..T-293)

## Current state snapshot (2026-09-11, 45th session CLOSE)

**The owner's 100% desktop→Android visual parity mandate is CLOSED (PARITY-003, TESTED):** every visualization in the owner's 13-item inventory now has a native Jetpack Compose implementation computed from the exact same canonical derivations as the desktop — the 5 missing engine families ported verbatim into `core/StatisticsEngine.kt` (weekly rhythm, collection heatmap, YoY + slicers, tranche waves, demographics), the repository/ViewModel wiring (DashboardKpi +10 engine-computed fields; the fixed-3-method parallel derivation RETIRED), 8 new Canvas chart primitives + the ElChartPalette (the desktop dashboard-theme hex mirror) + the 13 cards (the Analytique tab rebuilt as the desktop Analytics-tab twin: slicers → stat strip → trend explorer → mix cards → YoY + histogram → heatmap + aging composition → funnel + Pareto + demographics; the weekly rhythm in the Overview; the tranche-wave meters in the Financials Tranches tab; the capacity gauges in ClassDetailScreen). **The proof (T-292):** the `analytics_visuals` corpus category (3 scenarios, then-blocks generated by running the REAL desktop TS derivations — `scripts/generate_analytics_visuals_corpus.ts`), the `deriveAnalyticsVisuals` op in BOTH runners, and **`CrossPlatformEquivalenceTest.kt` — 3 scenarios × 5 derivation families, every value identical to the desktop engine output** (bins, levels, percentages, cumulative curves, null-delta semantics, wave flags, demographics slices). Suites at close: Android 54 files / 467 tests / 0 failures + lint 0 errors; desktop typecheck + eslint 0 errors + vitest 122/2897 + vite build green; full corpus 310 scenarios → 308 EQUIVALENT (the 2 discrepant = the documented pre-existing 023/025 null-vs-empty studentId). Live chain 0001–0084 untouched (zero migrations — the mandate was client-side parity). The push landed 2026-09-11 in the 45th-session continuation (the owner re-supplied the PAT): android 5c03a9e..181d060 + hub cf11cc7..df09226 pushed with a one-shot URL (never persisted) and API-verified on origin/main of both repos; pre-push re-verification of the exact pushed trees — Android testDebugUnitTest 467/0 (the equivalence + live-truth suites green), desktop typecheck + vitest 122/2897 + build green.

> **44th session (2026-09-11) delta:** the Android dashboard-parity REDONE at the core level + the three-leg equivalence proof (T-283..T-287, PARITY-002). See the 44th snapshot below.

# Current State — Project Snapshot (2026-09-11, FORTY-FOURTH session — the Android dashboard-parity REDONE at the core level + the three-leg equivalence proof: T-283..T-287)

## Current state snapshot (2026-09-11, 44th session CLOSE)

**The owner's mobile-parity redo mandate is CLOSED (PARITY-002, TESTED + the live leg VERIFIED):** the previous agent's Android dashboard patch (ea2441a/6f88b14) was audited, found shallow AND non-compiling (3 unresolved references on the pushed HEAD — the tree never built), and REDONE at the core level. **T-284:** a NEW `core/StatisticsEngine.kt` — the verbatim Kotlin mirror of the desktop's `analytics-derivations.ts` (source commit b6fbbcd in the file header, ADR-002) — now computes EVERY dashboard statistic (descriptive stats with SAMPLE σ, the 5 half-open bins, the full 11-category mix, the top-8 Pareto, the aging census, the funnel, the collection rate, the attendance rate) for BOTH the dashboard and the debt repositories; the five ledger-first/else-installment fallback sites are GONE (one canonical per-installment INV-4 derivation); every hard-coded desktop-number "fallback" in the UI (0.49f, the 26.9M/31.4M donut, 58 355 700 DZD, "54%", "6 fam. = 80%", the "0%"/"100%" funnel, the "Août" default, attendance 100.0) was DELETED — honest empty states; the PDF aging report and the debt screens now share the same derivation (the 91–180 j filter chips were unreachable before). **T-285 — the proof, three legs:** (a) the 24-test StatisticsEngineTest pins every derivation against the desktop's own fixtures, INCLUDING the two discovered traps — money-roundings must round at DZD granularity (`dzRound`), and the funnel consumes the census Σ of per-bucket family counts (379 = 196 + 183, families counted once PER bucket); (b) the new `deriveAnalyticsStats` corpus op runs in BOTH runners with a pinned `now` — **category `analytics_statistics` 3/3 EQUIVALENT, 0 discrepancies** (full corpus 303/307; the 4 non-equivalent scenarios proven pre-existing via a stash re-run on the unmodified runners); (c) the **LiveDatabaseEquivalenceTest PASSED LIVE** — the Android engine output equals the server-side SQL truth (canonical `scripts/verify_t-285.sql`) on EVERY value the owner quoted: 891 encaissements, 55 227 100 DZD, panier moyen 61 983, médiane 61 000, σ 44 951, best month Août, bins with 50k+ dominant (482), Scolarité 96% / Transport 4%, the 91–180 j / 180+ j census (196 + 183 = 379 families; 26.9M + 31.4M), outstanding 58 355 700, **collection rate 49%**, 24 classes. Evidence: `docs/recovery/t-285-live-verification.md`. Suites at close: **Android 53 files / 455 tests / 0 failures / 0 errors** (incl. the 24 new engine tests + the live test; compileDebugKotlin + lintDebug green; the JDK21/SDK35 toolchain re-provisioned per the android-env.sh recipe) + desktop typecheck 0 errors / eslint 0 errors / vitest 2896 passed with 1 unrelated timeout flake re-verified green in isolation; no migrations (the chain stays 0001–0084 — parity is a client-side derivation change); the desktop production code untouched (it is the reference the engine mirrors; the corpus op ADDS to the runner). Residuals: the 4 pre-existing corpus quirks (017/023/024/025) remain untouched; the academic-year selector scoping is a desktop-only surface (a year-scoped mobile analytics would be a new feature request); run_deploy.sh still lacks the pm-clear step the previous agent removed; the live test's snapshot sentinels (891/55.2M/49%) will legitimately move as the school collects.

> **43rd session (2026-09-10) delta:** the owner-pinned credential restored + the console triage (T-280..T-282, OPS-310/AI-312/OPS-311). See the 43rd snapshot below.

# Current State — Project Snapshot (2026-09-10, FORTY-THIRD session — the owner-pinned credential restored + the console triage: T-280..T-282)

## Current state snapshot (2026-09-10, 43rd session CLOSE)

**The owner's pasted production console is triaged and fixed (T-280..T-282), and the owner-pinned admin credential is restored and permanently pinned (OPS-310, VERIFIED LIVE):** the console's five Groq 429s were the AI-312 defect — ZERO retry anywhere on the BYOK path (free-tier per-minute limits + 27 tool schemas per request; an immediate-give-up client can never succeed) → **T-281** gave `executeOpenAIStream` transient retry (429/5xx, exponential backoff 800ms×2ⁿ capped 10s, `Retry-After` seconds+HTTP-date honoured, jitter, abort-aware, POST-only, the exhausted throw keeps the `HTTP <status>` shape), the runtime's persistent-429 now settles as a human French verdict, and the four AI-settings model fields became **SELECT dropdowns fed by the live-fetched list** (`ModelSelectField`: "hors liste" badge for out-of-list values, "Saisie manuelle…" escape hatch, the auto-discovery re-keyed on the SAVED config so the list loads on tab open — the old live-state effect never fired on a fresh mount). The intermittent audit 500 was OPS-311 (the OPS-309 residual, owner-reproduced; live-probed: 200 in ~1.1s, 161 small rows — transient server blip, not a query defect) → **T-282** added ONE 600ms-delayed retry on the three audit read paths (`log()` never retried). The early 400 is the DOCUMENTED P0: the 27-schema registry vs the deployed EF's 20-tool cap — the committed cap-40 fix needs the owner's sbp_ token deploy (runbook: t-277-live-verification.md; the client falls back to BYOK so the copilot still works). The VSync/Autofill/mime.cache/cookie lines are OS/browser noise. **OPS-310:** `admin@elimtiyaz.dz` / `elimtiyaz@admin2026` signs in again (live-probed; the old probe value dead), pinned by 4 layers (scripts' ADMIN_PW, credentials.md §1 directive, AGENTS.md §15 rule 23, next-task standing rule). Session-opening state: sandbox reset → hub re-cloned, HEAD `f908ed9` confirmed, push verified up-to-date. Suites at close: desktop **122 files / 2897 tests / 0 failures** (+22) + tsc + eslint 0 errors + vite build green; no EF, no migrations (the chain stays 0001–0084); website + Android untouched (desktop client-only changes). The P0 owner-gated residual is unchanged: the ai-proxy cap-40 deploy. Owner hygiene: the Groq key + GitHub PAT remain chat-transmitted (rotate at leisure); the admin password is the OWNER-PINNED value — agents must never rotate or re-set it.

> **42nd session (2026-09-10) delta:** the AI capability ecosystem (T-272..T-279, AI-311 — 27 tools, artifacts, statistics, workflows) + the EF cap-40 code + live matrix. See the 42nd snapshot below.

## Current state snapshot (2026-09-10, 42nd session CLOSE)

**The copilot is now a full AI-powered system, not a Q&A surface (AI-311, TESTED + live-matrix evidence):** the tool registry grew 12 → 27 across five suites, and every tool result can carry an ARTIFACT the user actually sees — a rendered SVG chart (7 types via a zero-dependency engine), a structural diagram (family/class, built from canonical entities), or a downloadable document (PDF statements/reports/payment-plans through the CANONICAL generators; XLSX/CSV exports with embedded rows through the SAME export engine the app's buttons use). The analysis suite (revenue trends with OLS slope + moving averages; descriptive statistics with Tukey outlier fences; anomaly detection across payments/attendance/grades; cross-class benchmarking; demographics) computes over CANONICAL streams only — statistical enrichment, never a parallel derivation of a business number (ADR-016 §3, the §15.5 boundary held). The workflow suite runs real operational programs: a prioritized collection campaign (amount×aging scoring, P1/P2/P3 tiers, per-debtor recommended actions), batch reminders as ONE human-validated card whose approval leg executes the canonical sendReminder PER parent with honest per-parent results, exact-sum payment plans over the canonical outstanding (printable PDF advisory — never a financial write), and composite intervention risk scoring (GPA×attendance×data gaps with drivers and concrete actions). The runtime's system prompt now teaches the capability map and the workflow discipline (search → analyze → visualize → propose), and MAX_TOOL_STEPS rose 5→8 for the composite chains. **The render path is gated:** every artifact — including charts the MODEL composes via render_chart — is schema-validated before it renders; a malformed artifact is dropped, never rendered (fail-open to "no visual"). **T-278 pins it all with a 51-test capability suite** (known-vector statistics, honest partial validation, the exact-sum plan, the 3 PDF generators + multi-page flow, RTL rendering, the FULL pipeline harness — a tool call rendered as a chart IN the drawer — and the batch approval execution leg). Suites at close: desktop **120 files / 2875 tests / 0 failures** (+51) + tsc + eslint 0 errors + vite build green; website + Android untouched (desktop + additive EF change, zero contract impact per §10); no migrations (the chain stays 0001–0084). **THE ONE OWNER-GATED STEP (P0, before edge-mode copilot use):** the ai-proxy tools cap was raised 20 → 40 in code (the registry sends 27 schemas; the cap must be ≥ the registry) but the DEPLOY needs the sbp_ token (not re-supplied this session; never persisted). The live matrix (t-277-live-verification.md) PROVES the dependency: the 27-schema probe → HTTP 400 invalid_tools ("at most 20 schemas") on the deployed EF, while the 20-schema probe → HTTP 200 + 77 SSE lines from Groq (the GROQ_API_KEY secret still live). Runbook: `SUPABASE_ACCESS_TOKEN=sbp_… supabase functions deploy ai-proxy --project-ref hkvkefubghbbotgnteir --no-verify-jwt` → re-run `scripts/t277-live-matrix.sh` → P2 flips 400→200. Until then: edge-mode AGENT-STREAM calls 400 (BYOK/mock modes + all single-shot features unaffected — they carry no `tools`). Owner hygiene (superseded 43rd session): rotate the chat-transmitted Groq key + GitHub PAT when convenient — the admin password is NO LONGER a rotation candidate: it is the OWNER-PINNED `elimtiyaz@admin2026` (restored + pinned T-280 — never re-set by agents).

> **41st session (2026-09-10) delta:** the production-quality AI integration (T-266..T-271, AI-310/AI-309/REG-005) + the LIVE agent-stream round-trip (EF v20, GROQ secret, the 2026 catalog repin). See the 41st snapshot below.

# Current State — Project Snapshot (2026-09-10, FORTY-FIRST session — the owner's production-quality AI mandate CLOSED: T-266..T-271)

## Current state snapshot (2026-09-10, 41st session CLOSE)

**The AI layer is now a production-quality integration (AI-310, TESTED + the server leg VERIFIED-LIVE), and the unregistered patch that degraded it was triaged per §15.14 (REG-005):** the session opened on an UNREGISTERED commit (6ce49b9, "chore: refresh smartexport selection-state file registry") that actually rewrote 14 AI files — deleted the production edgeLLMAdapter (server-held keys, EF rate limiting, audit logging), deleted the readRawStored encryption seam, broke 5 tests + 4 type errors, and stripped ~500 lines of docs, while ALSO carrying a sound but unregistered SuperAdmin AI-gate fix. T-266 triaged it: the good parts registered (AI-309), the damage repaired, the baseline returned to green before anything was built on it. Then the owner's mandate was delivered: **T-267** deepened the copilot from 6 generic tools to 12 production tools grounded in the canonical streams (overdue/debt ledger with aging buckets — the school's #1 business concern; collection analytics; payment history; attendance with drop-off risk; class performance with at-risk lists) + REAL proposal validation (parent exists, non-zero amount ≤ 5 000 000 DZD, reason ≥ 3 chars — structured errors the model must answer for, never a silent console.warn) + the CANONICAL send_reminder execution leg (repos.debt.sendReminder — notification + audit entry; AI-308 residual b closed) + the honest settle for unexecutable types. **T-268** completed the UX: the model's request_user_clarification questions now surface as an actionable card with framed answers, the stream is stoppable with the partial answer committed, and the conversation persists across restarts. **T-269** hardened + redeployed the ai-proxy EF (v20: .maybeSingle, agent-mode input validation) — **set GROQ_API_KEY live** (digest + behavior verified; never in any repo) — and ran the **first LIVE agent-stream round-trip** (9/9 probes GREEN: the authenticated SSE round-trip with real French content, the tool-call passthrough — search_entities({"query":"Benali"}) reassembled on the wire — and the single-shot narrative path with real tokens/latency; t-269-live-verification.md). **The 2026 Groq catalog discovery:** every llama/qwen model id 404s with the owner's key (removed from the catalog) → all defaults repinned to openai/gpt-oss-120b / openai/gpt-oss-20b (LIVE-reachable; the 39th/40th defaults would have failed on every call); the gpt-oss reasoning-channel behavior (delta.reasoning + channel:"analysis", reasoning shares the max_tokens budget) documented. **T-270** pinned it all with a 31-test deep-integration suite (tools, validation, clarification/abort/persistence, execution legs, source guards — the REG-005-class net). **ADR-015** records the copilot data policy (grounded tool results, BYOK/EF transports, human-gated writes, honest settle, LIVE-reachable defaults). Migration tokens (the owner's explicit ask): chain **81/81 = 0001–0084 ZERO DRIFT at open AND close**; EF fleet 14/14 ACTIVE; the supplied tokens verified live (sbp_/service-role/anon). Suites at close: desktop **119 files / 2824 tests / 0 failures** (+31) + tsc + eslint 0 errors + vite build green; website + Android untouched (desktop + additive EF change, zero contract impact per §10); no migrations (deliberately DB-free session). **Owner hygiene:** the admin password was re-reset to the documented value for the authenticated probes (rotate again at leisure); the Groq key + GitHub PAT were chat-transmitted (rotate when convenient — the Groq key lives ONLY as the live EF secret).

> **40th session (2026-09-10) delta:** the approval-queue 406 console-noise fix (T-264..T-265, OPS-309). See the 40th snapshot below.

# Current State — Project Snapshot (2026-09-10, FORTIETH session — the owner's 406 console-noise fix CLOSED: T-264..T-265)

## Current state snapshot (2026-09-10, 40th session CLOSE)

**The approval-queue console noise is gone (OPS-309, TESTED):** the Settings → Approvals matcher (`SupabaseApprovalRepository.findPotentialMatches`) no longer fires `.single()` on lookups where zero rows is the common case — all five call sites (activation_codes by code; parents by id / email / national_id / phone) now use `.maybeSingle()`, so a no-match parent lookup is a silent HTTP 200 instead of a red PostgREST 406 PGRST116 in the production console (~15 per refresh at the 39th-session launch). Enrichment semantics are byte-identical (error-or-null both fell through before; null-with-no-error falls through now; the >1-row case still errors and still falls through). A 7-test regression suite pins the contract (zero-row → no error + `parent_match: null`; the activation-code enrichment path; the >1-row fall-through; the email fallback; the empty-queue zero-lookup case; source guards). **The owner-supplied Groq BYOK key could not be verified from the agent sandbox** (egress Hong Kong → Groq geo-block → 403 Forbidden on every endpoint; NOT evidence the key is invalid — the desktop's Settings → AI tab live model discovery validates it in one click from the owner's machine in Algeria). The key was never persisted anywhere in the repos (§15.12). The EF server-side GROQ_API_KEY secret remains owner-gated (sbp_ token or dashboard manual set). Suites at close: desktop **117 files / 2789 tests / 0 failures** (+7: NEW t-264-approval-406-noise.test.ts) + tsc clean + eslint 0 errors (424 warnings, unchanged) + vite build green (19.85 s); website + Android untouched (desktop-scope, zero contract impact per §10); no migrations (the live chain 0001–0084 and the 39th session's 7-probe live evidence stand). **Owner hygiene:** rotate the two chat-transmitted credentials when convenient (the GitHub PAT — a standing residual — and the Groq key after confirming it works at console.groq.com). The ONE-OFF 500 on an audit-log list query at the 39th-launch startup did not reproduce and is recorded as an observation (chase only if it recurs).

> **39th session (2026-09-10) delta:** the Agentic AI Architecture (T-260..T-263, AI-308 — streaming + tool-calling copilot with BYOK live model discovery, human-in-the-loop proposals, Ctrl+J everywhere; ai-proxy EF agent-stream mode live v17). See the 39th snapshot below.

# Current State — Project Snapshot (2026-09-10, THIRTY-NINTH session — the owner's Agentic AI Architecture mandate CLOSED: T-260..T-263)

## Current state snapshot (2026-09-10, 39th session CLOSE)

**El-Imtiyaz desktop now has a production-ready Agentic AI layer (AI-308, TESTED + the EF leg live-verified):** a model-agnostic, autonomous copilot — Groq (default `qwen/qwen3.8-27b`, fallback `llama-3.3-70b-versatile`), OpenRouter, or ANY OpenAI-compatible endpoint (Ollama/LM Studio via customBaseUrl) — with native SSE streaming, OpenAI-style function calling, and BYOK keys encrypted at rest (AES-256-GCM, 3 keys). The settings tab live-discovers models from the provider's `/models` endpoint (zero hardcoded locks) with a streaming inference test + latency badge. Six domain tools ground the assistant in REAL data: search_entities, get_financial_ledger_summary (computeParentSummary), get_student_academic_profile (evaluateStudentTermPerformance), get_school_kpi_overview, propose_account_adjustment (a human-in-the-loop ActionProposal — the AI NEVER writes; the ONE sanctioned write path is the provider's approveAction → the canonical repos.payments.adjust), request_user_clarification. The universal copilot is reachable everywhere via Ctrl+J/Cmd+J, the topbar Assistant IA button (UseAI-gated), or the command palette's Enter-to-ask. The agent runtime loops up to 5 tool rounds and feeds results back as role:"tool" messages. The ai-proxy Edge Function gained the agent-stream mode (messages+tools+model override piped through, SSE relayed verbatim; auth/rate-limit/audit machinery preserved) — deployed live v17 with the 7-probe matrix GREEN (t-262-live-verification.md); chain 0001–0084 re-verified ZERO DRIFT. Blueprint adaptations (all registered): provider INSIDE ToastProvider (mount-order bug in the blueprint), additive topbar integration (the blueprint's wholesale topbar replacement would have deleted the palette/alerts/tenant/language/profile features — §15.3), RBAC gates preserved on the settings tab (§15.4), tools propose-not-write (§15.5), canonical display-name renderers (DATA-005). Suites at close: desktop **116 files / 2782 / 0** (+37: NEW agent-architecture.test.ts [29] + copilot-drawer.test.tsx [8]) + tsc + eslint 0 err (424 warn) + vite build green (21.1 s); website + Android untouched (desktop-scope + an additive, backward-compatible EF change — zero contract impact per §10). Owner-gated residuals unchanged (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp API, financial-RPC ratification ADR, admin password rotation, GitHub PAT rotation after chat transmission) + the AI-308 residuals (live authenticated agent-stream round-trip through the EF needs a staff JWT + server-side provider key; non-financial proposal dispatch; conversation persistence; abort-button wiring; Android/website copilot equivalents).

> **38th session (2026-09-09) delta:** the Power BI-style ANALYTICS report page (T-255..T-259, UI-307 — 9 visual families + cross-filtering slicers, all REAL data). See the 38th snapshot below.

# Current State — Project Snapshot (2026-09-09, THIRTY-EIGHTH session — the owner's Power BI-style visualizations mandate CLOSED: T-255..T-259)

## Current state snapshot (2026-09-09, 38th session CLOSE)

**The dashboard now has a dedicated Power BI-style ANALYTICS report page (UI-307, TESTED):** the new "Analytique" tab delivers 9 visual families — the interactive statistics strip (count / encaissé total / panier moyen / médiane / meilleur mois / volatilité σ), the hero trend explorer (monthly bars-or-area + cumulative line + 3-month moving average + view toggle + per-series show/hide chips), the method donut with the REAL center total, the category ranking with a [Montant | Nb opérations] metric toggle, the weekday×month collection heatmap (Dim→Jeu school week, 5-level intensity, row/column totals), the payment-amount histogram, the top-debtors Pareto with the cumulative % line and the "N familles = 80%" verdict, the aging 100% composition bar + statistical table, and the like-for-like year-over-year comparison (the page loads the previous academic year's series for the same month window). The Power BI interactivity is real: method + category slicer chips CROSS-FILTER every payments-derived card (stat strip, mixes, heatmap, histogram, and a dashed "Filtré" overlay on the trend chart) with a live opérations/total badge and reset — while the canonical repository bars stay untouched. Every figure derives from the REAL repository contracts (§15.16 discipline held: the encaissé definition is the repository's own status=paid + calendar-month-of-collectedAt; deltas are NULL rather than fabricated where the previous window is empty; 7 honest empty states). The T-088 single-fetch pipeline is preserved (the tab never fetches; the page adds exactly one aggregate call for the previous-year series). Zero migrations — the live chain 0001–0084 and the 37th session's 18/18 live evidence stand unchanged. Suites at close: desktop **114 files / 2745 / 0** (+34: NEW analytics-visuals.test.tsx) + tsc + eslint 0 err (410 warn) + vite build green; website + Android untouched (desktop-scope, zero contract impact). Session note: a full container reset wiped the workspace before the session — the 3 repos were re-cloned from the confirmed origin HEADs (nothing lost; the pristine baseline 113/2711/0 was re-proven before any change). Owner-gated residuals unchanged (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp API, financial-RPC ratification ADR, admin password rotation, GitHub PAT rotation after chat transmission) + the analytics residuals (revenueByCategory / revenueByCycle backend extensions; KPI sparklines beyond revenue need repository history).

> **37th session (2026-09-09) delta:** the AI-review completion (T-246..T-254) — the Part-2 design system + the review's remaining five screens, all REAL-data derivations; live token round 18/18 GREEN; push unblocked same-day with the re-supplied PAT (origin/main = 2ad3b17). See the 37th snapshot below.

# Current State — Project Snapshot (2026-09-09, THIRTY-SEVENTH session — the owner's AI-review completion mandate CLOSED: T-246..T-254)

## Current state snapshot (2026-09-09, 37th session CLOSE)

**The AI-review is now FULLY applied (UI-306 + its continuation, both TESTED):** the 36th session delivered the dashboard Overview 3-zone layout (T-243); this session delivered everything else the review specified — the Part-2 design system (the midnight canvas #0b0d14/#111523/#181d30 + crisper status hues + --brand-violet/--brand-coral + the .glow-border micro-rim, with the index.css↔tailwind.config.cjs↔TS-fallback PARITY CONTRACT documented), the drill-down SeeDetailsModal (collection-rate trio + the "Encaissements vs échéancier théorique" chart — REAL bars + the derived 40/30/30 planning line over encaissé+créances — + the dual-ring donut with the REAL center total + per-bucket severity badges), the Tranche Wave header on the installment schedule (T1/T2/T3 collection health from canonical label grouping), the payment modal's quick-pay chips + cashier change-return calculator, the 14-level tuition MATRIX replacing the 14-card scroll-wall (with the EXPLICIT 40/30/30 tool — never auto-applied, the official grid is non-equal), the academic-year readiness checklist + full 14-level grid, and the parent-drawer coverage stack + per-tranche 1-click collect (real row → exact installment targeting; synthetic → consolidated preset, never a phantom target). Every number derives from the REAL repository contracts (§15.16 discipline held throughout — the review's synthesized figures were re-derived or omitted and documented). **Live backend re-verified same-session (T-253, 18/18 GREEN):** chain 0001–0084 ZERO drift (live=81/local=81), EF fleet 14/14, dual-key health, RLS anon 0-rows ×5, anonymous EF 401 ×2, ALLOWED_ORIGINS canonical echo + reject, committed-key consistency. Suites at close: desktop 113 files / 2711 / 0 (+17: NEW ai-review-screens.test.tsx) + tsc + eslint 0 err + vite build; website untouched-green 40/544/0 + lint + strict build; Android untouched (desktop-scope, zero contract impact). Owner-gated residuals unchanged (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp API, financial-RPC ratification ADR, admin password rotation) + the UI-306 residuals (category pills need backend revenueByCycle; Departments needs revenueByCategory; admissions pipeline).

> **36th session (2026-09-09) delta:** the dashboard Overview became the review's 3-zone layout (T-243/UI-306) + the live token round 18/18 (T-244) + push (T-245). See the 36th snapshot below.

# Current State — Project Snapshot (2026-09-09, THIRTY-SIXTH session — the owner's AI-review dashboard integration mandate CLOSED: T-243..T-245)

## Current state snapshot (2026-09-09, 36th session CLOSE)

**The dashboard Overview is now the owner's AI-review 3-zone layout, adapted to REAL data (UI-306, TESTED):** Zone A (12-col grid, 8 wide) carries the 4 sparkline KPI cards (trend/delta rendered ONLY from a real series — today only the revenue card qualifies), the hero revenue spline (REAL monthly series; the T-088 dedup supersession is documented — trend view here, detail view stays in the drill-down), the recovery funnel (REAL debtAging family counts: En retard → ≤60j → 61–90j → >90j — the review's admissions numbers were NOT rendered; no repository contract carries demande/dossier counts), the weekly operating rhythm (REAL canonical Payment[] stream — the page's ONE new subscription; Dim→Jeu Algerian school week; refunded excluded; range-filtered) and the DashboardCalendar (kept, Row 4). Zone B (4 wide) is the InsightsRail: the AI decision card derives from the REAL overdue census (no fabricated N-1 projection or tranche deadline), the radial gauge shows the REAL collection rate encaissé/(encaissé+créances), and the "À relancer" feed lists the top-3 REAL most-overdue families (relocated from the old Top Débiteurs card). NEW `dashboard-theme.ts` is the single Recharts styling source. The topbar Cmd+K placeholder hints NL queries. **Live backend re-verified untouched-consistent (T-244, 18/18):** chain 0001–0084 ZERO drift (live=81/local=81), EF fleet 14/14 ACTIVE 1:1 with the hub source, dual-key auth health (ADR-009), RLS anon 0-rows on the 5 core tables, anonymous EF → 401, ALLOWED_ORIGINS canonical 4-origin set echoing + non-allowlisted rejected, committed keys byte-identical to the owner's re-supplied sheet. Suites at close: desktop 112 files / 2694 / 0 (+7 net) + tsc + eslint 0 err (399 warn, net −14) + vite build; website untouched-green 40/544/0 + lint + strict build; Android untouched (desktop-scope UI change, zero contract impact per §10). Owner-gated residuals unchanged (RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp API, financial-RPC ratification ADR, admin password rotation).


> **35th session (2026-09-07) delta:** the Personnel/Workforce RBAC boundary enforced on EVERY layer (teachers lose module-entry permissions but keep action permissions inside their in-Personnel workspaces on desktop AND Android; clerks gain the front-office trio; route guards close the URL bypass); migration 0083 applied live atomically (teachers can never UPDATE student profiles; students/assessments SELECT scoped to classes-they-teach; parents SELECT dropped); ARCH-001 slots #7/#8/#9 closed (purchaseRequests/deliveries/inventory persist on the canonical 0011 tables + migration 0084); account provisioning PROVEN live 14/14 (T-241 evidence doc). Suites: desktop 112/2687/0, Android 50/427/0. Chain 0001–0084 zero drift.

# Current State — Project Snapshot (2026-09-07, THIRTY-FIFTH session — the owner's Personnel/Workforce RBAC overhaul mandate CLOSED: T-233..T-242)

## Current state snapshot (2026-09-07, 35th session CLOSE)

**The Personnel/Workforce RBAC boundary is now enforced strictly on EVERY layer** (the owner's core mandate): the client matrices on desktop (permissions.ts, T-234) and Android (Rbac.kt, T-237) are in lockstep — teachers lose the CRM/Pédagogie module-ENTRY permissions (sidebar tabs + Android hub tabs disappear) while keeping the ACTION permissions exercised strictly inside their in-Personnel workspaces (desktop: RollCallScreen/GradeEntryScreen composed as overlays in the TeacherDashboard, T-235; Android: the "Mon espace" first tab in PersonnelHubScreen with homeroom-scoped class cards, T-237); clerks gained the front-office trio (ViewFinancials/ManageClasses/PromoteStudent); route guards on /crm, /academics*, /financials close the URL bypass. The DATA layer is stricter still: migration 0083 (applied live atomically) removed teachers from students_update (DB-level student-profile writes CLOSED), scoped students_select/assessments_select to classes-they-teach and dropped them from parents_select. The operational dashboards now persist: purchaseRequests/deliveries/inventory run on the canonical 0011 tables (T-238/239/240 + migration 0084 for the frozen display/audit columns) — the Buyer/Driver/Warehouse pipelines survive restart. Account provisioning is PROVEN live (T-241: super_admin → EF → teacher+driver accounts → immediate sign-ins → active profiles + roles + audit; escalation 403; teacher scoping probes 0/0/0 with a succeeding admin control). Final evidence: docs/recovery/t-241-live-verification.md. Migrations 0001–0084 zero drift; EF fleet 14/14 ACTIVE. Suites: desktop 112 files / 2687 tests / 0 + tsc + lint; Android 50 suites / 427 tests / 0. Owner-gated residuals: RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, WhatsApp API, financial-RPC ratification, + rotate the admin password (re-set during T-241's probe round).


> **34th session (2026-09-07) delta:** the DAG automation system COMPLETE and live-verified end-to-end (the owner's "fully solve the DAG" mandate) — T-223..T-232 closed the entire pipeline from the visual builder through persistence and execution: the server-side publish gate (0081/0082 — an invalid/cyclic DAG can never be published through ANY writer), the pure branch-aware server engine (27 desktop tests incl. dry-run equivalence), the workflow-execute EF v27 rewrite on the REAL schema contract, real action executors (0077 RPC notifications, tasks, account restriction, honest wa.me), real execution context (compute_parent_summary; server-wins-over-client), persistent delays + the workflow-resume-scheduler EF, EF dry-run with prediction parity, the desktop Exécuter/Test serveur wiring, and the Android run-contract fix (node_results decode). Final e2e: t-232-live-verification.md 18/18. Suites: desktop 105/2578/0, Android 49/416/0. Registry: DAG-100 → VERIFIED, DAG-101 → RESOLVED.



> **33rd session (2026-09-07) delta:** the owner's mandate COMPLETE ("fix the payment modal that gets cut off / goes outside the form boundaries — wider ≈16:9, responsive; show who issued each payment and when in the payments section instead of only a serial number; fully fully fully do the DAG automations"). Desktop financials: UI-305 closed at the design-system level — UnifiedModal dialogs are now height-capped flex columns (`max-h-[88vh]`, footer always reachable; the old grid layout made the body's flex-1 inert) + a NEW `2xl` wide-form tier (max-w-6xl ≈ 16:9 stage) + the payment modal restructured into a responsive 12-col two-column form (T-219); the payments journal now shows the ISSUER (parent avatar/name/family code/student), collector attribution, and the EXACT dd/MM/yyyy HH:mm timestamp (T-220). Automations: DAG-100 closed — the workflow builder grew from a visual skeleton to a functional system (T-221): 29 node subtypes (was 17) incl. the educational trigger/action set; the NodeInspectorDrawer with the visual predicate builder (conditions are finally AUTHORABLE, not just evaluable); a PURE branch-aware dry-run engine driving BOTH the canvas "Tester" visualisation AND the mock executor (single source of truth — the linear single-flag executor is gone); zoom/pan/minimap/snap-to-grid/auto-layout on the canvas; 3 one-click educational templates in the Nouveau picker. Suite 100 files / 2513 / 0 (+74 tests) + typecheck + lint 0 err. Registry: +UI-305 +DAG-100 (201 entries, 155 TESTED); T-218's missed registry flip truth-synced. **Open follow-up (the 34th session's first pick):** port the branch-aware execution semantics into the `workflow-execute` Supabase Edge Function (the server-side engine still diverges from the dry-run's predictions) + implement the real action executors (notifications/tasks/WhatsApp URL) behind the action nodes. Live chain untouched: 77/77 = 0001–0080 zero drift.

---


## (previous snapshot) Project Snapshot (2026-09-07, THIRTY-SECOND session — the owner's portal-detail mandate CLOSED: T-209..T-218 [a balanced 10-task set])

> **32nd session (2026-09-07) delta:** the owner's portal-detail mandate COMPLETE ("the web client portal lacks detailed information about the parents' children, the parents' personal details, and the children's enrollments; use the full labels Trimester 1/2/3 instead of T1/T2/T3; apply the migration tokens; consistent everywhere"). Portal: the Profile view now renders the parent's identity rows (T-209) + per-child identity/enrollment cards (T-210) + the per-child ENROLLMENTS surface — service_enrollments (first consumer of the previously-dead useServiceEnrollments hook) + the REAL per-student installment fee schedule + academic-year context (T-211) — and the dashboard child cards carry level·class·status (T-213); the academic view's T1/T2/T3 abbreviations are gone, full localized "Trimestre 1/2/3" everywhere incl. the t-202 mobile pattern for the wider labels (T-212; bulletin already full; the canonical AcademicTerm DATA union untouched). Backend: **migration 0080** (T-214) tightened `service_enrollments_select` from tenant-wide SELECT (INFO-300 — any parent could read every family's enrollment amounts) to staff/parent-own/student-self scoping, applied live atomically with registration — **chain 77/77 = 0001–0080 zero drift**; NEW Management-API quirk #9 persisted (doubled-single-quote LIKE corruption → position()/DO blocks). Tooling: the policy-census script (T-215, the 31st-session recommendation) machine-checks live-vs-chain policies — **189/189 zero drift**; the closing MIG-TOKENS round (T-216) ALL GREEN 10/10 (dual-key health ADR-009, EF fleet 13/13 ACTIVE 1:1, ALLOWED_ORIGINS canonical set echoing, anonymous EF 401). Desktop: the T-047 dashboards trio completed — workforceAttendance ported onto workforce_attendance_events (T-217; latestFor promoted into the domain interface; the three-same-named-AttendanceRepository trap documented). Suites at close: website **40 files / 544 / 0** + lint + strict build; desktop **94 files / 2439 / 0** + typecheck + lint 0 err; Android unchanged (zero commits). All three repos pushed + zipped for the owner.

> **31st session (2026-09-06) delta:** the owner's mobile-UI mandate COMPLETE ("fix the website UI — elements overflowing their containers, mobile-first but desktop-safe — fix the underlying problems, not just patches; verify the migration tokens are applied and consistent everywhere"). The portal's layout was live-measured at real phone widths for the first time (seeded UI-TEST family + @supabase/ssr cookie sign-in + DOM-geometry probes): FIVE new defect families registered and fixed same-session (UI-300 dashboard grid blowout — 935px horizontal overflow at 320px; UI-301 unbreakable Intl currency in KPI cards; UI-302 four non-wrapping header rows; UI-303 tab-label clipping; UI-304 raw English kind enums) — all with source-scan regression guards (website suite 30/496 → **35 files / 514 / 0**, lint clean, strict build green). Desktop parity shipped per §10 (18 latent bare responsive grids + the debt KPI value; desktop suite 90/2410 → **92/2422/0**, typecheck, lint 0 err); Android scanned — structurally immune to the family, unchanged. **Viewport matrix 36/36 GREEN** (9 views × 320/375/768/1280, all 0px overflow; sub-tabs + conversation clean). The T-204 live round re-proved full-stack consistency: chain **76/76 = 0001–0079 zero drift**, auth+REST 200 with BOTH key formats (ADR-009), EF fleet **13/13 ACTIVE = the hub's 13 function dirs** (the "14/14" figure in older notes was stale), ALLOWED_ORIGINS canonical set all echoing (ACT-203 intact), anonymous EF denied. UI-TEST family cleaned (0 residual rows). Harness recipe + CSS rules persisted (strategy §6, AGENTS.md §15 #21/#22). Owner residuals unchanged (Firebase service account, Resend key, web-push env vars, AUTH-200 client).

# Current State — Project Snapshot (2026-09-05, TWENTY-SEVENTH session — the owner-reported sync+notifications complaint CLOSED: T-171 desktop sync-queue recovery (SYNC-200: retry/discard + mock-era tenant re-scope + persisted lastSyncAt + honest toasts), T-172 overdue-alert lifecycle deployed live (NOTIF-200: 267 stale alerts resolved, active 958→691, all remaining alerts truthful; ADR-013; Android pull parity); all suites green: desktop 2336/0, website 476/0, Android 397/0; ONE owner action left: click "Supprimer les échecs" once)
> **29th session (2026-09-05) delta:** the owner re-reported "it's still [not working]" minutes after REDEPLOYING the T-184 fix — the fresh console paste proved ACT-201 fixed LIVE (the URL now resolves correctly) and exposed the SECOND layer: the production preflight fails the CORS access-control check because the deployed `ALLOWED_ORIGINS` Edge-Function secret allowlists only `http://localhost:5173` (live-probed: OPTIONS with the production origin returns 200 with ACAO `http://localhost:5173`; the EF CODE is correct — `_shared/cors.ts` echoes any allowlisted origin). Registered as **ACT-203 (Critical, owner sbp_-token-gated)** with the one-command remediation committed: `elimtiyaz-desktop/scripts/update_allowed_origins.sh` (idempotent merge-only + live preflight verification; instant effect, NO redeploy). The desktop paste exposed **AUTH-301** (Critical, TESTED — T-185: `refreshSession` "rebuilt" via `signIn(email, "")`, an empty-password grant that 400'd on EVERY refresh → the user was logged out at every session expiry; now both paths share `buildSession`, no second grant — 6/6 new suite) and **SEC-114** (Low, TESTED — T-186: `frame-ancestors` removed from the meta CSP — the spec ignores it there and Chromium warned on every launch; guard inverted). The website got **ACT-204** (T-187: fetch-level activation failures now show a dedicated "Impossible de joindre le serveur" message instead of the code-blaming generic one; 5/5 new suite). Lint baseline repaired (one pre-existing T-179 `no-this-alias` error). Suites: desktop **90 files / 2410 / 0** (+6), website **29 files / 488 / 0** (+5), strict build green. All repos pushed; zips regenerated. **Owner actions: (1) THE activation unblock — supply a fresh sbp_ access token and run `SUPABASE_ACCESS_TOKEN=… bash elimtiyaz-desktop/scripts/update_allowed_origins.sh` (or dashboard: Edge Functions secrets → ALLOWED_ORIGINS → append `https://elimtiyaz-website.vercel.app`); (2) 0071–0074 migration applies (same token); (3) the standing residuals unchanged (T-173 part a, AUTH-200 first sign-in, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, web-push Firebase env vars — optional).**

> **28th session (2026-09-05) delta:** the owner's URGENT mid-session report ("the activation code is not working — fix both the desktop app and the website, then push") was delivered as **T-184** AHEAD of the planned batch: the production portal had been requesting `/undefined/functions/v1/bind-activation-code` (404 on the portal's own origin) on EVERY activation attempt — `activation-code-screen.tsx` read the build-time env var DIRECTLY in a client component, and the Vercel project sets NO `NEXT_PUBLIC_*` vars (the same console paste's FCM warning proved it), so Next.js inlined `undefined`; the live EF was healthy the whole time (anon probes: structured 401 + OPTIONS 200). The screen now resolves through `@/lib/env` (T-096 committed fallback chain — a fresh clone needs no env vars), a whole-src regression scan enforces the rule, and the desktop's `bindActivationCode` surfaces the EF's structured errors (ACT-202). URL-routing live-verified; **the owner must REDEPLOY the portal on Vercel**. The T-047 port batch then landed: workflows+workflowRuns (T-176/T-177, migration 0071, the canonical execute EF path), leaveRequests (T-178, migration 0072 — the RequestType-widened CHECK + reviewed_by_name), suppliers (T-179, migration 0073 — category + fractional rating), tasks (T-180, migration 0074 — display names) — **five of the T-160 scoping's top-5 ports done; the desktop suite 2404/0; the migration chain 0001–0074 (0071–0074 live application owner-token-gated — apply_00XX_live.sh ready)**. Android: T-181 closed T-173 part b (Room v13→v14 dismissedAt + the server-dismissed pull eviction; 410/0). All three repos pushed to GitHub with the owner's PAT. **Owner actions: (1) REDEPLOY the portal on Vercel (activation fix); (2) re-apply the migration tokens when a fresh sbp_ access token is supplied (0071–0074: `SUPABASE_ACCESS_TOKEN=… bash scripts/apply_00XX_live.sh`); (3) the standing residuals (T-173 part a alert VOLUME decision, AUTH-200 first sign-in, RESEND_API_KEY, FIREBASE_SERVICE_ACCOUNT_JSON, the "Supprimer les échecs" click, the web-push Firebase env vars).**

> **27th session (2026-09-05) delta:** the owner reported with a screenshot — "I don't think the syncing is working / why are there 1,000 notifications / is this because I synced three Excel spreadsheets?" Live-DB forensics: the red 1 170 badge was the SyncIndicator's failed-entry count — 1 170 = 390 students × 3 mock-era Excel imports (03/08/2026, pre-Supabase-wiring), terminal-failed after 5 attempts with NO recovery path, while the server held exactly the 3 544 synced rows and ZERO failure audit rows (the pre-push sync_queue upsert failed RLS on the baked-in placeholder tenant "default"); the data itself was never at risk (390 students / 260 parents / 0 duplicate codes). T-171 shipped retryFailed/discardFailed (17-test suite), the drain-time placeholder re-scope + foreign-tenant local skip, the persisted lastSyncAt, honest toasts and the SyncTab/SyncIndicator recovery actions; t-022's source-scan updated for the entryToPush rename. T-172 shipped the alert lifecycle in BOTH the run-overdue-scan EF (redeployed live; alerts_resolved=267 on the verification run; auth matrix 401/401/401/200) and the desktop generator (13/13), plus the Android dismissed_at pull filter (17/17); ADR-013 records the lifecycle decision (active-only dedup + tracked-set resolution; user dismissal becomes temporary while the condition holds); the alert VOLUME question (691 truthful alerts) is deliberately deferred to T-173/UNKNOWN-020 as a product decision. New problems registered: SYNC-200, NOTIF-200. **The Google OAuth provider (AUTH-200) remains the #1 owner-only blocker; second: the one-click "Supprimer les échecs" purge of the 1 170 mock-era queue residue.**
> **26th session (2026-09-05) delta:** the owner's AI-generated patch (unregistered, junk-named commits, HEAD 8644719) was fully reviewed, its FALSE backend diagnosis disproven by live read-only audit (1 276 installment rows exist for 259/259 charged parents — the "Aucune tranche" symptom was `SupabaseDebtRepository.refreshProfile` hardcoding `installments: []` AND selecting only 6 ledger columns, stripping description/actor/student attribution = DATA-013), and its 8 test regressions + weakened CSP repaired. The patch's UX (itemized "Prestations facturées", Par Enfant/Par Service toggle, adjustment badges, academic-year context) was preserved and re-derived from a NEW canonical module `domain/calc/payment/billing-breakdown.ts` (real rows authoritative; 40/30/30 display synthesis only fills gaps, flagged) — ported read-side to the website (`Facturation` tab, 5th) and mirrored to Android (`core/BillingBreakdown.kt` + ParentDetailScreen card), with identical test vectors on all three platforms (285 000/125 000 → T1 paid, 74 500+85 500 = 160 000). Migration 0069 (adjustment description CHECK) applied live: convalidated=true, 0 violations. The drawer + payment modal now share the same canonical installment stream. DATA-013/DATA-014/REG-003 registered; change-log + registries updated. **The Google OAuth provider (AUTH-200) remains the #1 owner-only blocker.**
> **25th session (2026-09-04) delta:** run as one of ~10 CONCURRENT agents — coordination-first selection (the contested T-044 screen migrations + MainScreen route-model rewrite + legacy-tree deletion were deliberately NOT taken; only the additive DS prerequisite was). **The DebtMeter's "Crédit parent" row is live-wired** (T-157: repos.debt.observeParentProfile → displayParentCredit → DebtMeter.unallocatedCredit per ADR-010). **The desktop lint baseline is re-pinned at 379 with exhaustive-deps at ZERO** (T-158; the baseline had silently drifted 307→384 across ~20 sessions — per-session lint-delta recording is now the documented discipline). **The Android toolchain recipe is self-contained again** (T-159: android-env.sh re-created; NEW discovery: secrets-gradle-plugin 2.0.1 resolves .env against the ROOT project only — app/.env is never read; empty .env.example keys become blank literals that fail compilation). **DS ElScrollableTabRow exists** (T-044 pass 3a — the 6-tab FinancialsHub prerequisite; 5 semantic tests; suite 45/377/0; pitfalls persisted: semantics `selected` val-collision, LazyRow lazy composition vs ui-test). **T-047's scoping is DONE** (T-160: 23 of 45 desktop slots mock-backed; 19 have canonical tables already → adapter-only; teachers = modeling ADR; clubs/psychology/orthophonie = product decision; VERIFIED cross-platform drift: website reads `calendar_events` while desktop's calendar is mock; Android pull-syncs `workflow_runs` while desktop's workflows are mock). Registry truth-synced (the stale T-104 Ready row, T-020/T-082 tombstone, CROSS-004/REG-002 headers). Suites at close: desktop 82/2286+5s + typecheck + lint 0e/379w; Android 45/377/0 + lintDebug; website 26/457 + lint + strict build. Branch `session25-agent-work` in all three repos; zip-for-push handoff.
> **24th session (2026-09-03) delta:** **the portal's account ACTIVATION works end-to-end now** (ACT-200 closed: codes persist [tenant_id fix], the phantom fallback is dead, the ONE canonical EF activates pending users [ADR-011 — UNKNOWN-001 resolved], the screen shows precise localized errors; live round-trip 19/19 with a real pending user). **The messenger now connects parents to the ADMINISTRATOR only** (ADR-012 amends ADR-008: migration 0067's open_parent_admin_channel RPC + tightened chat RLS make parent↔parent structurally impossible; the website gained "Contacter l'administration"; live 14/14). **Parents no longer display their first child's name** (migration 0068: 259 parents repaired to "Famille {lastName} — phone"; students untouched, checksum-proven; live 11/11). UNKNOWN-001 resolved; CROSS-004/CROSS-009 closed (T-028's consolidation done). Registry: OPEN 7→6, TESTED 138→143 (+ACT-200/CHAT-200/DATA-012). Chain 65/65; EF fleet 14/14. **Owner residuals: ONE desktop click — issue fresh activation codes (YOUCEFI AYA / ABADA YAHIA's parents received codes that were never persisted); one real parent's "Contacter l'administration" click; the AUTH-200 first Google sign-in; RESEND_API_KEY; FIREBASE_SERVICE_ACCOUNT_JSON.**
> **22nd session (2026-09-03) delta:** the chain grew to **63 = 0001–0066** (0066 = the parent first_name backfill the OWNER applied live during the session — ARCH-014; T-139 reconstructed + committed it and live-verified 6/6). The workflow `send_email` action is REAL now (shared `_shared/send-email.ts` Resend integration; honest outcomes; the approve email links the production portal — both EFs deployed live). PARENT-102's limbo is closed (EF 400 + audit). The desktop parent-name render sites are canonical (18 sites → parentDisplayName; PDFs included). All three platform suites green at closeout: desktop 79/2271, website 24/440 (+ strict build + live render 200), Android debug 44/372 + release 42/367 + lint. Registry: OPEN 62→12 (T-133's truth-sync), NOTIF-101 flipped with live evidence, ARCH-011/WEAK-030 duplicates removed, next-task.md corruption repaired. **Owner residuals: RESEND_API_KEY (+ from-domain verification) for real emails; FIREBASE_SERVICE_ACCOUNT_JSON for real pushes; the Google OAuth client (AUTH-200 runbook step 1).**

> Answers: "What is the state of the project RIGHT NOW?" Update this file whenever the recovery state materially changes. CURRENT facts only; targets live in `docs/architecture/system-map.md` §5 and in ADRs marked Proposed.

## 1. Architecture status

- Three repositories operate against **one Supabase backend** whose canonical schema is the desktop repo's migration chain **0001–0068** (ADR-001). Migrations 0001–0068 are committed AND applied to the live project AND registered in schema_migrations (verified 2026-09-03, 24th session close: chain check 65/65 = 0001–0068, zero drift; 0067 = parent→admin chat [ADR-012, live 14/14], 0068 = parent display-name repair [live 11/11]). **0065_canonical_identity_codes has a HISTORY (ARCH-013):** it was applied to the live DB by an actor outside the repos after the 18th-session close (no file, no task, no problem entry; its SQL self-cited an unregistered "T-115"); the 19th session reconstructed the file VERBATIM from the live catalog (5/5 definitions byte-identical), committed it, re-applied it atomically (idempotent; registration preserved) and live-verified it 19/19 (verify_t-115.sql — deterministic identity-code functions + the batch_register_family idempotency contract; see docs/recovery/t-115-live-verification.md). 0064 = WEAK-030 expense transition guard (18th session, live 11/11); 0063 = Excel corpus alignment (16th session, live 259/259 × 6). KNOWN cosmetic registry conditions: live rows 0049/0050 carry names from the manual Management-API applications of earlier sessions ('expire_pending_approvals_fix', 'chat_read_receipts' ×2) that differ from the local filenames; and the live catalog carries NULL comments for 0065's functions because the Management API SQL endpoint DROPS COMMENT ON statements (AGENTS.md §11.1 quirk #1) — the underlying SQL is verified present in both cases; recorded here so the next agent does not misread either as drift.
- **Desktop** (Electron/React): the payment write paths are single-atomic-RPC only (T-011/T-012/T-013), the refund flow exists end-to-end (T-014), homework/roll-call persistence is fixed (T-023), payment preview ≡ collection (T-060); receipt numbers are SERVER-authoritative end-to-end (T-015 — migration 0058); global admins get a tenant switcher + honest null-tenant semantics (T-053); the sync queue handles ALL 15 entity kinds with a loud-fail default + session scoping + surfaced fallback (T-022); the justification review workflow is closed (T-040); LLM network paths refuse empty PII masks + EF audit failures are surfaced (T-055); identity codes on the sync path are deterministic + retry-stable (T-018); the year-end promotion flow is CANONICAL and atomic (T-041); the OnlineDetector is fail-closed and probes its own backend (T-050). 14th session: the Electron app STARTS again (T-097) and CHAT is real (T-099/T-100). **15th session: the financial READ side is canonical (T-103) — `installmentRemaining`/`totalOutstanding` honour the INV-4-family `due − paid − pending` rule (aligned with backend/website/Android), the Finance Tranches tab + student payments tab use the canonical helper (inline formulas removed), the parent dossier's `totalDue` is the NET obligation (charges + adjustments) with a "Crédit parent" card for overpayers, and `mapPaymentRow` surfaces the expected/excess hint columns. 16th session (T-105): the IMPORT ENGINE writes workbook-true shapes — no "Remise sur devis" ledger entry (the devis is already net; DATA-010) and `buildInstallmentRows` reconciles Σ tranches due to the ledger (C3) so a fresh import cannot reintroduce the DATA-003 family.** 69 files / 2192 tests ALL PASS; lint 0 errors; append-only migration guard green (60 files).
- **Android** (Kotlin/Compose): **T-050 landed (13th session, commit 3bc5cdd)** — the OnlineDetector is fail-closed, probes its OWN backend only (no third-party host, unconfigured = zero probes), and pullAll is deduplicated (single pull per cycle). **The 13th-session Android set is COMPLETE (T-026/T-054/T-062/T-063/T-064)**: the overdue rule is canonical (INV-4, due-date based, every call site passes the map — "Créances en Retard" is real); `regenerateForCycle` actually re-derives due dates (WEAK-006 dead); `WorkflowRunEntity` carries the real trigger (Room v12, MIGRATION_11_12); dead code removed (stub file, 833-line gallery, 76 audit constants, wrong KDoc); absence alerts = ≥3/current-term (core/Terms.kt mirrors terms.ts); the config dialog masks the anon key + no toolchain leak, and the inert `.invalid` fallback means unconfigured builds make ZERO real-host network calls (placeholder detection complete via `looksLikePlaceholderConfig`). 18th session: **T-024** (homework bare-UUID ids + promotion grade propagation — live 5/5), **T-039** (pullAll pulls the academic cluster with canonical mappers; every pull batch-upserts; notifications filtered + evicted by the 0019 RLS predicate with roles re-resolved per pull; Room v13 targetRole), **T-046-gap** (exportSchema=true + schemas/12+13.json committed + MigrationTestHelper v12→v13 4/4), **DTO-TENANT** (pull mappers default null tenant to ""), **T-082** (lint gate RESTORED: core-library desugaring — NewApi 337→0; lint-baseline.xml pins 117 pre-existing warnings, abortOnError; 2 SuspiciousIndentation fixed). Debug unit suite **38 files / 331 tests / 0 failures**; `assembleDebug` green — **30.7 MB** APK; **lintDebug GREEN** (first time ever). Toolchain re-bootstrapped at /home/z/my-project/tools (dl.google.com reachable again); gradle.properties memory-tuning COMMITTED (required for every test run in this 2-CPU container). KNOWN-then-FIXED: `testReleaseUnitTest` used to fail on GreetingScreenshotTest — **ARCH-012 CLOSED 2026-09-02 (19th session): both variants green now** — debug 38/331/0, release 36/326/0 (GreetingScreenshotTest + RoomSchemaUpgradeT046GapTest excluded from the release variant with documented reasons in app/build.gradle.kts; both remain the debug variant's canonical gates).
- **Website** (Next.js parent portal): realtime repaired (T-032); full-ledger paging (T-035); freshness fallback (T-033); migration copies REMOVED (T-048); notification badge = TRUE count (T-052); canonical port pruned (T-057); FCM token lifecycle closed (T-030); FCM auto-registration after the first user gesture (T-036). **14th session: the "Missing configuration" problem is fixed DURABLY (T-096 — public identifiers committed as code-level defaults in src/lib/public-config.ts, so a fresh clone works with NO .env.local; root cause ENV-300 [zod locale cascade] also fixed; live fresh-clone render verified, Google button enabled, banner programmatically ABSENT). Chat readiness: T-101 — typed 0061 columns, archived filter, last-activity ordering, preview rendering; read/reply/markRead already correct. 16th session (T-105): NEW corpus-equivalence suite `src/test/t-105-corpus-equivalence.test.ts` + fixture — `portalFinancialSummary` matches the canonical backend for ALL 259 parents of the workbook (262/262).** 18th session: AGENTS.md realtime claims corrected (WEBSITE-DOC) + the T-107 residual closed (publishable-default headless render: banner absent, Google button, live backend reached). 19th session: **AUTH-200 client-side UX mitigated (T-116)** — the disabled-provider server error now maps to a localized actionable message (fr/ar/en `auth.signin.providerDisabled`) instead of the raw English string; the typed Database interface gained 0065's four canonical RPC signatures; the t-052 test made portable (sibling probe). Suite 21 files / 429 tests ALL PASS; strict build green. **AUTH-200 (the blocker itself): Google OAuth provider STILL NOT enabled on the live project — portal sign-in is dead until the owner configures it (runbook docs/operations/portal-google-oauth.md; re-verified live 2026-09-02: external_google_enabled=false, client id/secret EMPTY; authorize endpoint answers 400 "Unsupported provider: provider is not enabled"; uri_allow_list already patched for localhost:3000/3100). THIS IS THE #1 USER-FACING BLOCKER AND IT IS OWNER-ONLY (~10 min in the Google Console + one PATCH).**
- Desktop homework + roll-call persistence FIXED (T-023, live 7/7); year-end promotion flow NOW IMPLEMENTED server-side + client-side (T-041, 13th session). Android homework UUID defect remains (T-024).

## 2. Major known problems (top of the risk stack)

174 total problems after the 2026-09-01 sixteenth session (session-16 additions: DATA-010 [double-remise, Critical → VERIFIED], DATA-011 [row-242 family never imported, Critical → VERIFIED]; extensions: DATA-008 corpus-level closure, DATA-003 classification CORRECTED, DATA-009 impact re-scoped to 2 genuine credits). The most dangerous remaining:
1. ~~`SEC-100`~~ **FIXED 2026-08-29 (T-001)** — desktop credentials removed.
2. ~~`SEC-101`/`SEC-102`~~ **FIXED 2026-08-29 (T-002)** — Android auth fail-closed.
3. ~~`BUSINESS-002`~~ **FIXED 2026-08-31 (T-011)** — single atomic path only.
4. ~~`CROSS-200`~~ **FIXED 2026-08-29 (T-019)** — Android sync surfaces 4xx/5xx.
5. ~~`TENANT-100`/`TENANT-101`~~ **FIXED 2026-08-31 (0053 reconciliation)** — tenant-scoped resolvers live.
6. ~~`ARCH-006`~~ **FIXED 2026-08-30 (T-080)** — `SupabaseOverdueAlertGenerator` wired into the assembly (8-test unit suite). Live integration = T-094.
7. ~~`ARCH-010`~~ **FIXED 2026-08-30 (T-088 + T-089)** — dashboard restructured (10-test regression suite); 4 hardcoded KPIs backfilled against real data.
8. ~~`ARCH-009`~~ **MITIGATED 2026-08-30 (T-091)** — `0051_chat_read_receipts.sql` added + applied live + registered in schema_migrations.
9. ~~`DATA-007`~~ **FIXED 2026-08-30 (T-087)** — test residue dropped via migration 0052 + test auth user + approval request deleted.
10. **`DRIFT-013` (NEW 2026-08-30)** — desktop code calls `.from("expenses")` but the canonical table is `expense_tickets` (with different status values). Dashboard KPI mitigated; wider port = T-093.

## 3. Source-of-truth decisions in force

Canonical writers = SQL RPCs; desktop TS engine = reference; deterministic identity codes; server-authoritative receipt numbers (ADR-002/003/004, Accepted). Android's target write architecture (ADR-005) and the single-equivalence-framework consolidation (ADR-006) are **Proposed** — direction set, implementation not started.

## 4. Active migrations

**0001–0064 — ALL applied + registered (verified 2026-09-01; 18th session close: 61/61, zero drift).** Session-18 addition: **0064_expense_transition_guard.sql** (WEAK-030 — the expense state machine + hard no-self-approval at the DB layer, applied live atomically via scripts/apply_0064_live.sh; verify_t-weak030.sql 11/11 live). Session-16 addition: **0063_excel_corpus_alignment.sql** (T-105 — the corpus aligned to the source workbook: double-remise cancelled with compensating adjustments, the missing row-242 MAMER family created, per-student devis alignment with last-tranche absorption, full waterfall replay; dry-run-validated then applied atomically via scripts/apply_0063_live.sh; verify_t-105.sql 259/259 × 6 live + verify_t-105-ops.sql 14/14 live). Session-15 addition: **0062_finance_reconciliation.sql** (T-103 — the financial data reconciliation: DATA-002 payment fix, 54 transport charges, dettes fold-in, devis correction, full waterfall backfill with payment_allocations + expected/excess; dry-run-validated then applied atomically via scripts/apply_0062_live.sh; verify_t-103.sql 8/8 live). Session-14 addition: **0061_chat_channel_completion.sql** (T-098). Session-13 additions: **0059_canonical_promotion_flow.sql** (T-041) and **0060_fcm_token_transfer_guard.sql** (T-030). Session-12 addition: **0058_receipt_number_server_allocation.sql** (T-015). Session-11 addition: **0057_canonical_tenant_resolver.sql** (T-025). Session-10 additions:
- **0053_tenant_scoped_rbac.sql** — RECONCILIATION (ARCH-011): captures the T-005 SQL that was applied live but never committed (is_global_admin, tenant-scoped resolvers, tenants_*/user_profiles_admin_update re-scoping).
- **0054_auth_trigger_no_client_metadata.sql** — RECONCILIATION (ARCH-011): captures the T-007 handle_new_auth_user rewrite.
- **0055_sec_definer_rpc_hardening.sql (T-006)** — applied live + registered; verified by scripts/verify_t-006.sql (9/9).
- **0056_expense_tickets_payee.sql (T-093)** — applied live + registered; payee column confirmed via information_schema.

Historical note: migrations 0034–0043 were a 10-migration fix-up chain after the "unification" of 0026–0028 (REG-001) — treat the chain as append-only from here.

## 5. Completed recovery work (most recent first)

- **2026-09-02 — TWENTIETH repair session (CLOSED — owner mandate "fix this auth thing or tell me what to do" + "apply the migration tokens, consistent everywhere"):** session-opening chain check 62/62 = 0001–0065 ZERO DRIFT (no new ARCH-011-class event). **T-119 TESTED-live** (AUTH-200 agent-side: site_url + uri_allow_list now carry `https://elimtiyaz-website.vercel.app` — PATCH 200 + GET-verified; localhost preserved; runbook + credentials §2.2 updated; the Google provider itself remains the ONE owner step). **T-122 TESTED** (MIG-TOKENS 15/15: dual-key health, RLS zero-rows × 5 tables, JWKS, chain, key consistency vs the owner-supplied values, auth census 1 user — the /v1/users REST path doesn't exist, AGENTS.md §11.1 quirk #4). **T-120 TESTED** (AUTH-201: pre-sign-in "Auth session missing!" alert + console noise eliminated — getSession-first, 4/4 suite). **T-121 TESTED** (AUTH-202: FCM env warning names the missing vars + Vercel location, 3/3 suite). **T-069 TESTED** (REALTIME-104 closed at unit level: Android realtime subscriptions — RealtimeSyncManager + SupabaseRealtimeEventSource + RealtimePullTarget seam; session-reactive; granular-pull routing with website cross-invalidation; 2s debounce; fail-closed online gate; 15-min fallback preserved; 11/11 suite; chat_messages deliberately deferred to T-102-follow-up). **T-124 TESTED** (DEAD-012 closed; 3 stale REALTIME summary rows flipped; AUTH rows added to the summary table; AGENTS notes fixed). **T-123 investigation** (lint-baseline AGP entries are a network-nondeterminism allowance — no change warranted; discovery documented). NEW discoveries persisted: secrets-plugin .env empty-value compile quirk; JRE-only system java; /v1/users REST path missing; provider-check-before-redirect-validation; Android applicationId vs namespace. Suites: desktop 75/2236/0 + typecheck + lint; website 23/436/0 + lint + strict build; Android debug 39/342/0 + release re-run + lint green. Chain 62/62. Repos zipped for push at closeout.
- **2026-09-02 — Nineteenth repair session (CLOSED — owner mandate "fix this auth thing + apply the migration tokens, consistent everywhere"):** the opening chain check found LIVE DRIFT (62 live rows vs 61 files — live-only `0065`, ARCH-013) → **T-115 VERIFIED** (migration reconstructed byte-identical from the live catalog, committed, applied atomically, live-verified 19/19; TS↔SQL equivalence pinned 9/9; typed RPCs registered desktop+website). **T-118 TESTED** (DRIFT-001 closed: mock layer + approve-signup-request EF aligned to the deterministic server contract; EF redeployed live, anonymous 401 sanity). **T-116 TESTED** (AUTH-200 portal UX mitigated; the provider itself remains owner-action-required). **T-117 TESTED** (12 stale registry headers flipped; t-052 portability fixed). **ARCH-012 TESTED** (both Android variants green for the first time). **T-107 follow-through** (§7 checklist re-run — both key formats healthy; committed values match the owner-supplied keys). NEW discoveries persisted: Management API drops COMMENT ON (AGENTS.md §11.1); batch_register_family requires date_of_birth; the hardcoded-absolute-path test-portability defect class. DEFERRED: T-069 + T-102-follow-up (unchanged reasons). Android toolchain re-provisioned (JDK 21 + SDK 35 — /home/z/my-project/scripts/android-env.sh). Desktop 75/2236/0; website 21/429/0; Android debug 38/331/0 + release 36/326/0; chain 62/62. EF approve-signup-request redeployed. Repos zipped for push at closeout.
- **2026-09-01 — Eighteenth repair session (CLOSED — owner-mandated 10-task batch: 8/10 COMPLETE, 2 deferred):** session-opening ritual DONE (chain 60/60 at open, MIG-TOKENS dual-key matrix, pristine baselines desktop 2215 / website 425 / Android 298). **T-024 TESTED+live** (homework bare-UUID ids + dispatcher prefix-strip + p_grade_level_code; live 5/5; Android 304). **T-039 TESTED** (pull completeness — academic cluster + RLS-mirrored notification cache with per-pull role resolution + Room v13 eviction; 16 tests). **T-046-gap TESTED** (Room schemas committed, backfilled 12.json via detached worktree; MigrationTestHelper 4/4). **DTO-TENANT TESTED** (nine demo-UUID pull defaults → ""; 7 tests incl. the no-tenant-filter tripwire). **WEAK-030 TESTED+LIVE** (migration 0064 — expense state machine + hard no-self-approval; live 11/11 both bypasses blocked, legal paths accepted; desktop 8 tests). **T-082 TESTED** (lint gate restored: desugaring NewApi 337→0, 117-warning baseline committed, abortOnError; APK 30.7 MB). **T-107-residual VERIFIED** (publishable-default fresh-clone render: banner absent, Google button, live auth reached). **WEBSITE-DOC DONE** (AGENTS.md realtime claims corrected to the T-032/T-033 truth). DEFERRED: T-069 + T-102-follow-up (full feature builds, fresh-session context; neither started). Android 38/331/0; desktop 73/2223; website 20/425; chain 61/61. Repos zipped for push at closeout.

- **2026-09-01 — Seventeenth repair session (CLOSED — owner-mandated batch, 10/10 COMPLETE):** session-opening chain check 60/60 (0001–0063) zero drift. **T-106 VERIFIED live** (DESK-LOGIN-200 — the owner's "desktop doesn't want to login": probes isolated invalid_credentials; admin credential reset via the auth admin API, value out-of-band; sign-in 200 with BOTH key formats + super_admin role check — AUTH-300). **T-107 TESTED + live** (MIG-KEYS-201 — ADR-009 dual-acceptance key migration: website committed default switched to sb_publishable_, desktop dialog dual-format, Android guidance; live dual-key matrix; KEYMIG-300). **T-104 TESTED** (ADR-010 — DATA-009 resolved option (b): writer preserved, display-credit derivation on desktop + website, 14 pinning tests). **T-034 TESTED** (CROSS-104 — CacheFreshness TTL+focus on all 9 seed-once caches; failed seeds now retry; CROSS-104b definition via the ADR-005 amendment). **T-108 TESTED** (DESK-CSP-202/SEC-113 — renderer CSP added; launch-verified dev+prod under Xvfb: zero warnings/violations). **Android batch TESTED: T-020** (SyncErrorClassifier — 5xx requeued, 4xx fail-fast), **T-021** (suspend syncNow + WorkManager retry/failure/success mapping), **T-046** (destructive migration fallback REMOVED — loud failure proven on real SQLite), **T-051** (AuditContext — real tenant + actor role on every audit row, zero demo literals, signed-out pull pulls nothing), **T-017 interim** (already-refunded guard + reason in the payload). Suites: Android 34 files / 298 tests / 0 failures (+23; baseline 275/275 re-established BEFORE changes — toolchain re-provisioned: JDK 21 + SDK 35, recipe in change-log); desktop 71 files / 2215 (+23); website 19 files / 425 (+10); builds green ×2. Repos zipped for push at closeout.

- **2026-09-01 — Sixteenth repair session (CLOSED — owner-mandated Excel-corpus equivalence, T-105 COMPLETE):** the session MANDATE was executed first (live chain diff 59/59 = 0001–0062, zero drift, JSON-diffed). The owner asked to "fully test this problem against all the other platforms using the real Excel spreadsheet sample … and make sure there is equivalence in the calculations across all 3 platforms in all the users in the spreadsheet and it is synced when someone does it in the supabase db", and to check whether the problem occurs in the workbook. **Findings:** the workbook is internally consistent (P = Σ payment cols 390/390; Q = L − P 390/390; L is NET of remise by formula) — the divergence lived between the workbook and the imported corpus: **DATA-010** double-remise (223 parents, Σ −9,709,700 DZD — the importer wrote the NET devis charge AND a −J adjustment), **DATA-011** row 242 never imported (a whole family: 255,000 DZD, same-name identity collision). **T-105 fix:** migration **0063_excel_corpus_alignment** applied live atomically with registration (compensating adjustments + family creation + per-student devis alignment + waterfall replay) + importer fixes (no REMISE ledger entry; C3 tranche reconciliation in `buildInstallmentRows`) + regression suite on the real workbook (5/5). **Verification:** verify_t-105.sql 259/259 × 6 (paid/netdue/balance/tranches/no-overapply/INV-4 — was 61/258 before); verify_t-103.sql 8/8 still TRUE; write-path ops 14/14 (payment/registration/pending-check/refund through the canonical RPCs, rolled back); **cross-platform: desktop TS 259/259 == backend, Android Kotlin 259/259 == backend (gradle run with in-container JDK 21 + SDK 35), website 262/262, triple comparator 304/304**; desktop 69 files / 2192 tests; website 18/415; chain 60/60. Full evidence: docs/recovery/t-105-live-verification.md.
- **2026-09-01 — Fifteenth repair session (CLOSED — owner-reported finance divergence, T-103 COMPLETE):** the session MANDATE was executed first (live chain diff 58/58 = 0001–0061, zero drift, JSON-diffed; CLI re-downloaded to /home/z/my-project/bin). **T-103** fixed the owner's report ("Finance tab says 100k paid; dossier says 30k paid / 40k remaining / 30k créance"): migration **0062_finance_reconciliation** applied live atomically with registration (payments-row fix 90k→100k per the Excel source; 54 missing transport charges; dettes folded into T1; SIDI MAMER's schedule reduced to the devis net; full waterfall backfill → 1,310 payment_allocations, expected/excess on all 888 payments) + the desktop read path made canonical (INV-4-family helpers, Tranches/student tabs, net dossier profile with Crédit parent card, mapPaymentRow hint fields). LIVE verification 8/8 with 0/258 residual mismatches (was 197 due + 181 remaining mismatches); the owner's exact parent spot-checked (due 337,000 / paid 493,500 / remaining 0 / credit 156,500 — superseded by T-105's 463,500/493,500/−30,000 which matches the workbook exactly). Desktop 68 files / 2187 tests ALL PASS; typecheck clean; lint 0 errors; chain re-verified 59/59. New registrations: DATA-008 (VERIFIED), DATA-009 (OPEN → T-104); DATA-001/002/003/004 flipped VERIFIED. Full evidence: docs/recovery/t-103-live-verification.md.
- **2026-08-31 — Fourteenth repair session (CLOSED — owner-requested batch, 7/7 tasks + chain check):** the session MANDATE was executed first (live chain diff 57/57 = 0001–0060, zero drift — script persisted at /home/z/my-project/scripts/verify_session_opening_chain.sh). **T-096** portal out-of-the-box config (public defaults committed — the fix now survives clone/push; ENV-300 root cause found+fixed; live fresh-clone render: banner ABSENT, Google button enabled; website 153/153). **T-097** Electron ESM start fix (owner's `npm start` crash; .cts→.cjs; NODE_ENV-aware isDev; launch-verified under Xvfb 30s alive, no ESM error, renderer loaded). **T-098** migration 0061 (chat backend completion; MIG-TOKENS atomic apply; verify 15/15 live; anonymous RPC → 401). **T-099** SupabaseChatRepository (chat slot off the mock; ID-space translation; 0051 append-only contract; 12/12 tests). **T-100** staff↔parent channel entry (openParentChannel + drawer "Messager" action; 3/3 tests). **T-101** website chat readiness (typed columns, archived filter, last-activity ordering, previews; 4/4 tests). **T-102** Android chat gap documented (ANDR-CHAT-200 + task). UNKNOWN-005 RESOLVED (owner decision) → ADR-008 accepted. Desktop 67 files / 2177 tests ALL PASS (+TEST-300 fix — session-13's all-pass claim was not reproducible); website 17 files / 153 tests; Android not run this session (toolchain not re-bootstrapped — no regression expected: zero Android code touched). AUTH-200 re-verified live + runbook written + uri_allow_list patched. New discoveries: ENV-300, TEST-300, ANDR-CHAT-200, AUTH-200 registration gap, the Management API uri_allow_list string quirk, the RLS silent-0-rows UPDATE semantics (documented in verify_t-098), and the T-050/0051 read_by guard interplay.

- **2026-08-31 — Thirteenth repair session (CLOSED — owner-requested ~10-task batch, 10/10 COMPLETE):** portal `.env.local` configured (the "Missing configuration" banner is GONE — live headless render verified); mandatory live chain check 57/57 (0001–0060); **T-041 TESTED** (migration 0059 live 10/10); **T-030 TESTED** (migration 0060 live 9/9; website 14 files / 135 tests); **T-058 TESTED** (append-only migration guard machine-enforced — matrix 9/9, suite 6/6; 3 pre-existing T-041 TS errors fixed); **T-050 TESTED** (OnlineDetector fail-closed + own-backend probe + pullAll dedup, both platforms — Android commit 3bc5cdd, debug 234/234; desktop 65 files / 2165 tests); **T-036 PUSH-103 portion TESTED** (website FCM auto-registration after first user gesture — 9/9 new tests, suite 144/144); **the Android set TESTED: T-026** (canonical overdue rule — 10/10), **T-054** (regenerateForCycle real + real trigger column — 7/7, Room v12), **T-062** (dead code removed — 5/5, ~973 lines, APK 29.8 MB), **T-063** (absence threshold ≥3/current-term — 10/10), **T-064** (config-dialog security + complete placeholder detection — 9/9); Android suite **275/275**; 12 problem entries flipped to TESTED. NEW problems registered: AUTH-200 (Google OAuth provider disabled — owner action, runbook in docs/operations/portal-google-oauth.md) and ARCH-012 (release-variant GreetingScreenshotTest pre-existing failure). Session closed with repos zipped for the owner to push.
- **2026-08-31 — Twelfth repair session (T-015, T-053, T-022, T-040, T-095→VERIFIED, T-052, T-057, T-055, T-018-partial + session-11 closeout + the live chain-consistency verification the owner requested):** 10-task owner-requested batch. Migration 0058 applied live (server receipt numbers) + verify 7/7; the live chain verified consistent 12/12; the run-overdue-scan EF rewritten to the batched pattern and VERIFIED live (the daily cron completes again — 819 overdue scanned in ~9s, zero duplicate alerts over 3 runs); EF audit failures surfaced (retry+throw+structured 500, 8 EFs redeployed); the justification workflow closed; the sync queue honest; badges truthful on both platforms; the canonical port pruned. T-041 (promotion flow) deliberately deferred to the next session — too large to complete safely in this session's remaining context (documented in next-task.md).
- **2026-08-31 — Eleventh repair session (T-011, T-012, T-013, T-014, T-023, T-025, T-068→VERIFIED, T-033, T-048, T-060):** owner-requested 10-task batch. The silent-fallback family eliminated from all desktop payment write paths; the refund flow implemented (UI + reason/actor propagation); homework + roll-call persistence fixed (live 7/7); migration 0057 applied live (canonical tenant resolver, histories writable — T-041 unblocked); EF permission resolution fixed + live-verified (SEC-109 → VERIFIED; BUG-NEW-004 discovered → T-095); website freshness fallback; migration chain unified across all three repos (client copies removed); payment preview ≡ collection + batch discount inputs. Registry sweep + this snapshot by session 12's opening (the 11th session ran out of context mid-closeout — its code was complete and pushed).
- **2026-08-31 — Tenth repair session (MIG-TOKENS, T-006, T-008, T-093, T-094, T-032, T-035, T-056):** owner-requested ~10-task batch with live Supabase credentials. (1) 0053/0054 live-drift reconciled (ARCH-011). (2) Migration 0055 applied live + registered — SEC-110/111/112 closed with a 9/9 live verification. (3) approve-signup-request redeployed (SEC-107). (4) Migration 0056 + SupabaseExpenseRepository (DRIFT-013 closed; WEAK-030 registered as the open follow-up). (5) T-094 live integration → ARCH-006 VERIFIED. (6) Website realtime + ledger paging + hygiene (suite 105→119). (7) Desktop hygiene batch 6/6. T-020 not attempted (SDK un-downloadable — documented).
- **2026-08-30 — Ninth repair session (T-088, T-080, T-089, T-091, T-087, T-092 all TESTED):** owner-requested dashboard restructure + backend testing + migration token reconciliation. (1) Desktop Dashboard UI restructured (ARCH-010 — 5 duplication/dead-code defects fixed). (2) `SupabaseOverdueAlertGenerator` ported (ARCH-006 closed — 8-test unit suite). (3) Migration 0050 drift reconciled (ARCH-009 — `0051_chat_read_receipts.sql` added + applied live + registered). (4) Test-residue cleanup (DATA-007 closed — `0052_drop_test_residue.sql` applied live + test auth user deleted). (5) Cross-platform migration token consistency verified (7/7 checks pass). (6) 4 hardcoded Supabase KPIs backfilled against real data (T-089 — verified live: totalStaff=0/pendingExpenses=0/attendanceRateToday=0/overdueAlerts=269). 3 new problems registered (ARCH-009, ARCH-010, DRIFT-013); 2 new tasks opened (T-093 — port desktop `expenses` repository to Supabase; T-094 — live integration test for `SupabaseOverdueAlertGenerator`).
- **2026-08-30 — Eighth repair session (T-083, T-084 TESTED):** live backend health check (11 findings → DATA-001…007, BUG-NEW-002/003); migration 0049 applied live; migration 0050 applied live (FCM verification); website portal restructured around the real data model; FCM token lifecycle hardened cross-platform; credentials sheet written.
- **2026-08-30 — Seventh repair session (T-016, T-027, T-061, T-031, T-029, T-071 all TESTED; T-079, T-004 promoted to VERIFIED):** balanced batch of 8 tasks across desktop + website + backend.
- Earlier sessions: T-002 (Android auth bypasses), T-079 (admin accounts), T-004 (cron EF auth), T-078 (desktop ESLint), T-081 (Android build), T-019 (sync errors), T-049 (website strict build), T-001 (desktop credentials), T-009 (website mock-auth), T-010 (desktop --no-sandbox), T-003 (desktop changePassword), T-000 (documentation reset).

## 6. Remaining high-risk work (recommended order)

Phase 0 security hotfixes (T-001…T-010) → financial integrity (T-011…T-018) → sync correctness (T-019…T-027) → account flows & realtime (T-028…T-035) → feature completion decisions (mostly blocked on unknowns) → architecture cleanup. See `task-registry.md`.

**Immediate next picks (per next-task.md):**
- T-093 (NEW, P2 High) — port desktop `expenses` repository to Supabase (DRIFT-013 wider port).
- T-094 (NEW, P2 Medium) — live integration test for `SupabaseOverdueAlertGenerator` (T-080 follow-up).
- T-005 (P0 Critical) — tenant-scoped RBAC resolver + admin policies (TENANT-100/101).

## 7. Blocked work

13 problems are BLOCKED, all on business/product decisions recorded in `unknowns.md` — most notably the activation-bind contract (`UNKNOWN-001`, blocks the EF consolidation), the Android write architecture confirmation (`UNKNOWN-002`, blocks the largest remediation), and the payment-EF gateway decision (`UNKNOWN-003`).

## 8. Current test coverage

- Desktop: 75 files / 2236 tests ALL PASS (session-19 final; +12 vs 18th: T-115 SQL-equivalence 9, T-018 mock-canonical 4 — minus one consolidated; typecheck clean; lint 0 errors / 384 warnings). Append-only migration guard green (61 files).
- Android: 38 files / 331 tests / 0 failures (session-18 final; baseline 298 re-established before changes + 33 new: T-024 6, T-039 16, T-046-gap 4, DTO-TENANT 7). **lintDebug GREEN for the first time** (T-082: desugaring + 117-warning baseline, abortOnError). The T-105 equivalence suite (304/304 incl. the 259 corpus scenarios) was NOT re-run this session — the financial engine is untouched by the 17th-session changes (verify: zero diff under core/ + financial-tests/). JDK 21 + SDK 35 re-provisioned at /home/z/my-project/bin (android-env.sh) — NOT part of the repo.
- Website: 21 files / 429 tests ALL PASS (session-19: +4 AUTH-200 UX suite; t-052 made portable); build green (strict).
- Live: migrations 0001–0064 applied + registered (61/61 chain-consistent); 12 canonical EFs deployed ACTIVE; financial three-way consistency verified 8/8 (verify_t-103.sql) AND the corpus-to-workbook equivalence verified 259/259 × 6 (verify_t-105.sql) AND the write-path ops verified 14/14 (verify_t-105-ops.sql, rolled back).
- Live Supabase: 1 auth user (`admin@elimtiyaz.dz` — password RESET 2026-09-01, T-106/AUTH-300; new value delivered out-of-band). New-format publishable key live-verified on all surfaces (T-107/ADR-009). Verification scripts persisted under `elimtiyaz-desktop/scripts/` and `/home/z/my-project/scripts/`.

## 9. Known cross-platform divergences (summary)

Payment write paths ×3, receipt numbering ×5 algorithms, overdue rule ×3 variants, attendance rate ×2 formulas, notification unread counts ×3 behaviours, freshness models ×3 (seed-once / 15-min pull / realtime+30s), sign-out semantics ×3. NEW this session: DRIFT-013 — desktop code calls `.from("expenses")` but the canonical table is `expense_tickets` (with different status values). Full list: `problem-registry.md` (CROSS-*, DRIFT-*, WEAK-019, DRIFT-013).

## 10. Known dead / obsolete code (summary)

Payment EFs never invoked (`DEAD-016`), desktop refund path unreachable (`DEAD-015`), legacy `homework_assignments` table + subscriptions (`WEAK-016`), dead SQL `promote_students` RPC (`ACAD-100`), `parent_student_links` table with zero writers (`DEAD-200`), unused canonical-port files on the website (`DRIFT-009`), legacy Android Room cache layer + design system (`DUP-005`, `DUP-003/004`), orphaned `receipts` table (`CROSS-101`). NEW this session: dead "PDF" report badge removed from the Reports tab (T-088); dead bottom Stat card removed from the Overview tab (T-088); dead auto-run `repos.overdueAlerts.run()` removed from `dashboard-page.tsx` (T-080); dead `_eq_test_fn` / `_eq_test_fn2` RPCs dropped via migration 0052 (T-087).

## 11. Known unknowns

11 open questions block 13 problems — see `unknowns.md`. No agent may resolve them by assumption.
